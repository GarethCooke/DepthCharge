# DepthCharge — Architecture

**Status:** Constitution. This document changes only by explicit decision (record it in §9).
Per-milestone work is specified in disposable briefs under `docs/briefs/`; this file is what
every session reads first and must not violate.

---

## 1. What this is

DepthCharge is a desk-top hardware market-data terminal: an ESP32-S3 driving a 64×64 HUB75
LED matrix behind smoked acrylic, rendering a live limit-order-book ladder — bids stacking
green, asks red, trade prints flashing white at the touch, a last-price sparkline along the
bottom, and an honest grey "stale" state whenever the data cannot be trusted.

It consumes three venues, in deliberate order of difficulty:

1. **Anvil** (`anvil.garethcooke.com`) — the author's own C++20 matching engine. Full top-N
   snapshots + per-fill trades; every frame carries a `seq`, but it is a single *global*
   engine counter, so one socket's received subsequence is sparse **and non-monotonic**
   (measured at M0) — the adapter synthesises its own `Seq` (§4). Easiest venue; also the
   only one we control end-to-end, which makes it the test rig.
2. **Kraken** — real L2 deltas, self-verifying via a CRC32 checksum over the top 10 levels.
3. **Binance** — the graduation exercise: buffered diff stream bracketed against a REST
   snapshot, with sequence-gap recovery. (Its partial-depth streams are an Anvil-shaped
   easy mode and may be used as a stepping stone.)

DepthCharge is the *consumer* side of the wire; Anvil is the *producer* side. The interface
to Anvil is Anvil's versioned `PROTOCOL.md` (vendored snapshot in `docs/vendor/`, re-pinned
at `4801ed8` on 2026-08-17), and
DepthCharge v1 requires **zero changes to Anvil**.

## 2. System overview

```
 venue ws ──► VenueAdapter ──► FeedEvent ──► BookEngine ──► DisplaySnapshot ──► Renderer
 (TLS)        (per venue)     (the one       (hot window    (SPSC double        (HUB75 DMA
                               boundary       + cold tail)   buffer)             / console)
                               type)
```

Two execution contexts share the same `engine/` code:

- **Harness (desktop)** — `harness/` builds the engine on the host, drives it from recorded
  replay files, checks golden expectations, benches it, and renders to a console ladder.
  This is where correctness lives.
- **Firmware (target)** — `firmware/` (PlatformIO, ESP32-S3) runs the identical engine.
  Core 0: network + adapter + book ("feed task"). Core 1: HUB75 render task. They meet at
  a seqlock/double-buffered `DisplaySnapshot` and nowhere else.

## 3. Repo layout

| Path         | Owns                                                                        |
| ------------ | --------------------------------------------------------------------------- |
| `engine/`    | Portable C++20 library: types, `FeedEvent`, book, adapters' *logic* (frame → events). No I/O, no ESP-IDF, no FreeRTOS. |
| `harness/`   | Host executables: replay runner, golden tests (doctest/ctest), console renderer, bench. |
| `firmware/`  | PlatformIO project: Wi-Fi, TLS WebSocket transport, tasks, HUB75 driver glue. Links `engine/` as-is. |
| `hardware/`  | KiCad carrier board, enclosure CAD + STLs, BOM.                             |
| `tools/`     | Capture scripts and other dev tooling (Python allowed here, nowhere else).  |
| `docs/`      | Milestone briefs (`docs/briefs/`) and vendored protocol snapshots (`docs/vendor/`). This file and `ROADMAP.md` live at the repo root. |

Transport (sockets, TLS) lives *outside* `engine/`; adapters in `engine/` accept received
frames as bytes/strings and emit `FeedEvent`s, so the identical adapter logic runs under a
Python-captured replay file, a host WebSocket client, or `esp_websocket_client`.

## 4. The FeedEvent contract (normative)

`FeedEvent` is the **only** type that crosses the adapter → engine boundary. Until code
exists this section is the source of truth; from M0 onward the header
`engine/include/depthcharge/feed_event.hpp` is, and must stay in sync with this intent.

```cpp
using PriceTicks = int64_t;   // price in integer ticks; per-symbol tick size (see below)
using Qty        = int64_t;   // quantity in integer venue steps; per-symbol qty step
using Seq        = uint64_t;  // adapter-normalised, monotonic per (venue, symbol) stream

enum class Side : uint8_t { Bid, Ask };

enum class GapReason : uint8_t { SeqGap, ChecksumFail, Disconnect, Overflow, Resync };

struct BookLevel { PriceTicks px; Qty qty; };

// Borrowed view of a Snapshot's levels; valid only for the duration of the sink
// call that delivered the event. A consumer that defers copies (M1).
struct LevelSpan { const BookLevel* data; uint32_t size; };

struct FeedEvent {
    enum class Kind : uint8_t { Snapshot, Delta, Trade, Gap };
    Kind kind; Seq seq;
    PriceTicks px; Qty qty; Side side; GapReason reason;
    LevelSpan bids, asks;   // Snapshot only
    // Snapshot: full replacement of both sides to the venue's stated depth,
    //           conveyed as the two spans (≤ kMaxSnapshotLevels each)
    // Delta:    one level, absolute quantity (qty == 0 ⇒ level removed)
    // Trade:    px, qty, aggressor side
    // Gap:      reason; book state must be treated as unknown until next Snapshot
};
```

Contract semantics:

- **Scaling.** Prices and quantities are integers scaled by per-symbol `tick_size` /
  `qty_step` supplied in venue symbol metadata. `int64_t` because crypto tick sizes and
  price ranges vary wildly across symbols and venues; exact integer equality is the point.
  Where a venue publishes no such metadata (Anvil does not), DepthCharge declares it in a
  `SymbolSpec` and the adapter **verifies** every wire price is exactly representable at
  that scale — a mismatch is a reported error, never a silent rounding.
- **Snapshot replaces; Delta amends.** A `Snapshot` discards all prior levels for the
  symbol. Depth beyond the venue's stated N is *unknown*, not zero.
- **Snapshot levels are borrowed, not owned.** `FeedEvent` stays a flat, trivially
  copyable value (invariant #7); a `Snapshot` points at an adapter-owned staging buffer
  that is valid only during the sink call. This is what keeps the boundary allocation-free
  with a single boundary type (invariant #2).
- **Seq is the adapter's problem.** Each adapter normalises its venue's native scheme into
  a single monotonic `Seq`. Anvil's wire `seq` is a *global* counter shared by all tickers
  and frame types, so a single socket's subsequence is non-monotonic (M0 measured 42
  backward steps in 5 minutes) and unusable for ordering: its adapter **synthesises** `Seq`
  from receive order and never raises `Gap{SeqGap}` — safe because Anvil's `snapshot`/`book`
  frames are idempotent full replaces. Binance uses `U`/`u` bracketing; Kraken has no seq —
  its adapter synthesises one and converts a CRC failure into `Gap{ChecksumFail}`. The
  engine's only rule: a discontinuity it is told about via `Gap` makes the book stale until
  the next `Snapshot`.
- **Gap is data, not an error.** Disconnects, ring overflow, checksum failures, and seq
  gaps all arrive as `Gap` events and drive the rendered stale state. No venue is required
  to *send* one: Anvil emits no gap/error frame at all, so `Gap{Disconnect}` is synthesised
  **transport-side** — socket close, or an RX watchdog whose timeout exceeds the venue's
  worst healthy inter-frame gap by a clear margin (host replay reads the same rule off
  `rx_ns`; see the M1 brief).

## 5. Book engine

- **Target design** (full form lands with delta venues, M4+): a **tick-indexed dense
  window** — a contiguous array of `Qty` addressed by `(px − anchor)` — over the hot band
  around mid, re-anchoring by bounded copy when the touch drifts out of the window; a cold
  tail (map) for levels outside it. On target the window lives in internal SRAM, the tail
  in PSRAM; on host it's all plain heap. This is deliberately the consumer-side twin of
  the "windowed dense array" named in Anvil's future work.
- **Phase 1 degenerate form** (Anvil-only, M1–M3): snapshots-only venues need no book
  maintenance at all — "adopt latest snapshot" *is* the engine, plus a trade ring. Build
  that first; do not gold-plate ahead of the first delta venue.
- **Output:** `DisplaySnapshot` — top ~27 levels/side (fits 64 rows with header, spread
  gap, sparkline strip), recent-trade ring (≥8), last px, status `{Live | Stale(reason)}`,
  symbol id, and **`age_ms` — the book's estimated queuing lag, with a `has_age` flag for
  "no reading yet"** (M4 stage A2). Published by version-stamped double buffer; the render
  side takes the latest complete version and never blocks the writer.
- **`age_ms` is queuing lag and nothing else.** Not time since the last frame, not time
  since the book last changed: *a book that has not changed is not old*. It is estimated
  from a deficit against the venue's declared liveness signal over a sliding window, and
  **nothing branches on it** — it is rendered as a number, never as grey (invariant #5).
  The engine does not compute it: `Book::publish` has no clock, and the feed side stamps
  the field immediately afterwards, same single writer (invariant #8).

## 6. Invariants (frozen — do not refactor through these)

1. **`engine/` builds on the host with zero ESP-IDF/FreeRTOS/Arduino includes.**
   *Why:* every line of book logic must be exercisable by ctest on the desk; this is the
   seam that made Anvil's demo cheap, inherited deliberately.
2. **`FeedEvent` is the only type crossing adapter → engine.**
   *Why:* venue mess (JSON dialects, seq schemes, checksums) stays quarantined; the book
   is written once against one vocabulary.
3. **Integer ticks and steps everywhere; no floating point ever touches book data.**
   *Why:* exact equality, no key drift, deterministic replay. Anvil's rule, verbatim.
   (Floats may appear only at the display-formatting edge.)
4. **The feed task is never blocked by the render task.** The hand-off is a wait-free,
   single-producer/single-consumer *latest-value* mailbox: publish overwrites in place and
   never waits on the render task. Superseded `DisplaySnapshot`s drop silently — lossless at
   the book level, because every published frame is a *complete* render state, not a delta,
   so the render side skipping v10→v13 loses no book information (only intermediate
   trade-ring / sparkline samples, which are best-effort). `Gap{Overflow}` is therefore a
   *feed-side* signal — a venue reassembly buffer, first produced at a delta venue (M4/M5) —
   never a render-hand-off event.
   *Why:* bounded per-event cost independent of consumer speed — same rule as Anvil's
   egress; on a microcontroller the render stall is the common case, not the rare one.
5. **Stale is a first-class rendered state.** Any `Gap`, disconnect, or resync greys the
   panel until a fresh `Snapshot`. A frozen ladder that looks live is the one unacceptable
   output. **Liveness is defined by events reaching the book, not by bytes arriving:** a
   transport that delivers frames the parser then rejects is *stopped* and must grey (M3
   Stage C — the firmware watchdog arms on a book-event for this reason; the current goldens
   pin a weaker byte-cadence definition, see §9). *Why:* the entire honesty of the object
   depends on it.
6. **No feature merges without replay coverage.** New adapter behaviour or book logic
   ships with a captured or synthesised trace and a golden expectation in `harness/`.
   *Why:* multi-session agentic work converges only when red/green is objective.
7. **Allocation-free steady state.** After connect + first snapshot, the feed→render path
   performs no heap allocation. *Why:* determinism and embedded heap health; also keeps
   host benches honest about target behaviour.
8. **One writer per state.** Only the feed task mutates the book; only the render task
   reads `DisplaySnapshot`. No third participant, no locks around the book itself.

## 7. Decisions already made (with rationale)

- **Separate repo from Anvil.** The boundary is the versioned wire contract, not shared
  code; DepthCharge is `PROTOCOL.md`'s second independent client, which is exactly the
  claim worth being able to make. Build/deploy isolation follows the portfolio-wide
  one-repo-per-deploy pattern.
- **One repo for engine + firmware + hardware.** Firmware, board, and enclosure versions
  travel together (MorayGlow pattern); the host-buildable `engine/` seam keeps this from
  hurting testability.
- **Anvil stays untouched in v1.** Known non-blockers live on Anvil's own backlog: chaos
  flag (deliberate frame drop/delay for deterministic gap testing), L2 incremental feed
  (DepthCharge becomes its test client when built), feeder realism. The WS `Origin`
  allowlist question is closed: M0 confirmed the deployed upgrade accepts a client sending
  no `Origin` header, so neither the capture tool nor the firmware needs one and no Anvil
  config change is required. Two DepthCharge→Anvil couplings are now on record — an Anvil
  change to either surfaces only as a dead panel, not an Anvil-side error:
  - **TLS-chain pin** — DepthCharge firmware pins ISRG Root X1 (and X2); an Anvil CA/chain
    change is a firmware-update event, not a transparent one.
  - **Republish-cadence assumption** — the liveness watchdog assumes Anvil's unconditional
    ~80 ms republish; Anvil's future incremental feed must ship a heartbeat, or DepthCharge's
    liveness inverts into crying-wolf (strain point 10).
  - **TLS-chain pin** — DepthCharge firmware pins ISRG Root X1 (and X2); an Anvil CA/chain
  change is a firmware-update event, not a transparent one.
- **Republish-cadence assumption** — the liveness watchdog assumes Anvil's unconditional
  ~80 ms republish; Anvil's future incremental feed must ship a heartbeat or DepthCharge's
  liveness inverts into crying-wolf (strain point 10).
- **Crypto venues for the real-data leg.** 24/7 markets suit a permanent desk object;
  free unauthenticated L2 depth; Anvil's synthetic flow is irrelevant to DepthCharge's
  correctness because the panel consumes wire semantics, not market truth.
- **Portable C++ first, target second.** Every milestone proves on the host harness
  before it touches the ESP32. Replay files are the ground truth artefacts.
- **doctest + ctest** for the harness (familiarity with the Anvil toolchain); JSON parsing
  choice is *not* fixed here — the harness may use a heavyweight parser, but whatever
  parses on the hot firmware path must respect invariant 7. Decide in-milestone, record
  in the brief.

## 8. Deliberately unspecified / out of scope

Unspecified on purpose (sessions decide, briefs record): file decomposition inside
`engine/`, class internals, console renderer aesthetics, exact capture format beyond
"NDJSON, one received frame per line, with a metadata header line".

Out of scope for v1: order entry of any kind, historical persistence, more than three
venues, any web UI (Anvil already has one), battery power.

## 9. Amendment log

| Date       | Change                    | Why |
| ---------- | ------------------------- | --- |
| 2026-07-23 | Initial constitution.     | —   |
| 2026-07-26 | **§1/§4 seq correction.** Anvil's wire `seq` is a global, per-socket non-monotonic counter; its adapter synthesises `Seq` from receive order and never raises `Gap{SeqGap}`. | M0 measured it on the live server (42 backward steps / 5 min, no reset across reconnect). The old text described a guarantee the venue does not provide; an adapter written to it would have gapped ~9 times a minute on healthy data. Safe because Anvil's book frames are idempotent full replaces. |
| 2026-07-26 | **§4 gains `LevelSpan`;** a `Snapshot` conveys its levels as two borrowed spans into adapter-owned storage, capped at `kMaxSnapshotLevels` (256/side). | §4 left "how the level list is conveyed" open until the phase-1 book (M1). Spans keep `FeedEvent` the *only* boundary type (inv. #2) while staying flat, trivially copyable and allocation-free (inv. #7). Cost: the lifetime rule — defer means copy. |
| 2026-07-26 | **§4 scaling made explicit for venues without tick metadata.** DepthCharge declares `SymbolSpec{price_decimals, qty_step}`; the adapter verifies exact representability. | Anvil's protocol carries no tick size or qty step (M1 known unknown). The choice is either declare-and-verify or silently round — and rounding book prices would break inv. #3 invisibly. |
| 2026-07-26 | **§4: `Gap{Disconnect}` is synthesised transport-side**, via socket close or an RX watchdog. | Anvil never sends a gap frame, so absence of data is the only signal. Pinning the rule in the constitution makes the M1 host replay and the M3 firmware transport provably the same contract. |
| 2026-08-07 | **Invariant #1 gains a target half.** `engine/` must compile for the ESP32-S3, not only for the host, and `dc_engine_target_check` enforces it with the PlatformIO xtensa compiler when one is installed. The language `engine/` may use is the C++20 subset that toolchain accepts — today `espressif32 6.5.0` / xtensa GCC 8.4, which means **no `<span>`, `<ranges>`, `<concepts>`, `<bit>`, no floating-point `from_chars`, and no `constexpr` algorithms**. | The host check proved "engine/ builds on the host" and was silently read as "engine/ builds". It does not follow: a `constexpr std::reverse_copy` compiles on the host GCC 15.2 and is ill-formed on GCC 8.4, so a header could be green on the desk and reject on the board with nothing to say so until M3 tried to link. Measured: all eight headers pass today, and the hot path costs 1541 B of `.text` at `-Os`. Raising the ceiling is a platform decision (pioarduino / IDF 5.x give GCC 14–15 and lift every restriction above) and belongs in a brief, not here — this line records what the ceiling *is*, not that it is permanent. |
| 2026-08-07 | **One definition of a valid trace.** `TraceReader::next` is it; `read_trace()` is a statistics pass over the same reader. A frame line must carry a string `type`, and `rx_ns` must not decrease. | `read_trace()` and `TraceReader` were written separately and had drifted: a frame with no `type` was rejected by one and accepted by the other, and `rx_ns` monotonicity was enforced by one and ignored by the other — with `dc_replay` plus the M0 goldens using the first and `dc_ladder` plus every M1 golden using the second. Two answers to "is this trace valid" in a project whose stated premise is that replay files are ground truth. Both rules kept, so the harness got stricter in the direction that fails loudly; both committed traces satisfy both unchanged. |
| 2026-08-07 | **§4 addendum: trailing silence is reported, never inferred.** A replay treats the end of a trace as the end of the timeline unless the caller supplies `ReplayOptions::end_of_trace_silence_ms`. | The RX watchdog is edge-triggered by the arrival of the next frame, so a capture that ended with 30 s of silence replayed as `Live` to the last frame while the panel's timer would have greyed it — the host replay and the M3 firmware disagreeing about the same rule. A file has no "now" and the capture tool does not record when it stopped listening, so the replay cannot infer the answer; a caller that knows says so. Off by default, so no committed golden moves. |
| 2026-08-07 | **§2/§5: the feed→render hand-off is a three-slot mailbox, not a seqlock or a two-slot double buffer.** `SnapshotChannel` keeps three `DisplaySnapshot` slots and one 32-bit atomic word; `publish` and `consume` each swap their slot into that word with a single `exchange`, so the writer's slot, the reader's slot and the ready slot are always three distinct objects. Both §2 and §5 say "seqlock/double buffer"; read them as naming the *contract* (version-stamped, latest-wins, never blocks the writer), which is unchanged, not the storage. | A seqlock and a two-slot buffer both let the reader copy a slot the writer is writing and then discard the result — the tear happens and the version check only stops it being drawn. Measured both ways at M3 stage A: a seqlock of exactly that shape delivered 4.8 M frames with **zero** tears reaching the consumer (x86, GCC 10, `-O2`), and ThreadSanitizer flagged it on the **first** frame. So the objection is not that a seqlock misbehaves on today's compiler; it is that the copy is a data race, stage A's definition of done asks for a clean TSan report, and buying one with a suppression would put that suppression on the single cross-core path, on a target compiler generation (xtensa GCC 8.4) nobody here controls. Two slots cannot avoid it either: the reader holds one, so a writer alternating across two must eventually land on the one being read. Cost of the third slot, measured on the target toolchain at `-Os`: +2,352 B of `.data` (1,176 → 3,528) and +114 B of `.text` (`publish` 30 → 87, `consume` 39 → 96). Invariant #4 survives intact — `publish` is a fixed copy plus one `exchange`, which GCC lowers to `xchg` on x86 and to a bounded `S32C1I` retry on the LX7. |
| 2026-08-08 | **§6 inv. #4: render hand-off is a silent-drop latest-value mailbox; `Gap{Overflow}` is feed-side only.** Superseded frames drop with no report (lossless — each `DisplaySnapshot` is a complete book state); `Gap{Overflow}` is reserved for a delta-venue reassembly buffer (M4/M5) and is correctly raised nowhere in the Anvil-only phase. | The §5 version-stamped double buffer has no queue to overflow, so the original "drops frames *and reports it* as `Gap{Overflow}`" named a mechanism the chosen hand-off doesn't have — a queue model imported into a mailbox design. Surfaced building the real channel at M3 Stage A (3-slot wait-free `SnapshotChannel`; slot-count rationale + measurements at `ARCHITECTURE.md:219` and the `snapshot_channel.hpp` header). The invariant's guarantee (feed never blocked, bounded per-event cost) is unchanged; only the false drop-reporting clause is corrected. |
| 2026-08-08 | **§7: the parse seam has two implementations, and the nlohmann one is the *specification* — its observable behaviour, accidents included.** A second `parse_anvil_frame` (streaming, allocation-free, target-bound) now links beside the M1 reference. Equivalence is not reviewed, it is executed: `harness/tests/test_parser_equivalence.cpp` compiles into both test binaries and holds both to one table, and `dc_tests_streaming` re-runs the M1 goldens and the malformed corpus against the replacement. Three divergences are deliberate and asserted as such (JSON nesting past 64; an *escaped* token unescaping past 64 bytes; the band between `DBL_MAX` and `1e309`). | §7 said only "whatever parses on the hot firmware path must respect invariant 7 — decide in-milestone". That left "and must it agree with the reference?" unstated, and the answer is not obviously yes: nlohmann 3.11.3 resolves duplicate object keys last-wins because its DOM is a `std::map` filled through `operator[]`; classifies an integer too large for `uint64_t` as a *float*, so an over-range qty is `BadShape` and not a saturated number; and treats a NUL byte as end-of-input mid-buffer. None of that is a decision anyone made, and all of it is load-bearing, because `AnvilAdapter` files `BadPrice`, `OtherTicker` and everything-else into three different counters and those counters are pinned goldens. Writing the second parser to a *reasonable* reading of JSON rather than to the reference would have moved goldens on malformed input while looking correct. |
| 2026-08-08 | **§4: the adapter's decode/semantics line stays at `AnvilFrame`; the *conversions* are shared instead.** `AnvilFrame` continues to carry `PriceTicks`/`Qty`, not raw decimal tokens. `kind_from_type`, `price_text_to_ticks`, `wire_qty_to_steps` and `aggressor_from_text` moved out of the nlohmann TU into `engine/include/depthcharge/anvil/anvil_scaling.hpp`, which both parsers include. | The M3 Stage B brief asked for scaling to be "lifted into the shared adapter", on the correct worry that two conversion paths is where invariant #3 drifts silently — a rewrite fails loudly on JSON structure but quietly on arithmetic. Moving the *boundary* would have done that badly: the nlohmann DOM's strings die with the parse, so it would have had to copy every one of ~250 tokens per frame into the frame to hand a token onward, and it would have rewritten `AnvilFrame`, the adapter, and the postcondition test — which are Stage B's own acceptance gate and which this milestone's constraints say do not change. Sharing the conversion achieves what the worry actually asked for (one implementation of #3, byte-identical in both parsers) at no cost to either. Recorded because the next two adapters (Kraken M4, Binance M5) should copy this split, not the one the brief described. |
| 2026-08-08 | **§6 inv. #7 on the target reads as "no net allocation and no fragmentation drift", not "no `malloc` anywhere in the stack".** The *engine* half keeps the strong form and is proven: `alloc_probe` counts zero global `operator new` across both full traces on the host, and the streaming parser's xtensa object has no undefined `operator new`, so it cannot allocate on the board either. The *transport* half cannot: `esp_websocket_client` dispatches every event through `esp_event_post_to`, which heap-copies the 28-byte payload and frees it when the handler returns. | Stage C is the first time invariant #7 met a library nobody here wrote. The measurement was not deferred to the bench — disassembling the shipped `libesp_event.a` shows `memset → calloc → memcpy → … → free` per post, which at a 4 KiB RX buffer and ~8.7 KB book frames is ~37 balanced pairs a second, squarely inside the "after connect + first snapshot" window the invariant names. Two things follow. First, the honest reading is the one above: what #7 exists to protect is determinism and a heap still healthy after a week on a desk, and bounded balanced churn of one fixed-size block threatens neither, whereas an unbounded or growing one does. Second, ESP-IDF heap tracing is unavailable to check it — the precompiled Arduino framework ships `CONFIG_HEAP_TRACING_OFF=y` — so `firmware/src/heap_probe.*` samples free bytes, the low-water mark and the largest free block instead, and is documented as detecting a *leak or fragmentation*, not the churn. An earlier draft claimed the low-water mark could see churn; it cannot (it is a since-boot minimum with no reset), and that claim was corrected rather than shipped. |
| 2026-08-09 | **The transport supervisor owns the reconnect cadence, and drives it from the socket state — never from the client's event stream.** `WsTransport::supervise()` is no longer only the clean-close rescue: it polls `esp_websocket_client_is_connected()` at 250 ms and restarts the client `kReconnectBackoffUs` (2 s) after the feed dies, then leaves each attempt `kHandshakeBudgetUs` of immunity before the next. `esp_websocket_client`'s own auto-reconnect stays enabled but is not relied on. | Third time the precompiled Arduino-ESP32 2.0.14 vintage has decided a design (after the certificate bundle and heap tracing): its `esp_websocket_client_config_t` has no `reconnect_timeout_ms`, so the library's 10 s `WEBSOCKET_RECONNECT_TIMEOUT_MS` is baked into the archive and unreachable — the only route to a shorter cadence is to preempt it. **The event-driven version of this was written first, shipped, and did not fire once**: it armed on `WEBSOCKET_EVENT_ERROR`/`CLOSED`, and this vintage dispatches neither for a dead socket — a read failure goes through `abort_connection()`, which raises `DISCONNECTED` (the library's `Error receive data` line is its own `ESP_LOGE` en route, not an event). Arming on `DISCONNECTED` instead is the trap, because `stop()` runs through the same `abort_connection()` and every restart would re-arm the timer that caused it. The socket state is the only signal that needs no theory about the library's internals, and the same poll had been driving the 20 s backstop correctly all along. Recorded because the general rule outranks the incident: **on this stack, supervise on observed state, not on reported events** — and an IDF bump that supplies `reconnect_timeout_ms` should set the field and delete the preemption rather than leave two mechanisms racing. |
| 2026-08-10 | **The transport keeps TWO `esp_websocket_client` handles and never waits for a dead one; the library's auto-reconnect is now OFF.** A reconnect starts the idle handle and publishes it as live; the handle that just dropped is left to expire on its own clock. `esp_websocket_client_stop()` is not called anywhere. The 2026-08-09 entry below said the supervisor "restarts the client `kReconnectBackoffUs` after the feed dies" and left auto-reconnect enabled as a backstop — both halves are superseded. The reconnect *policy* moved to `firmware/src/ws_supervisor.hpp`, which is ESP-IDF-free and host-tested (`test_ws_supervisor.cpp`), leaving `WsTransport` the platform half only. | **The backoff was never additive, and the brief that asked for it to be cut would have bought nothing.** Read out of the shipped archive rather than from IDF source that may not be what was built: `.literal.esp_websocket_client_init + 0x30` holds `0x2710`, so `wait_timeout_ms` is 10 s, and `.text.esp_websocket_client_task + 0x3b5` is `vTaskDelay(wait_timeout_ms / 2)` on every WAIT_TIMEOUT pass — **a socket that aborts puts the library's task to sleep for 5 s.** `esp_websocket_client_stop()` sets `run = false` and then blocks on `xEventGroupWaitBits(STOPPED_BIT, portMAX_DELAY)`, and the task cannot see the flag until it wakes, so stop() blocks for whatever is *left* of that 5 s. Our backoff and the library's sleep are therefore anti-correlated and their sum is a constant: run C measured 2445 ms of backoff + 2545 ms blocked = 4990 ms, and 5000 + 4018 (connect) + 435 (Anvil's snapshot) = 9453 ms against the panel's own `grey for 9451 ms`. Cutting the backoff to 250 ms would have moved 1.75 s out of a `vTaskDelay` and into a blocking call on loopTask, with the panel grey for exactly as long — a change that measures as "no effect" and reads as "the lever is exhausted". The only way past it is to stop needing the sleeper, hence the spare handle; auto-reconnect must go off with it, because otherwise the retired handle wakes 10 s later and opens a **second live socket to Anvil** that nothing here is watching (`.text.esp_websocket_client_task + 0x36e` takes the `run = false` arm when `auto_reconnect` is clear, and `+ 0x535` in `init` is the `auto_reconnect = !config->disable_auto_reconnect` that feeds it). Costs, measured by building HEAD with and without in a scratch worktree: **+8 B RAM and +956 B flash** statically, plus ~10 KiB of heap for the spare's buffers taken once at boot (invariant #7 untouched — steady state still allocates nothing) and one 6 KiB task stack for the ~5 s a retired handle overlaps its replacement. Only one TLS context is ever live, because `abort_connection()` closes the transport at the drop — which the 2026-08-09 bench saw as `free=172708 (+47780)` the instant the socket died. **Accepted trade, stated rather than buried:** the library is no longer even a nominal backstop, so `supervise()` is the *only* recovery path — justified because across two bench outages the backstop was given 12 s twice and recovered neither. Fifth time this precompiled vintage has decided a design, after `esp_crt_bundle`, `heap_trace_start`, `reconnect_timeout_ms` and `CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS`; the general rule from the 2026-08-09 entry stands and gains a second clause — **supervise on observed state, and price the library's blocking calls out of its own object code, not out of its docs.** |
| 2026-08-14 | **The clkphase half of the entry below is superseded: on the owned transport, `clkphase = false` no longer costs the feed, and it ships — the header-ghosting fix is free.** The general constraint the entry created (panel timing changes are feed-affecting until a carrier board with a ground plane says otherwise) stays standing; what changed is the verdict of the one experiment it was built on. | A/B/A on 2026-08-14, same divider 24, RSSI −38…−47, ~10 min per arm against the same morning's 25-minute `true` baseline: **true 6.58 msg/s median → false 6.78 → true 6.48**, byte rate 46–48 KB/s at the TCP-window ceiling in every arm, `connects=1 sock_gaps=0` throughout — the 2026-08-11 collapse (~6 → ~2 msg/s, reproduced both directions that day) does not reproduce on the rewritten client. Which variable actually moved — the owned client's socket path (4 KiB reads on its own core-0 task, no esp_event hop) or that week's mesh weather — is deliberately left open: the experiment separating them needs the old client, which the acceptance is about to delete, and the setting does not wait on the answer. Recorded above the old row rather than editing it, per this table's own rule. Logs `device-monitor-260814-{151322,154752,155929}.log`; `tools/board_log_lag.py` is the analysis. A same-day corollary of the clean 25-minute connection: with the socket deaths gone, nothing "un-lags" the book by accident any more (2026-08-13 entry), and the panel wore 873 s of age in 25 min — staleness is now the object's dominant defect, exactly as that entry predicted. |
| 2026-08-11 | **The association is supervised on observed state too, not just the socket — and the panel's own switching is now a constraint on the radio.** `WifiSupervisor` (`firmware/src/ws_supervisor.hpp`, host-tested) rejoins the station on its own clock when it has been down past a threshold, because Arduino's auto-reconnect list omits `WIFI_REASON_AUTH_FAIL` and therefore stops trying permanently after one refused retry. Separately and unrelatedly in cause but identically in shape: `clkphase = false` fixes the panel's ghosting and collapses inbound Wi-Fi to a third, so it stays at the library default and the artifact is recorded as an M6 carrier-board requirement in `hardware/BRINGUP.md`. | Two more instances of the rule 2026-08-09 already stated, and the reason it earns a third entry is that both were *invisible until hardware existed*. The Wi-Fi one is the same species as the socket supervisor: a library policy that looks like it covers the case and does not, found only because the panel made a permanent grey visible in a way a serial counter never did. The second is new in kind and worth the constitution knowing: **the display half of this object can degrade the feed half through the physical layer, with no shared code path and nothing either half can measure.** `clkphase` reaches one GPIO-matrix inversion — no DMA, data-rate or CPU change — so the coupling is ~14 outputs switching simultaneously down a bare ribbon beside a 2.4 GHz antenna. Nothing in `engine/`, the invariants or the task model is wrong; the boundary that leaked is one the software architecture does not model at all. **M4+ should treat panel timing changes as feed-affecting until a carrier board with a ground plane says otherwise**, and M6's buffers, short leads and ground plane are load-bearing rather than cosmetic. A related measurement fell out of it: the S3 backend *buckets* `i2sspeed` (anything ≤ 10 MHz → divider 16), so the panel has always run at 10 MHz while the library reported a refresh computed from 8 — every `refresh=` figure before 2026-08-11 is ~25% low, and DESIGN strain 15's timing margin was right by luck. |
| 2026-08-10 | **§6 #5 is now enforced by the type system and the build, not by a rule: the renderer cannot name a colour.** `firmware/src/ladder_render.hpp` emits `Ink` — a role — and a `Palette` maps `Ink` to RGB. The palette is selected exactly once, from `snap.status`, where `PanelCanvas` is constructed; `static_assert(all_grey(kStalePalette))` and `static_assert(none_black_except(kStalePalette, Ink::Count))` are compile-time proofs that a stale panel carries no hue anywhere and no pixel of it is black. The HUB75 driver is compiled `NO_GFX`, which removes `print()`/`setTextColor()` and with them the last route to a pixel that did not come through an `Ink`. | The obvious implementation of "stale greys the panel" is to check `snap.live()` wherever a colour is chosen, and that is a rule someone has to keep. It fails the first time a session adds a widget and forgets, it fails silently, and what it draws is a coloured ladder off a book nobody should trust — the one output §6 #5 calls unacceptable. Making it structural costs one enum and one array and turns three questions mechanical: a new `Ink` with no stale entry fails the build, a stale entry given a hue fails the build, and `test_ladder_render.cpp` pins the complement (the `Ink` grid over the ladder region is byte-identical Live and Stale — geometry stays, hue goes). Recorded here rather than in the brief because the rule outlives M3: **any future render surface — M7's board mode, the optional web mirror on the backlog — inherits the obligation, and the cheapest way to meet it is to reuse this split rather than re-earn the discipline.** The same session also fixed the row budget the same way: `kLevels` is *derived* from the leftover rows and clamped to `kDisplayLevels`, with a `static_assert` that the 64 rows balance, so a header that grows costs levels on the panel instead of silently overrunning it — which is what makes "draw fewer levels, never change `kDisplayLevels`" a build failure rather than an instruction. |
| 2026-08-10 | **The panel's memory is a measured runtime decision taken *behind* the network's, and the boot order is the mechanism.** Wi-Fi associates, then `Panel::begin()` sizes its framebuffers against the internal DMA-capable heap that is actually left (holding back a deliberately generous reserve), then the WebSocket client starts. Colour depth is chosen by calculation from that measurement — 8 down to 3, double buffered, before single buffering is considered — and `begin()` is called **once**. A panel that fails to come up is never fatal. | Both halves want the same pool: the HUB75 framebuffers are internal DMA-capable RAM (64 KiB at eight-bit double buffered) and so are Wi-Fi's, lwIP's and mbedTLS's. Whoever allocates first wins, so the order chooses which half degrades when it is tight — and the two failures are not comparable. A panel that drops a colour bit is cosmetic; a TLS handshake that cannot allocate is a dead object with an honest grey screen. There is no retry loop for the depth, and that is read out of the library rather than assumed: `setupDMA()` returns false leaving every row buffer it already took attached, above its own `// TODO: should we release all previous rowBitStructs here???`, so a second `begin()` appends more rows to the same vector — a failed attempt costs most of a framebuffer permanently and the rung beneath it then fails for a reason unrelated to the depth it was testing. Sixth time this precompiled vintage has decided a design (after `esp_crt_bundle`, `heap_trace_start`, `reconnect_timeout_ms`, `CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS` and the blocking `stop()`), and the standing rule from 2026-08-09 applies unchanged: **price the library's behaviour out of its own object code and source, not out of its docs.** |
| 2026-08-11 | **The 2026-08-09 shedding entry below is superseded: Anvil does not shed to a slow consumer, it QUEUES — and the durable rule is MEASURE FRESHNESS, NOT CADENCE.** The original entry is deliberately left standing and unedited; §9 records decisions *and their reasoning*, and the reasoning is what has to be readable for the mistake to be worth anything. Two things change. (1) The factual claim — "4× fewer messages, gaps unchanged, only `book` shed, `summary` and `trade` intact" — is withdrawn. (2) The rule that replaces it: **a measurement of rate or of inter-message gap cannot distinguish a shed stream from a queued one, and only freshness against a reference clock can.** No session may conclude anything about a venue's backpressure from a cadence measurement again, and that binds M4/M5 at Kraken and Binance, where a stale delta stream is not idempotent and cannot be papered over by the next full replace. | **The board found it and the desk proved it.** A clean single-connection bench run on 2026-08-11 — `parse=0 cont=0 trunc=0 oversize=0 qfull=0 abandoned=0`, `no_slot=2` of 1,703, both cores 85–92% idle, `behind=2/6`, `a→e worst=20 ms` — displayed a state on the panel **96–98 seconds** after the web client already had it. Every instrument in the firmware read healthy, because every instrument in the firmware measures *stopped* (the RX watchdog, the gap histograms), *wrong* (the parse counters, the reject log) or *whose fault the stopping was* (the stall probe). **None of them can see a feed that is running perfectly and is a hundred seconds behind the market**, and §6 #5 cannot either: it protects against stopped and against wrong, and has no concept of *old*. Two symptoms that had looked like separate bugs collapse into it — the "frozen bid" (`10.0034 x67` across 502 published versions and 87 seconds) was the board faithfully replaying a real stretch of history in which the bid genuinely did not move, and the "5 Hz refresh" was `summary` at 0.82/s against 2.00/s and `book` at 5.60/s against 13.44/s — *two independent counters, the same 41% ratio.* The desk measurement that settles the mechanism is `tools/anvil_freshness_probe.py`, written for this: two sockets from one process, one drained flat out and one sleeping 250 ms per message, matched on Anvil's global wire `seq` so "how stale is this message" is a subtraction against a real clock rather than an inference from a rate. Over 150 s — `book` 3.48/s of 13.66/s = **25.5%**, `summary` 0.51/s of 2.01/s = **25.7%** (so the per-kind claim does not reproduce: every kind is thinned by one fraction, which is a delayed byte stream, not coalescing), lag rising **linearly to 111 s across all eight bins with no plateau**, at **+0.745 s per second measured by seq-matching and +0.745 s/s predicted independently from the drain fraction alone**. Implied per-socket application queue when the probe disconnected: **~1,746 messages, ~12.4 MB, still growing.** Also measured the same day, over **30 minutes** on one unthrottled socket, and worth pinning because three prior sessions argued about it: Anvil is **15.70 msg/s** against M0's July 15.5, `summary` is **2.0003/s** (a 499.9 ms period, exact to 0.02%), `trade` is 0.15/s, and **the socket held all 1,799 s without a drop** — so the venue has not slowed, the board's 6.07 msg/s is the board's, and Anvil is not closing sockets on a timer. Recorded here rather than only in the brief because the failure mode outranks the incident and this milestone has now paid for it four times: **a measurement that answers a nearby question gets read as answering the one that mattered.** The 2026-08-09 probe was a good control, honestly run, and its numbers are all still true; what was wrong was the sentence written under them. **And the rule bit its own author the same afternoon:** a 60-second run of the new probe reported the same broadcast as "2.017/s", which was taken as a real 0.85% departure from the 500 ms constant and used to justify an analysis of estimator drift and a predicted false alarm. It was `n/span` where the span holds `n−1` intervals — a +0.84% bias at n=120 that vanishes by n=3,600. **Rate is (n−1)/span; a measurement of a period must be made over minutes, not seconds; and an instrument built to catch this class of error is not exempt from it.** `tools/anvil_drain_probe.py` keeps its original text with the supersession stated above it, for the same reason this row does not delete the one below. |
| 2026-08-16 (eve) | **One arithmetic correction to the row below, and to the four documents that quote it: the soak ran at 51–79% of Anvil's wire, not "60–80%".** 56/110.4 = 50.7% and 87/110.4 = 78.8%; there is no unit reading that rescues the low end. The row's measurements are all unaffected — the hourly medians, the slopes and the RX budget are what they were, and the conclusion (path-bound, nothing left on this side) does not move. Corrected in place in `ROADMAP.md` and `docs/DESIGN.html`, which are living documents; **left standing in the briefs and in the row below**, per this table's own rule. | **The cause was a label, and it is now fixed at the source.** `tools/anvil_freshness_probe.py` and `anvil_drain_probe.py` both divide by 1024 and both printed the result as "KB/s"; the firmware does the same (`render_task.cpp` computes KiB and prints "KB/s"). Every figure in this repo has always been binary and consistent — but with two spellings in play, a ratio got eyeballed instead of divided, and then quoted five times without anyone redoing the division. The probes' labels now read `KiB/s` with a comment saying why; **the firmware's is the one instance left**, deliberately, because it could not be compiled or flashed in the session that found it, and an unbuilt firmware edit is a worse thing to leave behind than a wrong label. Nothing parses either string (`tools/board_log_lag.py` does not match on it), so the change is cosmetic in both places. General rule, cheap and worth having: **a percentage in prose is a claim, and claims get recomputed when they are quoted — a ratio nobody re-derives is a ratio nobody has checked.** |
| 2026-08-16 | **The 2026-08-14 row below is corrected by a day of data: the ~44 KiB/s ceiling was neither the window nor the RX path — it was the transatlantic path at UK evening peak, and the object now runs at 56–87 KiB/s (60–80% of the wire) all day with the RX loop io-bound and its processing at ~0%. The staleness clock has a 71.6-minute ceiling of its own that must be fixed before the next soak is read.** A 23.6-hour soak on the wnd17232 build (`device-monitor-260815-002728.log`, 26 MB, one continuous monitor, four bench-power brownouts annotated by the owner and excluded): hourly median inbound ran **87 KiB/s at midnight, 56–60 at 07:00–10:00, 70–78 through the afternoon, 65–70 at 17:00–20:00, 73–81 from 21:00** — the previous night's 44 KiB/s did not recur, and neither did any hour below 56. `-- rx` read **wait 0 / read 98–99 / feed 0 / other 0–1** in every hour of the day: no wait means the socket always had bytes when asked, ~0 feed means deframe+reassemble+pipe cost nothing, so what remains inside `read` is TLS decrypt plus in-kernel delivery — the board is bound by how fast bytes come off the wire into it, not by anything it does with them. Lag slope by connection: +0.243 s/s (00:27–06:41), +0.294 (06:41–11:14), +0.323 (11:14–13:15), **+0.083 (13:15–00:06, 10.9 h)** — down from +0.57–0.59 on every 5744-window run, and the last segment's 0.083 is within reach of zero. Consequences: (1) the wnd17232 window is **load-bearing** and stays; the 44-vs-213 KiB/s comparison that convicted the RX path in the row below was a congested evening against an uncongested one and is withdrawn; (2) the residual drift is a path-bandwidth question this firmware cannot close from its side — the sized fix remains Anvil's delta feed (a tenth the bytes), and until then the object runs tens of seconds to minutes behind at UK peak and honestly says so; (3) **`staleness.hpp`'s `SecondsText` saturates at 4294.9 s (uint32 µs), which every `age`, `worst`, `run` and `over` field on the `-- age` line inherits** — three of the four segments hit it, so peak age is unreadable past 71.6 min and `board_log_lag.py`'s slope must be fitted before saturation. Fix owed before the next soak. | **Why the row below got it wrong, stated so it does not happen a third time:** it convicted the RX path on one evening's 44 KiB/s against a diag flow measured *the same evening* — an A/B where the uncontrolled variable (path congestion, which varies by hour) was the one that moved. The stopwatch instrument (`rx_budget.hpp`) was the right response, and its first reading eight minutes after midnight already showed 100+ KiB/s at `feed 0%`; the day of hourly data is what turns that from an anecdote into a curve. General rule, now paid for twice on this milestone: **a throughput comparison across time on a WAN path is not an A/B unless the hour is controlled.** The four brownouts are bench power (the owner switching the panel's independent 5 V supply, 1:1 correlated) and are excluded from every figure above by the owner's instruction. |
| 2026-08-14 | **The entry below is half-superseded by its own prescribed experiment: the TCP window names A ceiling, not THE ceiling. The framework rebuild was executed, verified in the binary, and moved nothing — the binding ~44 KiB/s limit is in the firmware's own RX path, and the venue-byte-rate design input for M4/M5 must be sized against THAT, not against `WND / RTT`.** `liblwip.a` was rebuilt from the exact shipped vintage (esp-idf v4.4.6 tag commit, same toolchain generation, the package's own build sdkconfig) with `CONFIG_LWIP_TCP_WND_DEFAULT` 5744 → 17232, symbol-tables proven identical and the `tcp_alloc` window literal proven changed, and shipped as `[env:depthcharge-wnd]` over a local package override. The acceptance run: 6.78 msg/s median, 44.0 KiB/s, lag slope +0.571 s/s — inside the noise of every 5744 baseline. The same evening, the repo's own diag with the same rebuilt lwIP measured **raw tcp-bulk at 213 KiB/s for 95 s on the −78 dBm node** and **anvil-ws (TLS+WS, no parser, no book) at ~79 KiB/s**, against the production firmware's 44: stack, radio and window exonerated; the pin is the serialized read→reassemble→parse→apply→publish RX path. The window change stays shipped because it remains **necessary** — 5744/87 ms genuinely caps Anvil inbound at 65.5 KiB/s once the RX path is fixed (the UK tcp-bulk server at ~20 ms RTT cannot exercise that bound) — it is just not sufficient. | **Two lessons paid for in one night, both generalisations of rules this table already holds.** (1) "Before attributing a rate to the code, measure the path the bytes actually take" cuts both ways: the 2026-08-11 analysis measured the path but stopped at the first constant whose arithmetic fit — 44 measured against a 65.5 ceiling is 67% "window efficiency" only if nothing else pins 44, and the falsifying experiment (change the window, watch nothing move) was also the fix attempt, which is the only reason it ran. A ratio against a theoretical ceiling is not a mechanism. (2) A hazard for every future framework rebuild on this vintage: **`CONFIG_LWIP_TCP_RECVMBOX_SIZE` must stay 6.** Kconfig's own guidance (WND/MSS + 2 = 14) produced a board that completes TCP connect and TLS handshake and then never receives the HTTP 101 — mapped across the full 2×2 (5744/6 healthy, 17232/6 healthy, 5744/14 connects then watchdog-greys in 25 s, 17232/14 never upgrades), mechanism formally open, details in `C:\local\framework-arduinoespressif32-wnd17232\WND17232-README.md` and the transport brief's 2026-08-14 (late) session log. The RX-path instrumentation and redesign is the next brief; a DESIGN §08 strain entry is owed with its numbers. |
| 2026-08-11 | **The board cannot keep up with Anvil, and it is arithmetic rather than a defect: a 5,744-byte TCP receive window across an 87 ms transatlantic RTT is a 65.5 KiB/s ceiling against a 110.4 KiB/s stream. No firmware lever reaches it.** DepthCharge on this framework can take at most **59%** of Anvil's feed at perfect window efficiency, and achieves 40%. The consequence is permanent, growing staleness — measured to **328 s** on the panel — and it is not fixable in `firmware/`. Two things follow for the constitution. (1) **A venue's byte rate is now a design input, not an afterthought.** M4's Kraken and M5's Binance must be sized against `WND / RTT` before an adapter is written, and their full-depth streams are larger than Anvil's. (2) **Anvil's backlogged "sequenced incremental L2 feed" is promoted from a nice-to-have to the thing that makes this object work at all** — a delta feed at a tenth the bytes fits inside the existing window. Anvil is still not modified from here; this is a note on their backlog. | **Everything else was eliminated first, and the eliminating measurement was in the repo the whole time.** Across 548 healthy 10-second windows the board's inbound *byte* rate is a flat ceiling — 41.1–44.2 KiB/s mean, max 49.8 — while message rate varies 2.6×, mean message size 2.3× and reads-per-message 1.9×; and across runs it is 42.70 KiB/s with **no panel** (2026-08-09, `free=124812` proves the framebuffer was never allocated), 41.54 and 43.29 with the panel drawing. **Byte rate moves 4% while message size moves 46% and the panel comes and goes.** That invariance refutes every per-message and per-read mechanism (they predict constant messages/s, so byte rate would track message size — it does the opposite), refutes the `kWsRxBufferBytes` lever outright (reads-per-message already varied 26% at constant byte rate: the natural experiment ran itself and returned zero), and refutes the panel-emissions candidate that §9's own `clkphase` entry makes tempting (the no-panel run sits inside the panel runs' scatter). Wi-Fi power save is separately dead on the board's banner — `driver ps=0` from an `esp_wifi_get_ps()` readback at −36…−47 dBm. What remained was flow control, and its missing parameter was the one nobody had measured: **`anvil.garethcooke.com` is `52.204.246.224`, AWS us-east-1, and eight TCP connects give a median RTT of 87.4 ms.** ICMP is filtered, which is why three sessions reasoned as though the server were on the LAN. `5,744 / 0.087 = 65.5 KiB/s`; measured 44.2 = 67% of that ceiling and 40% of the wire, matching the drain fraction with no fitting. The window is unreachable: `.literal.tcp_alloc+4` is `0x00001670` in the shipped `liblwip.a` and `CONFIG_LWIP_WND_SCALE` is absent from the sdkconfig, so both `TCP_WND` and scaling are compile-time — **a framework rebuild (pioarduino / IDF from source), which is milestone-weight and was pre-authorised to stop here.** Eighth time this precompiled vintage has decided a design, after `esp_crt_bundle`, `heap_trace_start`, `reconnect_timeout_ms`, `CONFIG_FREERTOS_GENERATE_RUN_TIME_STATS`, the blocking `stop()`, `setupDMA()` and the `AUTH_FAIL` list — and the first one that decides what the *product* can be rather than how the firmware is written. Recorded with a general rule this milestone earned four times over: **before attributing a rate to the code, measure the path the bytes actually take.** |
| 2026-08-13 | **The recurring socket deaths are `esp_websocket_client`'s own, not the link's — and the association now joins by signal, not by first answer.** Two changes. (1) `connect_wifi()` sets `WIFI_ALL_CHANNEL_SCAN` + `WIFI_CONNECT_AP_BY_SIGNAL` before `begin()`: the framework default (`WIFI_FAST_SCAN`, first probe response wins) made every boot a lottery across same-SSID mesh siblings, measured at four boots drawing −40/−76/−76/−39 dBm on one desk — and the station never roams off its draw, so the 2026-08-10 "mesh fix" was a re-roll, not a fix. (2) The websocket client layer is convicted and scheduled for replacement by an owned minimal client over esp-tls (`docs/briefs/M3-transport-own-the-websocket-client.md`); until that lands, its deaths are a known defect, not weather. This entry also **closes the 2026-08-09 open defect below** ("what stalls the feed 1–2.5 s with the socket up"): measured on a second board, they are real RF fades — ~1,140 five-second windows on a −7x association, mean worst-fade ~580 ms, max 3,914 ms — honestly reported by the watchdog and never themselves fatal. | **A two-board same-BSSID experiment separated the two faults the day kept conflating** (full chain: `hardware/bench-2026-08-13-wifi-drop-diagnosis.md`). Board B ran a minimal owned client (esp-tls + hand-rolled WS frames, `firmware/diag/link_autopsy.cpp`) *pinned to the same sick node* the production stack was dying on: production died continuously (36 connects in ~4 h; storm cadence ~7 s that morning), B held **one socket 3.7 h / 534 MB / zero deaths** through identical measured fades. An errno capture on the production client's death path then labelled **17/17 deaths `errno=119` — stale EINPROGRESS, i.e. the failing read set no socket error at all** (zero ECONNRESET/ETIMEDOUT; half via `WEBSOCKET_EVENT_CLOSED`, the clean-close path), while 85 co-timed parse rejects carried the `SPLIT@1` signature — exactly one stray tail-byte of the previous message glued ahead of clean JSON, post-decryption (TLS MAC guarantees the wire), an off-by-one in the library's payload-offset accounting and the same defect as the parked 2026-08-10 parse-burst finding. The strong-node "accepted residual" of 2026-08-10 (drops at ~1 per 6–10 min at −39 dBm) is the same defect at base rate; weather multiplies it (~50×) but never supplied the kill — 74/74 TLS handshakes succeeded mid-storm on the "dead" link, the 802.11 association held through every death all day (zero deauth reasons, zero rejoins), and the morning storm started and stopped with no board change. Ninth time this precompiled vintage has decided a design, and the first where the verdict is *remove the library* rather than route around it: no `error_handle` in this vintage's config meant the conviction required building a second client just to read the error codes. Consequence for stage E, stated so its numbers are read right: socket deaths currently "un-lag" the book via fresh-snapshot backlog skips (`seq+113984` ≈ 11 min observed); killing the deaths removes that accident, so staleness becomes *more* visible once the client brief lands. |
| 2026-08-09 | **§6 #5: liveness is book-events, not byte arrival; the strict definition is canonical.** A transport delivering frames the parser rejects is *stopped* and greys — bytes arriving is not liveness. The M3 Stage C firmware watchdog arms on a book-event; the host replay driver and the committed goldens pin the weaker byte-cadence definition. | Frames-arrive-but-don't-parse is a frozen ladder reading LIVE — exactly what #5 forbids — and a clean capture cannot contain it, so no existing golden exercises it (DESIGN.html strain point 10). Recording the strict definition here *binds* it: until a synthesised malformed-arrival trace pins it on the host (M4 opener), green under-specifies #5 — do not align the firmware down to the host driver. Measured cost of the strict arm: none (worst healthy gap 640.2 ms either way). |
| 2026-08-16 | **`esp_websocket_client` is gone from the tree, not merely unused — and the general rule it leaves behind is that a library which decides your design nine times has earned replacement rather than another workaround.** The `depthcharge-espws` and `depthcharge-nopp` arms, the `DC_OWNED_WS` / `DC_WS_PINGPONG` switches and every `#if` arm behind them are deleted, and with them `kClientWaitTimeoutMs`, `kClientSelfExitUs` and the two-handle `static_assert` — three constants that existed only to describe someone else's object code. **The rebuilt-lwIP framework (`wnd17232`) becomes the default build** at the same time, in `[env]` rather than on one environment, so no arm can differ from the daily driver by more than its own flag. | **The acceptance numbers, which is what this row was waiting for.** Baseline (2026-08-13, the library): a socket death every 6–10 minutes on a strong node, 17/17 errno-silent (`errno=119`, the failing read setting no socket error at all), 85 co-timed `SPLIT@1` framing rejects in four hours. Replacement (`device-monitor-260815-002728.log`, 23.6 h on the owned client): **`connects=1` across a single 10.9 h segment**, `sock_gaps=0`, **zero `SPLIT` anywhere in the 26 MB log**, `parse=0 price=0 ticker=0 unknown=0 trunc=0`, `a→e worst 19 ms` against the brief's ≤22 ms bar, heap flat (`+452 B` after 331,682 frames). The only two `socket end` lines in the whole day are both `upgrade-timeout` with **0 bytes and 0 frames**, four seconds apart, during the reconnect that followed a bench-power brownout — failed connects, not deaths of an established socket. Deliverable 0's five-boot association check passes on far more than five: every boot from 2026-08-14 onward joined `EE:D3:62:AE:81:9A` at −39…−47 dBm against siblings at −64…−86, and the `wifi up:` line confirms the association landed on the BSSID the scan named. **Still owed and not faked: the weak-node 1 h soak was never run on the final build.** Cost of making the window build the default, stated rather than buried: no firmware environment now builds without `C:\local\framework-arduinoespressif32-wnd17232`, where before a bare clone could build `depthcharge`. |
| 2026-08-16 (pm) | **The 2026-08-10 refused-retry fast path is deleted: it livelocked the station, and the rule it broke is that A MEASUREMENT OF THE FAILING CASE DOES NOT BOUND THE SUCCEEDING CASE.** `WifiSupervisor`'s `kWifiRefusedRetryUs` (1 s, taken whenever Arduino had latched `WL_CONNECT_FAILED`) is gone, with the `attempt_failed` parameter it rode on. `kObservedAssociationUs` was a guessed 2,500 ms and is now the measured **4,035 ms**; `kWifiRejoinCycleUs` returns to 10 s; `kWifiRejoinAfterUs` — the *first* takeover, which had no assert on it at all — joins it at 10 s; and **both are now asserted at ≥ 2× the association** rather than by the bare inequality that a bad constant satisfied at exactly 1.0×. | **M3's own pull-the-Wi-Fi acceptance found it, which is the strongest argument this project has yet produced for owner-driven bench acceptances existing at all** — every host test passed, the 23.6 h soak passed, and the object still could not survive its defining test. Blocking the board in Deco greyed the panel honestly and then never ungreyed it: the station could not re-associate for the rest of the run, and only a power cycle recovered it (`rst:0x1 (POWERON)` in `device-monitor-260816-085158.log`, four seconds before the association that "worked" — `connect_wifi()` runs once at boot and nothing interrupts it). **The signature is one ratio: 388 rejoin calls produced TWO `AUTH_FAIL` responses from the AP.** Had each call been an attempt the AP refused there would have been ~388 of them, 60 ms apart; they never arrived. A rejoin begins with `WiFi.disconnect()`, an association needs ~4 s, and a 1 s retry destroys the attempt in flight every time — and `WL_CONNECT_FAILED` is *sticky*, so once latched it kept selecting the fast path forever, which is why the failure was total rather than intermittent. The 2026-08-10 measurement behind the fast path was honest and remains true: a *refusing* AP answers in 60 ms, so 1 s looked like 16× margin. But the quantity a retry cadence must respect is the time to be **accepted**, 67× larger, and the path was sized against the case that could be measured while the case it existed to detect was the other one. Two structural consequences. (1) **A cadence that begins with a teardown is not a latency knob**; it is a floor set by the operation it interrupts, and every constant that schedules one is now asserted, not just the repeating one. (2) A counter exposing refusals beside rejoins was drafted and **dropped**, because sampling a sticky flag at 250 ms reads *one* in the broken case and *one* in the healthy case — an instrument that returns the same answer in both worlds is misleading rather than weak; the signal that actually diagnosed this is free and event-driven (Arduino's `Reason:` lines). Regression: a host `FakeStation` modelling the real asymmetry — 60 ms to be refused, 4 s to be accepted, `disconnect()` kills the attempt — with an AP that stops refusing part-way; it fails on the old code and passes on this one. |
| 2026-08-16 | **§6 #5 gains a second half: honesty is not recovery, and the transport now owns both.** Invariant #5 says a book that cannot be trusted must grey. It says nothing about the panel ever going green again, and the gap was real: `WsSupervisor` gated every decision on `socket_connected`, a silent-but-open TCP connection reads as connected, so a wedged peer would have been held forever behind a "healthy" transport. `SupervisorInput` gains `last_rx_us`; a socket up and silent for **`kSilenceRecycleUs` = 5 minutes** is treated as dead and recycled. | **Both numbers in this row are measurements, and the second one overruled the first inside four minutes.** At 2026-08-16 00:12:24 Anvil went silent mid-stream on a live socket: `-- rx` read `wait 99% / 0 reads`, the RX watchdog greyed the panel in under a second, no death, no autopsy, and the desk confirmed the venue rather than the board (`capture_anvil.py` timing out on the handshake while plain HTTPS answered 200 in 0.5 s). The first threshold written that night was **15 s**, sized against weather — Anvil's ~600 ms worst healthy inter-frame gap and the 2026-08-13 measured fades to 3.9 s. At 00:15:20 the server **resumed on the same TCP connection, 2 min 56 s after going quiet**, and the board went LIVE the instant bytes flowed. A 15 s recycle would have torn down a good connection and paid a reconnect plus a fresh snapshot for nothing. **So the threshold is sized against server stalls, not against weather**, and sits between the two failure modes with one measurement each: a real stall recovers in ~3 min on the held socket, a true half-open never recovers. Five minutes costs a half-open five minutes of grey instead of forever, and costs a stall like that one nothing; a `static_assert` pins it above the measured stall so a future session cannot quietly lower it back through the evidence. This also **closes the transport brief's half-open known-unknown without a client ping** — a ping's only job was to provoke the silence into an error the socket would report, and the RX loop can act on the silence directly, which needs no server cooperation and no write into a fade. Two review findings are recorded because their reasoning outlives them, and both are the same rule this table already holds three times over — **supervise on observed state, not on what a report implies**: the RX task checks the silence itself rather than trusting that a `connect_requested_` seen on a live socket must have come from the silence rule (it need not: `supervise()` samples `connected()` and stores the flag several lines and one malloc-ing `ESP_LOGW` apart, on the other core, so a connect that outran `kRetryCycleUs` could land a stale request on a socket that had just come up, and the first draft would have killed it); and the decision reports the live silence reading rather than a latched "this outage began in silence", which would have labelled every retry after a recycle as one. |
| 2026-08-16 (A7) | **The staleness problem is closed, by one query parameter on each side — and the general rule is that THE CHEAPEST FIX WAS NEVER A LEVER, IT WAS A QUESTION NOBODY ASKED THE VENUE.** `kAnvilPath` becomes `/ws?ticker=101&depth=27`; `docs/vendor/anvil-protocol.md` is re-pinned at `b4d31c2`; `harness/replay/anvil_101_depth27_20260816.ndjson` is committed with three ctest cases. `engine/` is untouched, as the ask predicted. **A new wire fact binds M4/M5: depth is served in TIERS** (1,2,3,5,8,10,15,20,30,…), rounding **up** and never down, so asking 27 is served 30 and the client truncates locally — a venue's depth parameter is a *request*, not a contract, and an adapter must not assume it received exactly what it asked for. | **Measured on this side rather than accepted, and that was the point.** The 2026-08-16 (eve) row below convicted this milestone of reading a nearby measurement as answering the question that mattered, five times over; taking Anvil's deployed-server figure on trust would have been the sixth. A 90-second capture at `depth=27`, priced by `tools/anvil_frame_economics.py`: `book` mean **8,428 → 2,471 B**, levels/frame **~205 → 60.0** (confirming the tier from the outside), 90 s of wire **10.4 MB → 2.84 MB = 27.3%** of the committed 2026-08-09 baseline, sustained **112.6 → 30.8 KiB/s**. Against the 23.6 h soak's worst measured hour of 56 KiB/s that is **1.8× headroom where there was 0.5×**. This repo's own pre-measurement predicted 27.8% and Anvil's independent deployed figure (2,476 B) agrees to 0.2% — three estimates from two codebases converging is the strongest evidence this project has produced for anything. **What it cost to find: one question.** The board had been convicted, the RX path instrumented and exonerated, `liblwip.a` rebuilt from source to raise `TCP_WND`, and a delta-feed protocol change promoted to "the thing that makes the hardware work at all" — all against a stream that was 97% padding because nobody had asked the venue for less. The tier's own cost is stated rather than rounded away: 30 served against 27 rendered is **9.7% of `book` bytes** the panel never draws, where Anvil sized it as "a few percent". **And one premise in our own ask was wrong**: we cited `GET /api/book?depth=` as proof that "the name, concept and validation already exist", and the parameter existed but was *silently ignored* — there was no validation to reuse. Arguing from a parameter's existence is not arguing from its behaviour. |
| 2026-08-16 (D5) | **An instrument that GENERATES traffic must never feed a detector that MEASURES traffic — and turning the client ping on is what found that rule sitting broken.** Two changes, and the second is the one with weight. (1) The client ping is on by default and is now an *instrument* rather than a provocation: a new ESP-IDF-free `PingProbe` (`firmware/src/ws_ping.hpp`, host-tested in `test_ws_ping.cpp`) owns the cadence, the ping/pong pairing and the readings, and the transport prints them on a `-- ping` line directly under `-- age`. (2) **`SupervisorInput::last_rx_us` becomes `last_data_us` and changes meaning from byte-arrival to DATA-arrival**, stamped in `WsTransport::on_chunk` rather than at the read. ROADMAP backlog D5; the `depthcharge-ping` arm inverts to `depthcharge-noping`. | **What the round-trip buys, and it is a term nothing else could isolate.** `staleness.hpp` says how old the book is and structurally cannot say whose fault the age is. Anvil's vendored Crow answers a client PING with no application code and appends the pong to the *same* per-connection `write_buffers_` as data, so a pong cannot overtake anything already queued on that socket and the round-trip prices **this socket's server-side send queue**. That splits the one diagnosis this project has been unable to make: `age` high with a **slow** pong is our own undrained queue and ROADMAP A7 is the fix; `age` high with a **fast** pong is lag upstream in the broadcaster and A7 would not help at all. Neither number alone supports either conclusion, which is why the two lines are printed adjacent and share a reading vocabulary. **The provocation question stays closed** — `kSilenceRecycleUs` still owns recovery, needs no server cooperation and no write into a fade. **Now the regression that shipping this would otherwise have been, stated as the general rule because it will recur.** The recycle's clock was fed from *byte* arrival, which was the same quantity as data arrival only for as long as data frames were the only thing that ever arrived. A client ping manufactures a pong every `kPingPeriodUs`, and a pong is bytes — so the board's own control traffic would have refreshed the detector's clock, and **a peer that answers pongs while publishing nothing could never have been recycled.** That is not a hypothetical peer: it is precisely the 2026-08-16 00:12 stall in the row below, and the recycle is the only recovery path there is. Stamping on data chunks makes the test strictly stronger ("no DATA for five minutes" rather than "no bytes"), costs no precision (`c.arrival_us` is the same pre-parse stamp the read used), and restores the meaning the constant was sized against. **Three design points are pinned rather than left to a comment.** RFC 6455 §5.5.2 lets a peer answer several outstanding pings with one pong, so exactly one is ever in flight — the alternative, a correlation key in the payload, is rejected because a key cannot create the responses it would correlate. §5.5.3 allows unsolicited pongs, so one arriving with nothing outstanding is counted and never treated as an answer; the fabricated round-trip it would otherwise produce is small and plausible, which is the worst kind. And **nothing may branch on any of it** — §6 #5 says liveness is defined by events reaching the *book*, and a server can answer pongs perfectly while publishing nothing, so the type deliberately exposes no `healthy()`/`live()` predicate and the render task holds it by const reference. **Honest bounds, because the number will be quoted:** it is blind to producer-side lag by construction (a pong is posted ahead of frames the broadcaster has not handed over yet), the ordering is one-sided, and all of it is read from Anvil's source and has **never been captured under induced backpressure** — ROADMAP A2 asks Anvil to make the ordering a contract, which costs them a sentence. Two review findings are recorded because their reasoning outlives them, and both are the sibling instrument's scars arriving unchanged in a new file: the peak had no run-level survivor, so the deepest reading this probe can take — a round-trip that outlives its own socket — was banked at `note_disconnect` and erased by the `note_connect` seconds later, exactly how 21 reconnects made the 86-minute run of 2026-08-09 look healthy; and `render` returned early on "no reading on this socket yet", printing a shrug over a 74-second finding it was holding. |
| 2026-08-16 (D5) | **The age clock's 71.6-minute ceiling is CLOSED, and the row below asking for it is answered.** `SecondsText` and every accessor behind `age`, `worst`, `run` and `over` are 64-bit; `test_staleness.cpp` pins a two-hour connection printing ~5,760 s rather than `4294.9`. Shipped in `1dea077`, in the same commit as the soak row below that recorded the defect — so that row's closing "Fix owed before the next soak" was already satisfied when it was written and is superseded here rather than edited, per this table's rule. | One residual is deliberate and is **not** a hole: `worst_ahead_us_` is still a clamped uint32. It is the premise-check diagnostic, its companion figure is a scale-free per-mille ratio that is uncapped and is what `premise_suspect()` actually tests, and saturation can only make that test *more* true — so the clamp cannot produce a false negative, and an "ahead" of 71.6 minutes would mean the 2 Hz premise had been screaming for an hour already. Stated because the header claims the fix covered "every `age`, `worst`, `run` and `over` field" and a future reader counting fields will find a fifth. |
| 2026-08-16 (eve) | **A venue's byte rate is not a property of the venue — it is a property of what you asked it for. Before sizing a path against a stream, price the stream against what the object actually renders.** M4/M5 inherit this as the sharper form of the 2026-08-11 rule below: Kraken's book subscription takes a `depth` and Binance's stream name encodes one, so the adapter brief must fix the requested depth *before* concluding anything about the path. Nothing in `engine/` changes; `kMaxSnapshotLevels` and the truncation-counting rule already make a shallower book legal (depth beyond N is *unknown*, not zero). | **The Atlantic was carrying 97% padding and three sessions never looked.** Both committed captures, priced by the new `tools/anvil_frame_economics.py`: `book` frames are **98% of Anvil's wire** at a mean **8,428 bytes** carrying **~205 levels**, and between consecutive frames a **median of one level changes** (mean 1.1, p99 3, max 4 over 90 s) — so ~99% of every frame is a retransmission of levels that did not move, and the panel renders 27 a side and discards the rest. `ANVIL_BOOK_DEPTH=0` ("all resting levels") is Anvil's deliberate default for its web client's scrollable ladder, not an oversight, and `GET /api/book?ticker=&depth=` already takes the parameter that would fix it. **Truncated to 27 a side the whole stream falls to 27.8% — 110.4 KiB/s becomes ~31 KiB/s, inside the soak's *worst* hour (56 KiB/s) with 2× headroom.** That demotes the item this project promoted on 2026-08-11 to "the thing that makes the hardware work at all": the delta feed is 1.3% and is still the better design, but a **query parameter** gets 80% of the benefit for 2% of the work, and staleness was never one lever away — it was one *question* away. The failure mode is the milestone's own, for the fifth time: **a measurement that answers a nearby question (how fast can the board take bytes?) got read as answering the one that mattered (why are there this many bytes?).** Corroboration collected in the same pass, which changes no decision but upgrades the 2026-08-11 row below from inference to construction: Anvil's per-socket send queue is Crow's `write_buffers_`, a `std::vector<std::string>` with no cap, no drop policy and no coalescing — it queues without bound, exactly as the freshness probe implied. Both go to Anvil as `docs/anvil-handover-2026-08-16.md` (ROADMAP A7/A3); Anvil is still not modified from here. |
| 2026-08-09 | **`kRxWatchdogMs` / `ReplayOptions::disconnect_gap_ms` stay at 1000 ms — the threshold was re-measured, not re-derived, and the M1 number survives.** Anvil's cadence to a healthy client is 17.02 frames/s with a worst inter-frame gap of **391 ms** over 20 minutes / 20,418 frames, all three counting rules agreeing on the worst gap as they did at M0. A second finding lands beside it and is the one with reach: **Anvil sheds to a slow consumer — evenly.** A client throttled to a quarter of the server's rate receives 4× fewer messages and is still never silent for longer than one drain interval, because `book` frames are coalesced per socket and a backed-up socket gets the newest book rather than a queue of stale ones. | M3 stage C's bench showed the panel greying every 15–20 s on a socket that never dropped (`wd_gaps=5` in 90 s, `sock_gaps=0`, worst silence 2,461 ms), and the board was reading ~6 msg/s against M0's 15.5. The brief that followed took the obvious reading — the server had slowed, healthy silences had outgrown the threshold — and asked for the constant to be re-derived upward, pre-sanctioning the golden move as the one exception to "a moved golden means stop". **The premise was false, and measuring it first is the only reason the constant did not move the wrong way**: re-derived from current Anvil cadence the threshold would have gone *down* (391 ms worst, ×1.6 = 626 ms), making the false greys worse. The board's low message rate is real but is backpressure, not cadence — holding `summary` at 2.00/s and `trade` at 1.58/s and solving the bench's 8.59 msg/s at 42.70 KB/s for `book` gives 5.14 book/s and a 5,012 B mean against 5,148 B reported, inside 1.5%. The control that settles it: a desk socket throttled to the board's own 8.30 msg/s and measured under the board's own book-event rule shows a worst silence of **594 ms** and zero watchdog trips in four minutes, where the board shows 2,461 ms. So the multi-second holes are board-side and the watchdog is reporting them honestly — **raising the threshold would have hidden a real 1–2.5 s freeze of the feed pipeline, which is exactly the frozen-ladder-reading-Live outcome invariant #5 exists to forbid.** Recorded as an amendment despite changing no code because the *reasoning* is now load-bearing: M1's 1.6×-over-640 ms derivation is superseded not by a new number but by a better-supported old one (2.6× over 391 ms on 3× the sample, plus 1.68× over 594 ms at the board's own rate), and the constant is coupled to Anvil's send cadence in a way that a future real change must still be allowed to break — so the measurement is now a golden (`test_replay_goldens.cpp`, "2026-08 capture: Anvil's cadence still clears the watchdog by 6x") and goes red on the desk instead of grey on the panel. The durable fix for the coupling remains the heartbeat/keepalive on Anvil's backlog (ROADMAP backlog A2, cross-referenced by DESIGN.html strain 10); it is not needed at this cadence and would be needed immediately at a much slower one. **Open defect, owned by the next firmware session, not by this constant:** what stalls the ESP32's feed for 1–2.5 s with the socket up. The shedding finding is also a note to M4/M5 — a thinned Anvil stream is not a broken one, and an adapter must not read its own client's slowness as a venue fault. |
| 2026-08-16 (stage 0) | **A venue's silence is not one measurement, and the clock must be stamped by the thing whose absence you actually care about.** DepthCharge now has **three** distinct clocks where it believed it had one: byte arrival, data-frame arrival, and **book-event arrival**. At Anvil the three coincide, because a timer broadcast means bytes stop when the book stops — which is why `kRxWatchdogMs = 1000` survived two re-measurements. At Kraken they do not, and the divergence is **9×**. This is the same rule §9 recorded on 2026-08-16 for **D5** — the silence recycle moved off byte arrival because our own pongs manufactured bytes over a dead publication — generalised one notch: **Kraken's heartbeat is an *application* frame, so it stamps `last_data_us` too, and the split D5 made is not deep enough for an event-driven venue.** **No constant changed at stage 0.** Which clock drives invariant #5's grey at an event-driven venue is an M4 decision and is §4-adjacent; it is not a threshold tweak. | **Evidence** (`harness/replay/NOTES-kraken.md`, four 90 s captures taken concurrently on three sockets so the hour is controlled): worst **book** silence **4,535 ms** on BTC/USD at depth 25, 5,058 ms at depth 10, and **9,007 ms** on a deliberately quiet pair — whose **p90 is 8,480 ms**, so multi-second silence is its normal condition rather than an outlier. Against Anvil's **391 ms** worst healthy gap, from which 1000 ms was derived at 2.6× margin. Meanwhile Kraken publishes a **1 Hz `heartbeat`** (23 bytes, exactly 90 per 90 s capture) that floors *byte* silence at **998–1,026 ms in every capture** — straddling the same constant, so a byte-armed watchdog would sit on the heartbeat's own noise floor and trip intermittently while the book was fine. `NOTES.md` records that at Anvil all three counting rules agree on the worst gap, and treats that agreement as a stable property surviving 3× sample growth; it is a property of a venue that broadcasts on a timer, not of the rules. The brief forbade touching the constant and it was not touched — this row exists so the next session inherits the measurement rather than the surprise. |
| 2026-08-16 (stage 0) | **Strain 3 resolves as `static_assert`s on the sink signature, not a `concept`; and the 2026-08-07 subset row is accurate about the header and incomplete about the language feature.** A C++20 `concept` costs **one flag** (`-fconcepts`) on xtensa GCC 8.4 — not the pioarduino/IDF-5.x migration everyone assumed was the price. `<concepts>` the *header* is genuinely absent, exactly as 2026-08-07 says. **The decision is (ii) anyway**, because GCC 8.4's `-fconcepts` is the **Concepts TS and not C++20**: host and target would both compile the same spelling under different dialects, which is a silent divergence in the one property invariant #1 exists to protect. Option (i) re-opens if and when the toolchain moves for unrelated reasons. **The 2026-08-07 row stands as written** — per this table's rule, the reasoning is the valuable part, so it is corrected here rather than edited there. | **Measured, not argued**, with the exact command `dc_engine_target_check` runs (`CMakeLists.txt:97-101`): `#include <concepts>` is `fatal error: concepts: No such file or directory`; the bare C++20 keyword is `error: 'concept' does not name a type` … `note: only available with -fconcepts`; **with that one flag it compiles clean**. Cost of the flag on the engine as it stands: **9/9 headers and `anvil_frame_streaming.cpp` compile, and the object file is byte-identical** — same hash, `.text` 5,861 B either way. The trap the flag hides: GCC 8.4 also accepts the TS spelling `concept bool`, which host GCC 15.2 rejects outright (`error: variable concepts are no longer supported`), so only the C++20 spelling satisfies both compilers — and *that* spelling compiles on the target under TS semantics, which nothing here has shown to match C++20's. The missing experiment, named so it can be run rather than re-argued: a concept whose subsumption behaviour is exercised on both compilers and shown to agree. **And the shape of the 2026-08-07 row's incompleteness is this project's own recurring one, now at instance six:** an inventory taken by one method (*does this header exist?*) got read as answering another (*is this feature available?*). Write-up and decision: `docs/briefs/M4-stage-0-price-the-kraken-wire.md`. |
| 2026-08-16 (stage 0) | **A trace that contains the system's own healing events measures recovery, not the defect. Exclude them from a golden deliberately, or the golden reports green for the wrong reason.** Any mechanism that restores correct state on a schedule — a resync snapshot, a reconnect, a periodic full replace — bounds how long a defect stays visible. So **a trace long enough to contain one is a trace short enough to miss it**, and the two properties a capture is usually chosen for (long, realistic) are the two that destroy it as a golden. When a capture is taken to guard a rule, the healing event is excluded on purpose and the exclusion is recorded; when it is taken for coverage, it may stay. | **Falsified during pinning, by the capture taken to be thorough.** The M4 stage-0 reconnect capture scores 3 evicted-level returns to the checksummed window and detects a never-truncating client **not at all** (840/840 clean), where the plain depth-25 capture over the same venue detects it 1,077 times in 1,537 messages. The mechanism is certain rather than inferred: `Book.replace` clears every level, so a resync **repairs** a book that never truncated, and the defect is only visible in the window between two snapshots. **Second instance, and the first is in this table already:** the 2026-08-13 row records that socket deaths were "un-lagging" the book via fresh-snapshot backlog skips (`seq+113984` ≈ 11 min observed) and explicitly predicts that *killing the deaths would make staleness more visible* — a healing event masking the defect it healed. The 2026-08-16 (D5) row is the same shape at one remove: 21 reconnects made the 86-minute run of 2026-08-09 read as healthy, because each reconnect erased the deepest reading the instrument was holding. Three instances, three different subsystems, one rule. Consequence for M4/M5: the committed truncation goldens contain no reconnect, and `tools/kraken_frame_economics.py` reports **CATCHES / MISSES** per trace so the question is answered by measurement rather than by how production-like the capture looks. |
| 2026-08-17 (M4 stage A) | **A refused subscription is not a new `GapReason`.** `success:false` on the subscribe ack is treated as a fatal transport error: `die()`, the supervisor retries, the panel greys as `Disconnect`. The constant case is proven impossible at compile time — a `static_assert` pins the subscribed depth to Kraken's offered ladder {10, 25, 100, 500, 1000}. **§4 does not move, and the decision not to move it is itself this row.** | Stage 0 asked whether the vocabulary needs a way to say *this feed will never start*, and held that grey-forever is honest about the wrong thing — `Gap` means the book is unknown until the next snapshot, and a refused subscribe means no snapshot is ever coming. The counter-argument is recorded because it is a good one and it lost on a specific ground: **a 64×64 ladder cannot act on *unknown forever* differently from *unknown now*.** There is no third palette, no extra glyph and no user action that differs between the two, so a new `GapReason` would buy a distinction the object cannot render — while costing §4 a permanent widening for one venue's configuration error. The serial log names it, which is where a configuration error belongs; and the depth that would provoke it cannot reach the wire, because the whitelist is a compile-time assertion rather than a runtime check. |
| 2026-08-17 (M4 stage A) | **⚠ THE BOOK-EVENT CLAUSE OF THIS ROW IS SUPERSEDED by the ruling row dated 2026-08-17 (M4 stage A, ruling) at the end of this table. The two-clock split below STANDS; what the staleness clock COUNTS does not. Read both.** The declared per-venue numbers below (Anvil 1,000 ms, Kraken ~15,000 ms) are withdrawn: a second quiet-pair window measured a healthy 25,843 ms book silence, so no threshold on book silence can be correct. Text preserved unedited below, per this table's own rule — the reasoning is the part worth keeping, and it is right about everything except which arrival stamps the clock. — **The clock splits in two.** Book staleness is invariant #5's business and counts **book events**; transport liveness is the supervisor's business and counts **bytes**. The staleness threshold becomes **declared venue metadata**, beside tick size and qty step — Anvil 1,000 ms, Kraken ~15,000 ms — not a firmware constant. The any-data-frame rule is rejected. | The two clocks coincide at Anvil, and *that is the only reason one constant ever worked*: a timer broadcast means bytes stop when the book stops. The 2026-08-16 (stage 0) row measures the divergence at Kraken as **9×** — worst book silence 9,007 ms on the quiet pair with a **p90 of 8,480 ms** (multi-second silence is its normal condition, not an outlier) against Anvil's 391 ms worst healthy gap, from which 1,000 ms was derived at 2.6× margin. The middle option is the trap: **the any-data-frame rule measures the heartbeat at Kraken**, because a 1 Hz `heartbeat` is an application frame and floors data silence at 998–1,026 ms in every capture — a data-armed watchdog would sit on the heartbeat's own noise floor and tell you the venue is alive when it is telling you nothing but that. So the split D5 made (bytes → data) is not deep enough for an event-driven venue, and the third clock is the one invariant #5 actually names. Making the threshold venue-declared rather than a constant is forced by the same measurement: 1,000 and 15,000 are both correct, for different venues, and a single number cannot be either. |
| 2026-08-17 (M4 stage A) | **The trace metadata contract gains a venue tag, and one reader dispatches to per-venue decoders.** The replay driver feeds **the venue's own adapter**, not a normalised shape. | Three shapes were available — venue-tag the metadata, teach one reader a second dialect blind, or give Kraken its own reader — and the deciding constraint is what the goldens are for. A replay that normalises Kraken's frames into an Anvil-shaped intermediate tests the normaliser and stops testing the adapter, which is the only thing invariant #6 exists to cover; a second reader duplicates the definition of a valid trace, which is exactly the drift ARCHITECTURE §9 (2026-08-07) already paid for once. One reader, one metadata contract, an explicit tag, and the dialect knowledge in a per-venue decoder behind it. The tag is **additive and absent reads as `anvil`**, so the four committed Anvil traces do not move by one byte. |
| 2026-08-17 (M4 stage A) | **`age_ms` on `DisplaySnapshot` (§5) is taken now**, rendered numerically in the header, no third palette. **Authorised; implemented in stage A2, not in stage A.** | Two things are true at M4 that were not true during M3, when the same proposal was declined. First, **stage D's strain-20 criterion is unassertable unless *old* and *stopped* are separable states**: the bar is "the ladder holds its colour through a nine-second legitimate silence, and greys when the feed actually dies", and a panel with one grey state and no number cannot show the difference between those two sentences. Second, **Kraken's 1 Hz heartbeat is a fixed-rate broadcast** — the same denominator Anvil's 2 Hz `summary` gave the M3 stage-E estimator — so the age meter generalises to the second venue rather than being an Anvil special case, provided the expected rate is venue-declared like the threshold above. No third palette: the number is the honesty, and a colour for *old* would re-open the §6 #5 question that the `Ink`/`Palette` split closed structurally. |
| 2026-08-17 (M4 stage A) | **M4 runs as four evening-sized stages**: **A** the replay dialect; **B** the adapter (parse, ack check, CRC32, deltas); **C** the dense-window book in `engine/`; **D** the bench. **C is the only stage that touches `engine/`, and it does not share an evening with anything.** | The ordering is invariant #6 read literally: *no merge without replay coverage*, so the reader that can cover an adapter has to exist before the adapter does — otherwise the pressure at review time is to reshape the committed traces to suit the reader, and stage 0 deliberately did not reshape them. **The reader learns the wire; the wire does not learn the reader.** C's isolation is the other half: `engine/` is the code both execution contexts share and the only code invariant #1 protects on two toolchains, so a session that changes the book must not also be carrying a venue dialect, a bench rig or a golden move it can blame. |
| 2026-08-17 (M4 stage A) | **One venue per build until M7.** Compile-time constant, default stays Anvil. The runtime toggle is M7's encoder, and **M7 depends on M4 and M6 — not M5.** | A runtime venue switch is not one `if`: it is two adapters live in one binary, two `SymbolSpec`s, two declared thresholds, two book depths and a ladder whose height changes under it — on a part where invariant #7 forbids the allocation that would make any of that comfortable. None of it is needed to prove Kraken works, and all of it is needed to *use* the encoder, so it belongs with the encoder. The dependency correction matters more than the deferral: M7 needs a second **venue** and a **knob**, which is M4 and M6; it does not need a third venue, so M5 slipping cannot block it. |
| 2026-08-17 (M4 stage A, from the work) | **The 2026-08-07 "one definition of a valid trace" row is NARROWED, not abandoned, and it is narrowed in exactly two places: a required `ticker` and a required string `type` become VENUE-CONDITIONAL.** Anvil is held to both, unchanged — a frame that loses its `type` still fails there. Kraken requires `symbol` instead of `ticker` and its frames need no `type`. Everything else in that row stands: `TraceReader::next` is still the single definition, `read_trace()` is still a statistics pass over it, and `rx_ns` must still not decrease. Two new rules arrive with it. **(1)** A capture declaring a venue the build does not know is `UnknownVenueError`, a *different* failure from a malformed trace, because only one of them is a bug. **(2)** A snapshot is a RESYNC exactly when a book event preceded it in the trace — one rule, every venue, replacing "not the trace's first record". | The 2026-08-07 row's reasoning was that two readers had drifted and given two answers to "is this trace valid". Nothing about that is weakened by making a rule venue-conditional; what would weaken it is a **second reader**, which is the option this stage rejected. Both narrowings are Anvil properties that were universal only because Anvil was the only venue: an integer ticker id, and a `type` field. Recorded because the 2026-08-07 row is cited elsewhere as though it were unconditional, and a future session reading it alone would conclude a Kraken trace is malformed. **The resync rule earned its own clause by being got wrong twice in one session, in opposite directions, before the right question was asked.** Anvil's on-connect snapshot is record 1 and Kraken's arrives third (status, ack, snapshot), so the index rule calls every Kraken capture a reconnect. "The first snapshot after a subscribe is on-connect" fixes that and breaks the reconnect capture, whose second subscription's snapshot *is* the resync. "Not the first acked subscription" fixes that and breaks a windowed reconnect *slice*, which carries only one ack. Each repair was venue-shaped, plausible, and wrong somewhere else. Asking what came BEFORE the snapshot needs no venue split and gets all four cases, because it is the question the concept actually names — a snapshot re-baselines a book, and it is a resync exactly when there was a book to re-baseline. It reproduces Anvil's existing answers on all four committed traces, including the reconnect WINDOW that contains no on-connect snapshot at all, and it is the same rule `tools/slice_trace.py` cuts a window with, so the tool that makes a trace and the reader that counts what is in it cannot disagree about the same file. General form, and this milestone has now paid for it twice: **a rule written per venue is a rule written twice, and the second writing is where they diverge — before adding a venue branch, check whether the general question has a venue-free answer.** |
| 2026-08-17 (M4 stage A, ruling) | **SUPERSEDES DECISION 2'S BOOK-EVENT CLAUSE OF 2026-08-17.** Decision 2's split of transport liveness from book staleness **stands**; what changes is *what the staleness clock counts*. **1. Book-event silence cannot be a staleness signal at any venue, and no threshold on it can be correct.** A quiet market and a silently dead subscription produce identical wire behaviour, so book silence carries no information about whether the displayed book is trustworthy. At MINA/GBP a 25,843 ms healthy silence is a book that is exactly right, and greying it asserts the opposite. The bound on book silence is a market property, not a protocol one, and a market can be closed. **2. Staleness counts the venue's declared liveness signal.** Kraken's heartbeat: 600 in 600.2 s, worst record gap 1,119 ms, cadence held at 936–1,042 ms inside the 25.8 s book hole, and every worst record gap in stage 0's four slices within 27 ms of one second at a different hour. Anvil's `summary`: a fixed engine-thread deadline that fires on an empty queue, order flow changing contents and never timing; 2 Hz measured across 60 s idle with the feeder never started, and across 31 s idle with the feeder stopped mid-run — 62 idle frames byte-identical with `seq` advancing; nominal 500 ms, worst 547 ms. **3. The threshold is measured at runtime, not declared.** Anvil's interval is `ANVIL_SUMMARY_HZ`, operator config, not discoverable by a client; Kraken's is a protocol constant DepthCharge also does not control. A hardcoded threshold is therefore coupled to a value the client cannot read back and breaks silently when it changes. The threshold is a multiple of the observed inter-arrival median of the liveness signal itself. **4. Book-event silence becomes `age_ms`** — rendered as a number in the header, never as grey. Decision 4 was taken on the grounds that *old* and *stopped* must be separable states; this is that separation becoming load-bearing rather than anticipated. | **5. A healthy silence beats a threshold; a faulty silence is what the threshold is for.** MINA/GBP's 25,843 ms was a healthy feed, so greying would have been a false statement. Anvil's unexplained 176,000 ms WS silence of 16 Aug (Anvil's `docs/anvil-plan.md` A2b, unreproduced, HTTPS unaffected, both internal hypotheses would silence `summary` too) was a fault, so greying is a true one. The two are indistinguishable in a distribution and opposite in meaning; only the question *was the feed healthy?* separates them. **6. Accepted cost, in its general form.** A connection-level liveness signal does not prove the book subscription is alive. Anvil's `summary` is global fan-out delivered to every socket regardless of which ticker it subscribes to; Kraken's heartbeat is connection-level. Neither proves the book. This trades an observed false-grey — three per ten minutes on a healthy Kraken feed — for an unobserved false-colour. Stage B's CRC covers the case where updates arrive and disagree; it does not cover silence. **7. Cadence is not freshness.** Anvil queues and never drops per socket — no cap, no drop rule, no per-socket coalescing — so a backlogged socket receives every frame late rather than losing any: 111 s of lag over 150 s, measured. Arrival proves the server was alive when the frame was generated, not that the frame is fresh. **8. The vendored contract currently denies the behaviour relied on.** Anvil's PROTOCOL.md states that a genuinely idle book produces no frames and that a client must not treat a quiet socket as dead. That clause is false as implemented, and the snapshot in `docs/vendor/` carries it. Until Anvil corrects it, DepthCharge relies on measured behaviour its own vendored contract denies. **Rule for the arrangement, not the number:** measuring a market property and measuring a protocol property look identical until the sample widens. Ask which of the two a constant is made of before declaring it. |
| 2026-08-17 (M4 stage A, close-out) | **§9 item 8 of the staleness ruling is CLOSED: the vendored snapshot is re-pinned at `4801ed8d8b09b62ec4fcee8e68280f16b3c4780c` (2026-08-17), and DepthCharge no longer relies on measured behaviour its own vendored contract denies.** The ruling's point 8 recorded that Anvil's PROTOCOL.md said *"a genuinely idle book produces no frames"* while the staleness rule DepthCharge had just adopted depended on the opposite. Anvil corrected it at the source. **§3 Keepalive** now reads *"The server never initiates a WebSocket ping, and there is no dedicated heartbeat frame in v1 … What the server does send unprompted is the data stream itself, on fixed timers that do not depend on order flow: one `book` frame per subscribed ticker on the ~14 Hz publish tick, and one `summary` frame on the `ANVIL_SUMMARY_HZ` cadence. Both continue on a completely idle book"* — which also makes the 12.6 book-frames/s this project measured against a feeder-off server a contract statement rather than an observation. **§3.5 gains a guarantee**: *"Emission is timer-driven, not activity-driven — this part is a guarantee … The interval is not part of that guarantee. It is operator configuration (`ANVIL_SUMMARY_HZ`) … A client that needs a staleness threshold must derive it from the cadence it observes on the connection it is using, not from a number read out of this document."* | **The self-calibrating threshold stopped being DepthCharge's idea and became the venue's requirement.** Point 3 of the ruling reasoned that a hardcoded threshold is coupled to a value the client cannot read back; §3.5 now *instructs* clients not to hardcode one, and `liveness_clock.hpp` was written the day before that sentence existed and satisfies it unchanged. **That is NOT independent convergence, and an earlier draft of this row said it was — the correction is the owner's and it matters.** Both texts descend from one instruction: the reply to Anvil said *promise the mechanism, not the interval, and refuse the number*, which is visible in Anvil's own commit (`25ade0e`: "3.5 now promises the mechanism … and explicitly refuses to promise the interval"). The texts had not seen each other; **the reasoning shares a parent**, so agreement between them is common cause and carries none of the weight of two measurements taken by different methods. The smaller claim, which is the true one and is still worth having: **the ruling survived contact with a second implementer who could have found it awkward and did not.** Contrast the A7 row of 2026-08-16, where "three estimates from two codebases converging" *was* independent — different methods, no shared instruction — and note that the two look identical in a summary and differ entirely in what they license. The failure mode is this milestone's own, at instance seven: **a measurement that answers a nearby question read as answering the one that mattered**, here in its epistemic form — shared-parent agreement read as corroboration. **What the refresh changes about the OBLIGATION, and it is the part worth recording:** the ruling's point 8 was a standing exception — the object depending on behaviour its own pinned contract contradicted — and an exception nobody is tracking is how a client ends up wrong about a venue. It is now discharged rather than annotated, and the annotation was deliberately never applied to the snapshot BODY (only to DepthCharge's own header block), so nothing had to be un-edited to accept the new text: the body of this re-pin is byte-identical to upstream, verified. Three DepthCharge measurements appear in the new §3.5 — the 62-frames-over-31 s idle proof, the global-coalescing consequence, and the 2026-08-16 A2b silence as a bound a client must size against — so the correction also closes the loop opened by `docs/anvil-handover-2026-08-16.md`. General rule, cheap and earned twice this milestone: **when a measurement contradicts a vendored contract, the fix is upstream and the annotation is a holding position — record it with an expiry, and close it with a SHA.** |
| 2026-08-17 (M4 triage) | **M4's remaining stages are re-cut into A2 · B1 · B2 · C · D, and three questions are decided.** The twelve items stage A left owed were sorted rather than worked through in order, and the sort moved five of them: **A2 — the age meter** (items 4, 5, 7, 8; `engine/` + harness, no venue work, unblocked immediately); **B1 — the adapter** (parse, subscribe-ack including *absence is not failure*, deltas onto the phase-1 book, `kRxWatchdogMs` deleted, `symbol_for()` and `console_ladder`'s hardcoded `" ANVIL "` fixed in the same sitting because that is when they break); **B2 — the healing path** (CRC32, resync, the deliberately captured resync slice, the decoder-identity mutation test, `slice_trace`'s reconnect self-containment); **C — the dense-window book** (`engine/` only, plus an explicit *uninitialised* state); **D — the bench**. Decisions: **(a) the client ping does NOT land in M4** — it is the only non-estimated freshness source, but it is transport work in the firmware WS client, needs its own bench evidence, and the windowed deficit is sufficient for a numeric header; it is owed by whichever milestone next opens the transport layer, **M6 on current shape**. **(b) item 11 splits C/D** — the engine carries an explicit *uninitialised* state distinct from *empty book*, because a book that has received nothing and a book whose every level is genuinely empty are different facts and only one of them is knowledge; what the panel draws for it is decided at desk distance, because the two candidate renderings differ in nothing a host test can assert. **(c) the defensive resubscribe is NOT built** — revisit at M5 at the earliest, when a second venue's behaviour can inform it. | **The list made stage B look like the milestone and it is not: five of the twelve were never adapter work.** Sorting them by *what has to exist before this can be tested* rather than by the order they were found moved the age meter to the front, where it is unblocked and shares nothing with the venue work, and left B with the two items that genuinely need Kraken bytes. The three decisions are all the same shape — **naming a thing that was not built, with its reason, so it is not later mistaken for an oversight.** (a) is recorded against a milestone rather than a backlog because a freshness gap with no owner is how an estimate becomes believed; (b) is split because the engine question ("is *nothing yet* a state?") is answerable by a test and the rendering question is not; (c) would convert an honest grey into a hidden retry, and its trigger would be book silence — the exact quantity the same day's ruling established carries no information — so on a healthy MINA/GBP it would fire every ~26 s. M4's DoD is now one sentence with four clauses, each mapping to exactly one stage, which is the test of whether the split was drawn in the right places: *the panel renders a Kraken book off the wire, greys within the calibrated liveness threshold when the heartbeat stops, holds colour through 26 s of legitimate book silence, and shows a book age that is a lag estimate rather than a time-since-anything.* |
| 2026-08-17 (M4 stage A2) | **§5 gains `age_ms` + `has_age`, and the age is measured against a BASELINE latched once per connection — not against the rolling median the grey threshold uses.** The estimator (`dc_harness/age_estimator.hpp`) is the supremum of `(now − t_i) − n_i × baseline` over every suffix of a 256-arrival window of the venue's liveness signal, clamped at zero — Lindley's backlog recursion, truncated to a window. The baseline is the estimator's **own** median of the connection's first `kBaselineSamples` (32) intervals, frozen there; it is re-learned after a `Gap{Disconnect}`, which also voids the estimate and banks its peak. **Both halves of that sentence were corrected at review, and each correction was a silent wrong answer rather than a style point.** Reading the *threshold clock's* rolling median instead would inherit the DEAD connection's cadence, because that clock is deliberately not reset at a disconnect (cadence is a venue property) — so reconnecting away from a backlogged socket would make the fresh healthy one latch the throttled interval and report no lag for ever. And latching over `kMinSamples` (8) intervals rather than 32 swallows the resumption burst: measured on `anvil_101_reconnect`, the first post-reconnect intervals are `253 871 174 475 …` ms against a 500 ms broadcast, whose lower median reads **478.0 ms at n=8, 495.6 at n=16, 499.3 at n=32**. A baseline 4.4% low manufactures age at 4.4% of wall-clock for ever — it put 2.8 s on a healthy connection — so the latch window is a full window and the two gates now differ deliberately. `firmware/src/staleness.hpp`'s cumulative estimator and its hardcoded `kSummaryPeriodUs = 500000` are **superseded**, and stage B lifts the replacement across. | **Sharing one median with the grey threshold would have produced a confidently wrong number, which is worse than a missing one.** The threshold must adapt — a feed that has permanently halved its rate should not cry wolf — but an age computed against an adapting median is blind to exactly the failure it exists to catch: a socket throttled to a quarter of the broadcast rate sees its rolling median settle at 4× the interval within one window, after which `elapsed − n × median` is **zero**, so the meter would peak while the median adapted and then read 0.0 s while the true lag climbed to 111 s. That is the reading M3 stage E was built to make impossible. The baseline is legitimate because **a fresh socket's server-side send queue is empty**, so the cadence observed at the start of a connection is the broadcast cadence — the one moment a single client can measure the venue's clock — and it satisfies `PROTOCOL.md` §3.5's instruction to derive the cadence from the connection rather than from a document, per connection, with nothing hardcoded. **The blind spot it leaves is a proof, not an oversight:** a connection already behind when it latched cannot know it, because *"the venue broadcasts at 2 Hz and I am two minutes behind"* and *"the venue broadcasts at 0.5 Hz and I am current"* are byte-identical on the wire. Measured on the one real backlogged capture (`_local/drain-120ms.ndjson`, 236 summaries in 239.9 s = 49% of broadcast, so a true lag near 122 s): the meter latches 969 ms and reports **12.5 s**, a 10× under-read, and the only visible symptom is the baseline itself — which is why the baseline is printed beside every age. Server-side the case cannot arise (a queue not yet created cannot be full), so what is undetectable is a *client* too slow from birth, which the board's own RX loop was instrumented for across the 23.6 h soak and exonerated. What closes it is the client ping, deferred to M6 with its reason (triage decision (a)). Sliding rather than cumulative for two further reasons, both measured here: a cumulative estimate never forgets a bias (one unparsed liveness frame adds an interval **for ever**), and it averages away stall-then-burst, which is the failure shape of a queueing venue. Cost: 2 KiB of window, an O(256) scan per publish, and a ceiling — the meter reports the lag accumulated **within** its window and under-reports an older backlog, which is the right direction for a number printed beside a live ladder. |
| 2026-08-18 (M4 stage B1) | **A GREEN CHECK MUST BE SHOWN CAPABLE OF GOING RED.** Mutation-verification is not a per-case habit applied to interesting tests — it is the standing condition for a test being counted as coverage. A check that has never been seen to fail is a claim about the world, not evidence about the code. Concretely: a test, pin or self-check may be cited as coverage for a behaviour only once the behaviour has been deliberately broken and the check observed to fail. Where that cannot be done, the check is recorded as **unverified** and does not discharge invariant #6. | **Three checks on this project have now passed for the wrong reason, and the three failures are the same failure.** (1) `ok_never_truncating < checksummed` read `0 < 49` on the reconnect trace — true, and true only because every comparison failed; the criterion was reporting CATCHES off an instrument that was measuring nothing. (2) Fifteen `test_replay_goldens.cpp` cases pinned `ReplayOptions::legacy_anvil()`, so the path the object actually ships — the calibrated threshold — had no golden at all, and the suite's green said nothing about it until one case was added deliberately. (3) A per-commit verification ran in a worktree that was configured against the main tree, so `CMAKE_HOME_DIRECTORY` pointed at the source being verified *from* rather than the one being verified — a green build from the wrong tree. Each was found by accident, none by the suite, and in all three the instrument was healthy and pointed somewhere else. The general form matters more than the three instances because the failure is not detectable from the green: a check that cannot fail and a check that is passing are the same colour. This is the epistemic sibling of the 2026-08-17 close-out row's *shared-parent agreement read as corroboration* — both are an observation that answers a NEARBY question being read as answering the one that mattered, which is now this milestone's recurring defect at instance eight and the reason it is being written as a standing condition rather than a lesson. **Cost, stated so it is not discovered as friction:** mutation-verifying a check costs a build and a deliberate edit that must be reverted, so it is not free and is not automated here. What makes it affordable is that it is required per BEHAVIOUR, not per assertion — one mutation that swaps a dispatch covers every golden downstream of that dispatch. |
| 2026-08-18 (M4 stage B1) | **The delta venue's book lives in the ADAPTER, truncated to the subscribed depth, and the engine's book is a MIRROR built from what the adapter emits.** Kraken sends no removal for a level that falls out of the subscribed depth — the client truncates its own book — so somebody must hold a depth-limited ladder. Two places were available: the engine's book learns a subscribed depth, or the adapter holds the venue's book and emits the resulting changes as `Delta`s. **The adapter, and the deciding argument is B2 rather than taste:** the CRC32 is computed over the top 10 of the TRUNCATED book, so whoever truncates is whoever can verify. Putting it in the engine would force the adapter to read the book back to checksum it — inverting the one-way adapter→engine flow invariant #2 exists to protect, and putting a venue's CRC rule inside `engine/`. A level evicted by truncation therefore leaves the adapter as an ordinary `Delta{qty = 0}`, which the §4 vocabulary already says, so **no new `FeedEvent` variant and no new `GapReason` were needed** — the third time that question has been asked and the third no. `engine/ladder.hpp` holds the one definition of *better* and the three array operations, shared by both ladders. | **A mirror that sorts differently from its original is a book the panel draws and the adapter never held, and no counter anywhere would say so.** Both ladders were first written with their own private `better()`; they agreed, which is precisely the condition under which a duplicated rule survives long enough to matter. Sharing the MECHANISM and not the POLICY is what makes that impossible rather than unlikely: finding a rank and shifting an array are identical everywhere, while what to do about a displaced level is a venue decision (emit a removal, because Kraken sends none) or an engine one (count it, because through a truncating adapter it cannot happen). The cost is stated rather than discovered: two copies of the book, 8 KiB of ladder on top of the 8 KiB staging frame, in an adapter that is one per build until M7. |
| 2026-08-18 (M4 stage B1) | **`SymbolSpec::qty_step` becomes `qty_decimals`, and this is a semantic replacement rather than a rename.** The old field meant *wire quantity units per Qty step* and DIVIDED (`out = raw / qty_step`). That shape can only express a step COARSER than the wire unit, and it was never exercised, because Anvil is the only venue consumed before now and quotes whole units — **every scaling path in the tree was correct only because the value was 1.** Kraken's step is `qty_increment: 1e-08`, finer than the wire unit and **not representable in the old field at all**: there is no integer n for which dividing by n scales `0.65540712` to an exact integer. A divisor field would have had to become a multiplier, and a multiplier that is always a power of ten is a decimal scale in disguise — so it is declared as one, mirroring `price_decimals`, and both are now applied by `parse_scaled`. The old exactness check (`raw % qty_step != 0`) is not lost; `TooManyDecimals` rejects the identical class of disagreement, loudly and never by rounding (§4). | **The tell was that the field could not express the second venue it was designed for.** §4 has said *tick_size / qty_step supplied in venue symbol metadata* since M0, and the integer-divisor spelling read as a faithful implementation of it right up to the moment a real venue metadata document was opened. What made it survive is that the identity case is indistinguishable from the general case: at `qty_step = 1` a divisor, a multiplier and a scale all agree, so three years of goldens would pin any of them. **A parameter that has only ever held its identity value has not been tested, it has been declared** — the same shape as this milestone's other quiet passes, and the reason the replacement was spelled with designated initialisers everywhere: a bare `{101, 4, 1}` would have kept compiling while silently asserting one decimal place. |
| 2026-08-18 (M4 stage B1) | **No baseline, no deltas — and *absence of a subscribe is not failure of a subscribe*.** The adapter's subscribe state starts at `Unknown` and ACCEPTS book messages there, because the extreme quiet-pair slice begins mid-stream with no `status`, no subscribe and no ack anywhere in the file, and an adapter that required an ack would emit nothing for **the one artefact carrying the 25,843 ms healthy book silence the 2026-08-17 staleness ruling rests on**. Separately, an update arriving before the first snapshot is COUNTED AND DROPPED rather than applied: measured, replaying that slice's 49 updates onto an empty ladder reproduces **0 of 49** of the venue's checksums, against **4,878 of 4,878** on the four slices that open with a snapshot. The book stays `Stale{Resync}`, the heartbeats still stamp the liveness clock, and the trace stays exactly as usable for the ruling as it was. A REFUSED subscribe is the opposite and is terminal: `Gap{Disconnect}` plus a latched `refused()` the firmware turns into `die()`. | **The two halves look like the same question and are opposite answers, which is why both had to be written down.** *We have not been told we are subscribed* is not evidence of anything and must not stop the feed; *we have been told we are not subscribed* is conclusive and must stop it immediately, because stage 0 measured that the socket then STAYS UP, heartbeating at 1 Hz over a permanently empty book, with nothing that looks like an error after the first second. Conflating them either way produces invariant #5's forbidden output: refusing on absence kills the evidence trace, and tolerating a refusal draws a live-looking empty panel for ever. The dropped-deltas rule is the same honesty one level down — **a book assembled from amendments to nothing is not an approximation of the venue's book, it is a different book that looks plausible**, and the checksum says so in one number. |
| 2026-08-18 (M4 stage B1) | **Kraken's CRC32 is reproducible from INTEGER TICKS ALONE — no wire text has to be retained anywhere — and that is what makes B2 a check rather than a parse.** The checksum tokenisation is *decimal text, point removed, leading zeros stripped*. Measured before the adapter was written: every price and quantity in all five committed slices carries EXACTLY the venue's declared precision (8,172 level entries — BTC/USD 1 price decimal, MINA/GBP 4, both 8 qty decimals, **zero** exponent notation on the book channel). At exactly that precision the checksum token IS the decimal spelling of the scaled integer — `0.00005100` at 8 decimals is 5,100 steps and tokenises to `5100` either way. Verified end to end: **4,878 / 4,878 checksums reproduced from integers alone** on the four slices with an opening snapshot. So `KrakenFrame` carries `checksum` as a number and has no text buffer, and invariant #3 turns out to be not merely compatible with this venue but SUFFICIENT for it. | **Stage 0 established that a float parser fails every checksum; this is the complementary half, and it is the one that decides the adapter's storage.** *Do not use floats* leaves open whether the exact decimal TEXT must be kept — and if it must, the adapter needs a per-level text store, which is a per-frame allocation or a second fixed buffer, on the exact path invariant #7 governs. The measurement closes it: the integer form is not a lossy convenience, it is byte-equivalent to the wire under the venue's own tokenisation. **The residual assumption is named rather than buried** — this holds only while the venue spells every number at its declared precision, which is an observation over 8,172 entries at two hours of one day, not a contract statement. A venue that started emitting `0.5` where it now emits `0.50000000` would break the reconstruction and NOT the parse, so B2's CRC check is also the detector for that, and its first mismatch would mean this row rather than a lost message. |
| 2026-08-18 (M4 stage B1, from the review) | **A CHECK THAT CAN GO RED STILL PROVES NOTHING IF EVERY AVAILABLE INPUT MAKES THE WRONG ANSWER COINCIDE WITH THE RIGHT ONE — and captured traces cannot cover that class, because a trace records a venue BEHAVING.** This is the companion to the same day's mutation-verification row and a different failure: there the check could not fail, here it could and the corpus never asked it to. **Two defects found at review tonight, both after the suite was green, both this shape.** **(1) `adopt_snapshot` truncated nothing** — it seeded every level a snapshot carried, capped at the 256-level staging buffer rather than at the subscribed depth. All five committed slices passed, because Kraken serves EXACTLY the depth requested (10/10, 25/25, 100/100 measured at stage 0), so our cap and the venue's cap coincided on every file and the truncation rule was half-implemented behind a green suite. The consequence is Headline 2's defect precisely: a non-truncating book, wrong in 1,077 of 1,537 messages at depth 25. **Two live routes reach it and neither is exotic** — a capture whose metadata records no `depth` falls back to the firmware's compile-time constant while the file may be a depth-100 slice; and `ack_depth_mismatch` exists in the adapter precisely because a venue may serve a depth other than the one asked for, since **Anvil already rounds depth UP** and `NOTES-kraken.md` requires this adapter to assume neither behaviour. Reproduced in one synthetic frame — a 5-level snapshot against a subscribed depth of 2 left the ladder holding 5. **(2) A use-after-move in `KrakenTraceDecoder`** — it owned a `std::string` symbol that the adapter's `SymbolConfig` viewed. `Replay` takes its decoder BY VALUE, so the decoder is moved, and a moved `std::string` short enough for SSO copies its bytes into the new object's inline storage, leaving the view pointing into the moved-from husk. It passed the test that constructed a decoder directly and failed only through `run_replay` — the one path that moves. In both cases the test and the world agreed by accident. | **The rule this leaves, and it inverts the usual ranking of evidence: where an adapter's assumption and a venue's behaviour could coincide, the SYNTHETIC case is mandatory and the golden is decoration.** A captured trace is the strongest evidence available for *what this venue does*, and it is no evidence at all for *what this client does when the venue does something else* — which is the entire content of a truncation rule, a depth fallback, or a reconnect path. Invariant #6 asks for a trace and a golden; it does not ask whether the trace can reach the branch, and five files that all agree with the code are five copies of one observation. The second defect generalises the same point past venues: **a test that constructs the object differently from the driver is testing a different object.** Direct construction and construct-then-move are not the same lifetime, and only one of them is what ships. Both defects were invisible to 326 passing cases and 855,439 assertions, which is the number worth keeping beside this row — coverage counts measure how much was run, never whether the input that discriminates exists in the corpus. The cheap operational form, applied for the rest of M4: for every constant an adapter shares with a venue (depth, precision, cadence, ordering), write the case where the two DISAGREE, and do not wait for a capture in which they do. |
| 2026-08-18 (M4 stage B2) | **The healing path needed NO new vocabulary, and that is the fourth asking and the fourth no.** Kraken's CRC32 is now compared against the adapter's own ladder after every applied book message, and a disagreement raises `Gap{ChecksumFail}` — a reason §4 has carried since M0, written down for this venue before any of this code existed. The book is dropped, not merely marked; the deltas already emitted for that message stand, because they did happen and the Gap behind them says the result cannot be trusted; and no second `Gap{Resync}` is emitted on the way out, because §4's rule is that the book stays unknown until the next **Snapshot**, not until the next Gap. Separately and by the same argument, an `unsubscribe` ack drops the book and raises `Gap{Resync}` — also frozen vocabulary, and also the reason already written beside it. **The one genuinely new thing is a latch, not a word:** `KrakenAdapter::resync_wanted()`, which the transport reads, acts on and clears, in the same shape as `refused()`. | **A CRC failure is the only case in this system where the socket is perfectly healthy and the book is not, and Kraken never re-snapshots on its own.** A disconnect is followed by a reconnect and a fresh subscribe, so the supervisor produces a snapshot without being asked; here the heartbeat keeps arriving at 1 Hz over a book we have just proved wrong, and without somebody asking, a client that detected corruption would sit grey over a live chattering socket **for ever** — invariant #5's forbidden output wearing the other face, honest and permanent. The latch is `engine/`'s half of that (it has no socket, invariant #1) and the cadence is deliberately NOT here: the adapter has no clock, and a cadence that begins with a teardown is a floor set by the operation it interrupts, not a latency knob (§9, 2026-08-16 pm). A storm cannot come from the deltas that follow a failure, because dropping the baseline makes every one of them *unverifiable* rather than *failing* — which is also why the checksum ledger counts three disjoint outcomes and asserts their sum, rather than a pass/fail pair that would have hidden the distinction. |
| 2026-08-18 (M4 stage B2) | **The venue's checksum validates the TOP TEN LEVELS A SIDE AND NOTHING ELSE, and at the shipped depth of 25 that is 20 of the 50 levels the ladder holds: of the panel's 54 rows, 20 carry a validated level, 30 carry an unvalidated one and 4 are empty by construction.** Recorded as a stated limitation in `kraken_checksum.hpp`, in `NOTES-kraken.md` and on `dc_ladder`'s own report line, rather than as a fact a reader could infer from a constant. It is not a reason to distrust levels 11–25 — a delta stream that checksums clean at the top has provably lost no message, so an error down there would be ours rather than the wire's — it is the statement that **a defect confined below level 10 is undetectable by this check**, which is the exact shape of the non-truncating book stage 0 measured as 100% "correct" on a top-10 CRC at depth 100. | **Stage 0 confirmed the top-10 RULE by reproducing 8,677 captured checksums; that is no evidence at all about the BLIND SPOT, because every captured message agrees with both readings.** The venue has no reason to send a message whose only effect is invisible to its own checksum, so the discriminating input cannot be captured and had to be synthesised: a book edited below level 10 leaves the CRC unmoved, the same edit at level 10 moves it. Mutation-verified in the direction that matters — widening `kChecksumLevels` from 10 to 25 turns 6 cases and 28 assertions red, including all 4,878 committed checksums, so the corpus discriminates the VENUE's rule and not merely our implementation of one. This is the 2026-08-18 coincidence-class row applied to a constant the adapter shares with the venue, which is what that row asked for, and it is the reason `kKrakenSubscribeDepth` is 25 rather than 100. |
| 2026-08-18 (M4 stage B2) | **A CAPTURE IS NOT FINISHED UNTIL IT HAS BEEN READ BACK, AND A CORPUS THAT HAS NEVER CONTAINED A FRAME KIND IS A THIRD KIND OF BLIND SPOT — neither a missing golden nor a missing synthetic case.** Two defects, both found by *widening the corpus* rather than by review or by a test. **(1)** The parser filed every `method` frame as a subscribe ack (`if (have_method) return SubscribeAck`) because no trace at any venue had ever contained an `unsubscribe` — the healing path is the first thing this project has built that sends one. Benign while the venue answers `success:true`; one refused unsubscribe from latching `refused()`, which the firmware turns into `die()`. Fixed by reading the method name; an unrecognised method now falls to `Unknown` rather than to the nearest familiar thing. **(2)** The new `--resubscribe-after` capture mode wrote its `tx` record at the top of the message loop, stamping it *after* a message it then wrote *behind* it — `rx_ns` went backwards and `TraceReader` rejected the whole file. Found by replaying the first capture; a capture tool contains no reader. | **The operational rule this leaves is narrower and cheaper than "write more tests": when a stage makes the client SEND something new, capture what comes back before trusting any code that handles it.** The 2026-08-18 coincidence row's cheap form — *for every constant an adapter shares with a venue, write the case where the two disagree* — would not have found either of these, because neither is about a shared constant. Defect (1) is about a frame kind that did not exist in 9,932 captured frames, and no amount of reasoning over the existing corpus reaches it; defect (2) is about the tool that makes corpora, which is the one thing in this pipeline that nothing else validates. Both took minutes to find once a 96-record capture existed, and the capture cost 72 seconds. **The asymmetry between those two numbers is the whole argument**, and it is the same one ARCHITECTURE §9 (2026-08-07) made when it declared `TraceReader::next` the single definition of a valid trace: the reader is the authority, so anything that writes a trace must be run through it before the trace is believed. |
| 2026-08-18 (M4 stage B2) | **KRAKEN QUEUES TO A SLOW CONSUMER. `age_ms` at this venue is a lag and stands as shipped** — the assumption B1 pinned with B2 named as its owner is discharged by measurement rather than carried to M5. `tools/kraken_backpressure_probe.py`, one connection, BTC/USD depth 25, 80 s in three phases, throttled with the 120 ms drain lever: **331/331 checksums clean under the throttle** (nothing lost), **lag growing +0.690 s/s to 26.0 s** off the venue's own microsecond timestamp (everything late), and on release **the backlog draining at 64.2 msg/s against a 19.5 msg/s baseline** with lag decaying back to its start (there was a backlog to deliver). The socket survived a 26 s backlog without closing, so the brief's rows two (sheds → stop and raise) and three (server disconnects us) did not occur. | **A DEFICIT IS AN AGE ONLY IF THE VENUE QUEUES, and this project has already been wrong about that once at the other venue** — `anvil_drain_probe.py` concluded "Anvil sheds" from a rate measurement on 2026-08-09 and was superseded on 2026-08-11 by a freshness measurement showing it queues. Rate cannot distinguish the two; a client drained to a quarter receives something every 250 ms whether that something is current or ninety seconds stale. So the probe uses **two discriminators that fail under opposite hypotheses** — the CRC sees loss and is blind to lateness, the venue timestamp sees lateness and is blind to thinning — plus a release phase, and it prints a mechanical verdict including an explicit NULL RESULT branch for "the throttle never bit", which a single-signal probe would report as an answer. **The internal cross-check is what makes it more than three plausible columns:** the slope implies the venue was producing ~27.8 msg/s during the throttle and the release volume independently implies ~27, two routes to the same number with neither derived from the other. **Scope, stated so it is not over-read:** the lever is TCP backpressure, so this measures the whole path rather than Kraken's internal send queue, and unlike Anvil's — whose `deliver()` is readable source — it is an observation and not a contract. It could change without notice; re-running it is one command. |
| 2026-08-19 (M4 stage C) | **THE RENDER WINDOW'S AXIS IS RANK, NOT PRICE, AND A MEASUREMENT DECIDED IT RATHER THAN A PREFERENCE.** Stage C was proposed with three candidate row policies, two of which were price axes — a fixed tick band around the mid, and a density-adaptive version of it. Measured over every committed trace before any of it was written: **only one adjacent level pair in five is a single tick from its neighbour** (19.5% on the densest book in the repository, 10.0% on the sparsest), and one side of 25 levels occupies **182 ticks on BTC/USD and 6,613 on MINA/GBP**. A 27-row panel used as a 27-tick price axis therefore renders about **four of twenty-five levels** on the liquid pair and **one — the touch — on the quiet one**. So the three policies that shipped select by rank: `TopOfBook` (the best 27), `LargestFirst` (the touch plus the largest behind it) and `ThinnedTail` (the best 13, then the tail sampled at a stride derived from the book). All three render only levels the book holds, which is what keeps §5's `DisplaySnapshot` contract true and its `sizeof` unmoved. | **A PRICE-AXIS WINDOW IS NOT REFUTED, IT IS BLOCKED — and the block is one field wide, so it is recorded with its price rather than left as a dead end.** Any policy whose rows are ticks needs to say *row 7 is not a level* with the unknown rows INTERLEAVED, and `bid_count` can only express trailing ones. Two 32-bit present-masks would carry it exactly (27 rows fit a `uint32_t`) for **8 bytes on `DisplaySnapshot` and 24 in the three-slot mailbox**, plus correcting the three documents that quote the size. Stage C's brief says stop and raise rather than take it, so it is raised. Widening a row to span several ticks is the alternative that needs no field and it costs more than it saves: a row would become an AGGREGATE of levels rather than a level — which §1 of the brief forbids and which no renderer in this project can draw — and its width would be a constant taken from the market's own span, which is the mistake the 2026-08-16 (pm) row is about. **The measurement is also the reason the candidate set could be changed at all**: the brief invited a different third policy if the book's shape argued for one, and the book's shape argued for a different second as well. |
| 2026-08-19 (M4 stage C) | **AT THE SHIPPED DEPTH THE POLICY CHOICE IS NOT A CHOICE, AND THAT IS THE STAGE'S HEADLINE.** Kraken subscribes at depth 25 and the panel has 27 rows, so **every level gets a row and all three policies produce byte-identical windows** — `levels_dropped` is 0 on every committed Kraken slice, in every frame. The policies only become distinguishable when the book is deeper than the panel, which in this repository means Anvil's 101 and Kraken's depth-100 slice. Asserted as a property in `test_window.cpp` rather than noted, because it stops being true the moment somebody raises the subscribed depth. | **So the real question stage D inherits is not "which window" but "should the firmware subscribe deeper than it can draw", and B2 supplies the other half of that trade.** Kraken's CRC32 covers the top 10 levels a side whatever the depth, so at depth 100 a 27-row window shows at most 10 rows a side the venue ever confirmed — and WHICH policy decides how far below that it reaches. Measured over the depth-100 slice: `top` and `thinned` render 104,120 of 281,124 rows within the checksum (37.0%), and `LargestFirst` renders **33,433 — 11.9%**, because it reaches for size deep in the book and the checksum does not follow it out there. `thinned` matching `top` exactly is arithmetic rather than luck: its head is the best 13 ranks and the CRC covers the best 10. Anvil's row is worth as much: `validated_depth` is **0** there — no checksum exists anywhere in that protocol — so the honest count is zero confirmed rows, which is precisely the reading a missing field would have turned into "all of them". |
| 2026-08-19 (M4 stage C) | **§5's TICK-INDEXED DENSE-WINDOW STORAGE IS STILL UNBUILT AFTER THE STAGE NAMED FOR IT, AND IT IS UNBUILT ON ARITHMETIC RATHER THAN BY OMISSION.** §5's target design is a contiguous array of `Qty` addressed by `(px − anchor)` with a cold tail — a STORAGE strategy, whose purpose is to make a level lookup O(1) instead of O(depth). Stage C's brief is about which levels of a deeper-than-the-panel book earn a rendered row, which is a question about `publish`. Both are called "the dense window" and they are not the same thing; C built the second. §5's text stands unamended and describes an intention, not the code. | **The storage half would be optimising work that has never been shown to cost anything.** `Book::amend_side` is an ordered insert over a side of 25, at a measured 1.55 changed levels per message and ~26 messages a second — on the order of a thousand 16-byte shifts a second, against a 240 MHz core that spends its time on TLS. The re-anchoring copy and the cold-tail map would add two structures and a boundary between them to remove that. **Recorded rather than left silent because §5 promises it by name and a future session will come looking for it**, and because the honest trigger for building it is a measurement nobody has yet had a reason to take: a venue whose subscribed depth is large enough for O(depth) to appear in a profile. Kraken's 500 and 1000 tiers would be that venue, and `kMaxSnapshotLevels` (256) already refuses them. |
| 2026-08-19 (M4 stage C, from the review) | **AN ABSENT CAPABILITY AND AN UNREAD ONE ARE INDISTINGUISHABLE DOWNSTREAM UNLESS THE ABSENCE IS REPRESENTED. So a venue's lack of something gets a VALUE and a reason, never a missing field.** The instance: `validated_depth` is how many of a venue's best levels the venue itself confirms. Kraken's CRC32 covers 10. **Anvil publishes no checksum anywhere in its protocol, so its row reads 0 and the console says it in words** — *"this venue publishes no checksum, so NO rendered row was ever externally confirmed"* — rather than printing `0 of 66,150`, which reads like a broken counter. Had the field simply not existed for Anvil, a report with no validation line would read as *not applicable*, and *not applicable* is heard as *not a concern*: the same 27 rows, the same panel, and an operator holding the opposite belief about whether anything on it had ever been checked. **Enforced rather than documented — a `kVenueTable` row must carry a note explaining its `validated_depth`, checked at compile time**, so the next venue's author has to write down what its zero means. | **This is the 2026-08-18 never-observed row generalised past frame kinds, and it will recur at every venue this project adds.** There the absence was in the CORPUS — a frame kind no capture contained, so the code and every committed file agreed — and it closed the moment somebody captured one. Here the absence is in the VENUE, and it is the harder case precisely because **nothing will ever capture it**: no future trace can contain an Anvil checksum, so the gap cannot close by accident. **The project has already arrived at this same answer at least eight times without naming it, which is the evidence that it is a rule rather than a case.** `has_age` — *no reading yet* and *the book is current* are different statements and exactly one of them is reassuring. `has_last`. `initialised`, this stage — *nothing yet* against *nothing there*. `checksums_unverifiable` (B2) — 49 unverifiable, not 49 failed. `book_msgs_unchecksummed` (B2) — an adapter fed messages carrying no checksum verifies nothing while every other counter looks healthy. `NOT_A_CHECKSUM_GOLDEN` (stage A) — *cannot be graded* is a third state beside *pinned* and *forgotten*. `clock: undeclared` in the trace metadata — an absent clock reads back as undeclared rather than being inferred from the venue, because a sound inference nobody re-checks is how this project has been wrong before. And §4's oldest one: Anvil publishes no tick metadata, so DepthCharge **declares** a scale and verifies every wire price against it rather than guessing a default. Eight independent arrivals at one answer, each argued from scratch. **The operational form, for M5's Binance and everything after it: for every capability one venue has and another lacks, give the lack a value, give the value a note, and make the reporting path say it in words — because a zero is also what a missing measurement looks like.** |
| 2026-08-20 | **M4 stage D, Part A — §5's age and the grey rule now run ONE implementation, because `firmware/src/staleness.hpp` is DELETED.** `sample_window.hpp`, `liveness_clock.hpp` and `age_estimator.hpp` moved from `harness/include/dc_harness/` into `engine/include/depthcharge/` (namespace `depthcharge`), the firmware links them, and `kRxWatchdogMs` went with the file. **The firmware's watchdog no longer arms on book events at all**: it arms on the venue's declared liveness signal against `LivenessClock`'s self-calibrated threshold, which is the 2026-08-17 ruling reaching the target. Book silence is `age_ms` and nothing branches on it. `engine/` acquires floating point for the first time — the two clocks are `double` throughout — which is legal under invariant #3 (instruments, not book data) and is a measured cost rather than a free one. | **A divergence invisible to every host test can only be closed by deletion, and this is the second time this project has reached that conclusion.** The two estimators agreed for two milestones *because Anvil broadcasts at the 500 ms the deleted one hardcoded* — the coincidence class, one level up (2026-08-18). No test could see it; a Kraken build would have put an age on the panel wrong by exactly 2x with nothing on the board saying so. **What the deletion cost, measured against the pre-change image and recorded because M3 banked the opposite as a property:** `feed_task.cpp.o` 6,708 → 8,806 B of `.text` with eight soft-float helpers where it had none, `render_task.cpp.o` 16,523 → 19,082 B, the whole image 877,053 → 880,885 B of flash and 142,048 → 144,488 B of RAM. The M3 streaming-parser acceptance recorded *"zero floating-point instructions in the disassembly"*; **that property is now gone, on purpose.** The runtime cost lands in `worst_parse_us`, which the board already prints, so the bench produces the number rather than an argument about it. |
| 2026-08-20 | **M4 stage D, Part A (from the review) — A CROSS-TASK REQUEST THAT CAN BE REFUSED MUST BE A LEVEL, NOT AN EDGE.** The firmware's healing path was built edge-triggered: the adapter latched `resync_wanted()`, the feed task consumed it into a one-shot flag, and the transport consumed that with an `exchange(false)`. Review found **four** paths on which the request was destroyed and nothing could re-raise it — the 5 s floor, a request with no socket, a request arriving mid-heal, and the liveness watchdog firing over a LIVE socket (which drops the book and deliberately latches nothing, because *"a reconnect subscribes on its own"* — true for a dead socket and false for a 4 s RF fade). Each left the panel grey over a healthy heartbeating socket until a power cycle. The signal is now the STATE `!adapter.has_baseline()`, republished every frame and polled by the transport. | **The adapter's latch is not wrong; consuming it into a second latch is.** `KrakenAdapter` can only set `resync_wanted_` while the book is baselined, and dropping the book clears that — so once a request is lost the adapter is structurally unable to raise another, for ever. The general rule this leaves: **an edge is a claim about a moment, and a moment can be arrived at when nothing may be done about it. If the receiver may refuse, the sender must publish a condition rather than an event, so refusing costs nothing.** It also removes the storm argument from the request path entirely — the floor now paces SUBSCRIBE FRAMES, which is what the venue and the grey budget actually care about, and which is why it anchors on the last subscribe (the opening one included) rather than on the last completed heal. |
| 2026-08-20 | **M4 stage D, Part A (from the review) — A STALL IS NOT A RECONNECT, and only a reconnect may discard a measured baseline.** `AgeEstimator` gains `on_stall()`: it banks the peak and clears the arrival window, and leaves `baseline_ms_` alone. `on_reconnect()` keeps doing both and is called only from a real socket change. `read()` additionally returns *no reading* for an empty window rather than the clamp. | The estimator's own header already argued that a baseline is measurable **exactly once, at connect, when the server-side queue is empty** — and then the firmware's watchdog path called the function that re-latches it, on a live socket, at the worst possible moment. Anvil queues and never drops per socket, so a 40 s stall flushes ~80 summaries ~2.5 ms apart; a baseline latched from that reads 2.5 ms against a true 500, and **every age afterwards accrues 99.5% of wall-clock as phantom lag for the life of the connection** — a panel reading minutes behind while being current. The host replay driver models a watchdog gap AS a disconnect because in a trace file it is one; **this is the one place the two ends must deliberately differ**, and it is written down here because the next reader will otherwise 'fix' the asymmetry. |
| 2026-08-20 | **M4 stage D, Part A (from the review) — `kThresholdFloorMs` 1,000 → 2,000 ms. The floor had been sized against the wrong quantity.** | It was chosen as *"the smallest threshold this project has ever run"*, i.e. `kRxWatchdogMs` — which bounded **book-event** silence at Anvil, worst healthy 391–640 ms. It now bounds **liveness-signal** silence, whose worst healthy values are 968.8 ms at Anvil and **1,119.0 ms at Kraken**. A 1,000 ms floor is *below* the second of those, so a window still clamped from a drain burst would grey a perfectly healthy Kraken feed on an ordinary heartbeat — **the exact false grey the floor exists to prevent, produced by the floor itself.** The general form: **when a constant is re-pointed at a new quantity, its derivation must be re-run against that quantity — a number that was well-evidenced for the old one carries none of that evidence across.** It survived review's first pass because the test that pinned it exercised Anvil's cadence only; it is now swept at both venues' worst healthy intervals. |
| 2026-08-20 | **M4 stage D, Part A (from the review) — the panel republishes on the venue's clock, not only on the market's.** A liveness arrival that produces no book event now republishes the unchanged book (at most once per second). | **A liveness indicator gated on the thing it exists to detect the absence of cannot do its job.** `RenderTask` redraws only on a new `DisplaySnapshot` version, and versions only appeared when an event reached the book — so through MINA/GBP's 26 s of legitimate book silence, *which is M4's own definition of done and the reason this build subscribes that pair*, the panel froze completely: ladder, age, and the corner beat pixel whose whole purpose is to say the renderer is alive. A bench watching for "holds colour through 26 s" could not have distinguished a healthy quiet market from a hung Core 1. It is also a correctness point rather than a cosmetic one: `age_ms` is stamped at publish, so the one number that would say *live, and ninety seconds behind* could not update in precisely the case it exists for. |
| 2026-08-23 (M4 stage D, B3) | **THE HALF-OPEN CASE IS REAL — IT HAPPENED TWICE IN 25 HOURS AND THE LIVENESS WATCHDOG BEAT THE TRANSPORT BY 141 s AND 291 s — BUT THE WIN IS CONDITIONAL ON THE VENUE SPEAKING WHEN THE BOOK DOES NOT, AND THAT CONDITION IS A CONSTRAINT ON M5.** The 25.39 h soak of 2026-08-22/23 (`hardware/bench-2026-08-22-kraken-b3-soak.log.gz`, raw sha256 `6a9139f6…fa8`) recorded `wd` 0→1 at uptime 15,492 s with `sock` following at 15,633 s, and `wd` 1→2 at 85,446 s with `sock` at 85,737 s — both measured on the SAME clock, the SOAK line's own, at 10 s cadence. In both outages the heartbeat stopped and the socket was still believed up for another 2.4 and 4.9 minutes. **This is the exact condition B1 could not stage from the network side and had to mute the firmware to produce, occurring unprovoked.** | **The staleness ruling of 2026-08-17 is now load-bearing rather than argued.** The deleted `kRxWatchdogMs` counted book silence and would have false-fired 183 times in A5's 31 minutes; the transport's own socket detection was 141 s and 291 s late; only the calibrated liveness threshold caught these, at ~4 s. Without it the panel shows a coloured, frozen ladder for minutes — the one output ARCHITECTURE calls unacceptable. **The conditionality is the half that must be carried forward, and it is not a caveat.** The watchdog beat the transport *because Kraken broadcasts an unsolicited 1 Hz heartbeat*. At a venue that speaks only when the book moves there is no liveness signal to count, the threshold has nothing to arm on, and this same code detects nothing at all — it does not degrade, it goes blind. Binance at M5 must therefore declare its liveness signal before an adapter is written, exactly as stage 0 priced Kraken's wire before B1 wrote one; if it has none, M5 owes a different mechanism (the client ping, deferred to M6 at triage) and not a smaller threshold. **Three of the four combinations are now observed and the fourth is the argument.** Besides the two half-open events, a socket died with no preceding liveness stall (`sock` 2→3, `wd` unmoved), and the watchdog fired over a socket that SURVIVED (`wd` 2→3 at `up=127760s` with `sock` and `connects` unmoved for the following twenty minutes — the resync LEVEL healing the book with no reconnect, which is the review's critical fix working unprompted in the field). **The fourth cell — book silent, socket alive, heartbeat arriving — was never observed and CANNOT be**: it is precisely the case point 6 of the 2026-08-17 ruling accepts as an unobserved false-colour, because a quiet market and a silently dead subscription are identical on the wire. Its absence from 25 hours is **consistent with the ruling, not evidence of coverage**, and reading an empty cell as a clean bill of health would be the coincidence-class error in its purest form. **UPDATED 2026-08-24, and the fuller count STRENGTHENS this row rather than qualifying it.** Counting every event across the soak and the two runs that followed it — per boot segment, since the counters reset — gives **7 socket losses, of which 4 were preceded by a watchdog firing (141 s, 291 s, 140 s, 301 s earlier)**, and **5 watchdog firings, of which 4 preceded a socket loss and 1 was healed over a socket that survived**. So the half-open case is the *plurality* pattern at 4 of 7, not the 2-of-3 curiosity the soak alone suggested, and the four lead times cluster tightly in two groups near 140 s and 300 s rather than scattering. **An earlier draft of this update said the opposite** — that half-open was the minority — by counting the later socket-alone events without re-counting the half-open ones in the same pass. Recorded because the error is this milestone's signature and it very nearly hardened into the constitution: **a tally updated on one side only is not a tally.** |
| 2026-08-23 (M4 stage D, B3) | **"193 CRC FAILURES, ALL HEALED" IS NOT BENIGN — THE HEAL/DROP LOOP IS REAL, MEASURED AT 8.2× ITS OWN BASELINE, AND IT IS THE FOUR-SLOT PIPE.** Against a Poisson baseline of 2.07% implied by 193 failures in 25.39 h, the probability that the next checksum failure lands within ten seconds of a completed heal is **17.30% (32 of 185)** — a ratio of **8.36×**. The inter-arrival histogram is bimodal where a venue artefact would be flat: 58 of 185 inter-arrivals are under thirty seconds against a mean gap of 478 s. `no_slot` reached **1,594** — 8.57 pipe drops per STALE(checksum), 8.26 per counted failure, against A5's 10 in 31 minutes. **The mechanism closes:** a drop corrupts the book, the CRC catches it, the heal re-subscribes, the snapshot burst overruns the four-slot pipe, and the next failure follows within seconds. | **This is the finding B3 existed to produce and no 31-minute run could have.** A5 saw 3 failures and 10 drops and recorded the pipe overrun as a consequence rather than a cause — correctly, on that evidence, because `no_slot` was 0 at the first failure. At 25 hours the direction reverses: the drops are now numerous enough to be the input as well as the output, and the loop is turning over slowly rather than resonating. **It has not yet found a resonance, and nothing here says it cannot.** Written up as benign it would have read as a venue artefact and closed. **Deliberately not fixed**, per this stage's own constraint that nothing is repaired at the bench: the remedy is a venue-sized pipe (Kraken's largest message is 1,970 B against a 16,384 B slot, so more slots cost less RAM), it is already priced in DESIGN §08, and it must not be done in the sitting that measured it. **The general form, and this milestone's ninth instance:** a rate that is a consequence at one timescale can be a cause at another, and the only thing that separates them is a run long enough for the loop to close. |
| 2026-08-23 (M4 stage D, B3) | **BOTH OUTAGES WERE DNS AND THE ASSOCIATION NEVER DROPPED; THE CACHED-IP FALLBACK IS PRICED WITH TWO GUARDS AND PARKED, NOT BUILT.** 105 failures to resolve `ws.kraken.com` (`rc 202`), every one at 13,999–14,000 ms, and **both successful reconnects also paid the full 14,000 ms** — the signature of a primary resolver that never answers and a stack falling through to a secondary on a fixed timeout, i.e. a specific dead server in what DHCP hands out rather than general upstream flakiness. **NARROWED 2026-08-24, and the narrowing matters because it changes where to look:** two later resolves on the same board returned in **10,784 ms** and **50 ms**. A resolver that answers in fifty milliseconds is not dead. What the soak measured was an OUTAGE WINDOW — inside one, every resolve pays the full timeout; between them, resolution is prompt. The practical impact is unchanged and so is every figure above, but *"dead server"* would send the next person hunting a permanently broken host when the fault is **episodic**. All 108 autopsy lines report `assoc=1` and there are **zero** Wi-Fi down/rejoin/up events in 25.39 h: the association never dropped. The firmware behaved correctly throughout — greyed honestly, retried, recovered unaided, twice. | **The fallback is a downgrade unless it carries both guards, which is why it is priced rather than filed as an easy win.** Kraken sits behind rotating infrastructure, so a cache must (1) carry a TTL and always attempt fresh resolution first, falling back only on failure, and (2) **keep using the hostname for SNI and certificate validation** — connecting to a cached address with the address as the name is how certificate validation gets silently disabled, and this project pins GTS Root R4 precisely so that it is not. Without both, the cache trades a visible 25-minute outage for an invisible loss of authentication. Backlog, not M4. **Recorded as an instance under the 2026-08-18 coincidence-class row rather than as a new finding: a two-point read of a noisy series is not a measurement.** A heap-exhaustion alarm was raised in this session from the first and last samples of the free-heap series and retracted an hour later by 3-hour medians (61,132 / 61,128 / 61,164 — flat). The series never changed; the reading did. `tools/soak_report.py` exists so that no figure reaching this table is hand-read again. **The same error nearly recurred on the largest free block**, where endpoints said 47,092 → 31,732 and read as a ratchet; the full plateau series shows it stepping down in exact 2,048 B units during a retry storm, recovering to 49,140 for 19.5 h, dropping again at the second outage, and — confirmed on the live board afterwards — **returning to 49,140 again. It is a sawtooth.** `Panel::begin()` allocates at boot and is not exposed; what is exposed is the reconnect's own TLS handshake, which is both the cause of the fragmentation and the thing that would fail if it ever went deep enough. Two events cannot distinguish a sawtooth from a slow ratchet with recovery. **And the same misreading was then made a THIRD time, on the same series, which is why this is a row and not a note:** a later reading claimed the second capture had turned two steps into three; it had not — one distinct value, 49,140, flat across all 132 pre-reboot samples. Three readings of one series, three different stories, none of them from new data. **What the series needs is not more runtime but more OUTAGES** — the two real steps were 19.5 h apart and both landed on a DNS outage — which makes the currently-faulty resolver the only generator of the events the question needs. Repairing it removes the instrument, and that is a trade to make deliberately rather than discover. |
| 2026-08-24 (M4 close) | **INVARIANT #5's “GREY, NEVER BLANK” NOW HAS ONE NAMED EXCEPTION — SIX ROWS OF BLACK UNDER THE HEADER TEXT — AND THE static_assert THAT GUARDS IT PROVES LESS THAN IT READS.** The stale reason was legible only at an oblique angle: a 4×6 glyph at 255 against the 64-grey wash has no contrast left once six-bit quantisation and the driver's CIE1931 curve have taken their share. `Ink::HeaderBed` is black in BOTH palettes and is the wash under the header band; `none_black_except()` takes a second permitted `Ink` rather than the rule being relaxed, so every other stale ink is still proven visible at compile time and an unassigned ink is still caught. `all_grey(kStalePalette)` is untouched — **the hue half of #5 is exactly as strong as it was, and no stale snapshot can reach a coloured pixel.** The stale wash then dropped 64 → 8 at the owner's call. | **Two things are weaker than before and both are stated rather than discovered later.** (1) *“The black stays inside the header” is now a rule, not a compile error.* Nothing stops a future `fill_rows(..., Ink::HeaderBed)` elsewhere; it is held by the per-frame stale sweep in `test_ladder_render.cpp`, which asserts no pixel below the header band carries that ink on every generated frame. That is good coverage and it is still a test, in a file whose entire thesis is that #5 should not depend on one. (2) *`none_black_except` cannot prove the wash is VISIBLE, only that it is not `{0,0,0}`.* At six bits under the CIE curve, 8 is under one percent of full scale, so the check now forbids a value indistinguishable on the panel from a value it permits. The wash remains the entirety of #5's “this panel is ON and not to be trusted” signal on a boot frame that has no ladder and no price, and that judgement was made **by eye, at the bench** — the build will not stop the next session turning the signal off. **The general form, and the reason this is a constitution row rather than a brief note: a structural guarantee survives its first exception only if the exception is named, bounded and written where the guarantee is.** This ramp has now failed the bench twice for the same reason — the whole palette was chosen at eight-bit depth and this build runs six — and the 2026-08-11 correction treated the symptom by lifting everything except the wash. |
| 2026-08-24 (M5 stage 0) | **BINANCE HAS NO DECLARED LIVENESS SIGNAL, AND THE ANSWER THE 2026-08-23 ROW DEMANDED IS THEREFORE "NONE" — ITS ONLY UNSOLICITED TRAFFIC IS A WEBSOCKET PING, WHICH IS NOT A RECORD AND CANNOT REACH A TRACE.** That row required Binance to declare its liveness signal before an adapter is written, and named the consequence if it had none. Measured over five 90 s captures: `@depth@100ms` on a quiet pair (ATOMEUR) emitted **30 messages in 90 seconds, not 900**, with a worst inter-message gap of **10,503.8 ms** — and `@depth20@100ms` behaves identically (29 messages, 10,503.7 ms). **Both streams are change-driven; neither is a heartbeat, and there is no `summary`, no `heartbeat`, no third record whose arrival proves the feed is alive without claiming the book moved.** What the venue does send is a **PING every ~20 s**, carrying a 13-byte millisecond timestamp, enforced by a 60 s pong deadline. `tracefile.is_liveness("binance", …)` therefore returns False for every frame, permanently and by construction rather than pending a decoder. | **This is the blind case of the 2026-08-17 ruling arriving exactly as the B3 row predicted it would: the calibrated liveness threshold has nothing to arm on, so it does not degrade — it detects nothing at all.** The B3 soak's headline was that the liveness watchdog beat the transport's own socket detection by 141 s and 291 s, and that win was **conditional on Kraken broadcasting an unsolicited 1 Hz heartbeat**. At Binance that condition is absent, so a Binance build inherits only the transport's own detection and the half-open case — the *plurality* pattern at 4 of 7 socket losses — goes unseen for minutes with a coloured, frozen ladder on the panel. **The mechanism the B3 row says is then owed is not a smaller threshold; it is the ping.** The venue's 20 s server ping is a real, unsolicited, venue-clocked liveness signal that is already answered correctly by `WsTransport::on_ping` — it is simply one layer below where every liveness decision in this project is currently made, because §4's boundary is defined over `FeedEvent`s and a control frame is not one. **So M5 owes a decision the previous two venues never forced: either the transport publishes ping arrival across the §4 boundary as a liveness event, or the panel greys on Binance by socket death alone.** The client ping (M6 at triage) is the *other* half and does not substitute — a client ping proves the round trip when we ask, whereas the point of a liveness signal is that it arrives when we do not. |
| 2026-08-24 (M5 stage 0) | **THE BINANCE ORACLE IS THE VENUE'S OWN `@depth20` STREAM, IT IS EXACT AT EVERY TICK, AND `U`/`u` BRACKETING CATCHES NONE OF THE BOOK-CORRECTNESS MUTANTS — 0 OF 3.** The partial payload's `lastUpdateId` coincided with a diff event's `u` on **every payload of every COMPLETE capture — 899/899, 901/901, 90/90 and 29/29** — so the comparison is exact rather than probabilistic. **The bound on that claim is stated with it, because the stage's own first draft contradicted itself over it:** a committed *slice* can carry a trailing partial that never coincides, and one does (150/151 on the 15 s slice). It was chased, not rounded away — the odd payload is the **last record in the file**, its id is beyond the highest diff `u` the slice contains, and the slice holds 151 partials against 150 diffs because `slice_trace.py` cuts on an `rx_ns` boundary that fell between the two streams' publications of one update boundary. A cut artefact, not a venue behaviour. So **`unverifiable` is a LIVE bucket, not a permanently empty one**, and the oracle named this case (`never-reached-before-capture-ended`) rather than matching or failing it silently. Graded against a correct client: **884/884**. Against three deliberate breakages — `qty: 0` not treated as removal, sides swapped, a book bounded to the rendered depth that cannot refill — it is **RED at 884, 884 and 722**, while `U`/`u` is **GREEN for all three**. `U`/`u`'s real reach is the other question entirely: it caught the deliberate mid-capture reconnect on the first event after it (`prev_u` 99076647376 → `U` 99076649580, 2,204 updates missed). Pinned as `binance_oracle_mutants` in `ctest`, and that check is itself mutation-verified. | **The clause that transferred unchanged from 2026-08-16 is the one that paid.** *Prove the trace catches a deliberately broken implementation before pinning it* — and it did so immediately, on a defect nobody had designed: seeded from the obvious `limit=100`, an otherwise correct client scored **734/891 (82.4%)** while `U`/`u` was **890/890 clean throughout**. That is the Kraken-depth-100 failure class reproduced at a second venue by a different mechanism, and it is why a sequence check must never be quoted as a book oracle. **Two costs are stated now rather than discovered at B2.** (1) **The oracle is blind below rank 20** — `@depth20` is the deepest partial-depth tier Binance offers on spot — while the panel renders 25 rows a side, so *the oracle's reach is narrower than the panel's*. **It is the same gap this project has lived with since M4 and it is SMALLER: Kraken's CRC validates 10 of the 25 rows drawn, Binance's partial stream validates 20 of 25.** Stated with both numbers so that a later session reading this row does not open an evening to close a hole already narrower than the one it has been shipping. (2) **It costs 141% more wire** (13.37 KiB/s on top of `@depth`'s 9.47) and is the *majority* of the stream at 58.6%, rising to 7.13× the total on a quiet pair. **REST re-snapshot comparison was measured and rejected as the primary**, on the argument M4 stage 0 wrote for it: at this venue the re-snapshot **is** the healing event and is scheduled rather than incidental, so a detector built on it measures recovery instead of the defect, and — unlike a Kraken resync — cannot be dodged by choosing a clean window, because a client that never re-snapshots is a client that never recovers. |
| 2026-08-24 (M5 stage 0) | **BINANCE'S WIRE PRECISION IS A UNIFORM 8 DECIMALS AND IS *NOT* THE DECLARED `tickSize` — A SCALE DERIVED FROM `tickSize` WOULD BE WRONG BY 10^6 ON BTCUSDT. AND `kMaxSnapshotLevels = 256` IS NOT ENOUGH.** Counted over **202,012 level entries** across five captures: 202,012 prices and 202,012 quantities carry **exactly 8 fractional digits; exponent notation 0; bare JSON floats 0** — on BTCUSDT (`tickSize` 0.01, 2 dp) and ATOMEUR (`tickSize` 0.001, 3 dp) alike. Separately, replaying with the maintained book bounded to N levels a side: 256 still fails **33 of 884** graded ticks inside 90 seconds; 500 and above are clean. | **The precision finding inverts what §6's *verified representable* check was expected to look like here.** Kraken's B1 pass proved the wire matched the declared precision; Binance's does not match it and does not need to — the wire is fixed-point at 8 decimals regardless of the symbol, so the **scale is a constant and `tickSize`/`stepSize` are validators** (is this value a whole multiple of the tick), not the source of the scale. Integer ticks are safe with room to spare: `78564.00000000` scales to 7,856,400,000,000, 43 bits, so invariant #3 needs no new path and the adapter needs neither a float nor a `Decimal`. **The depth finding is the one with a schedule attached.** The seeded window is a *price* window, not a level count: `limit=100` covers **$15.63–$16.68** per side and BTCUSDT moved **$29.85 in 90 seconds**, so the best ask left the seeded window on **406 of 901 ticks** and every level above it was one the client had never been told about — a resting order that predates the seed and is never restated never enters the diff stream. `limit=1000` covers ~$240 and is the recommendation, **but depth alone cannot fix this at any tier**: no fixed window survives indefinitely, so the board must **re-snapshot on a schedule**, and M5 owes that interval a measured basis (the walk rate against the chosen window) rather than a constant. |
| 2026-08-24 (M5 stage 0) | **A TRANSPLANTED MUTANT THAT SILENTLY STOPS BEING A MUTANT IS A SUITE LOSING ITS REACH WHILE KEEPING ITS GREEN — SO THE NO-OP IS ASSERTED, NOT DELETED. AND "GREEN" IS NOW THREE STATES, BECAUSE THE THIRD ONE HAPPENED.** Kraken's mutant list did not transfer intact: *no truncation to the rendered depth* is a **no-op at Binance**, scoring identically to the honest control on every capture, because `@depth` is the whole book and every removal is explicit (`qty: 0`) — so a non-truncating client is not wrong. Its Binance-shaped inverse (a *bounded* book that cannot refill) replaces it as the third mutant, and `--check` still runs the original and **fails if it ever starts being caught**. Separately, `binance_oracle.py` now reports **`VACUOUS (0 graded)`** distinctly from `GREEN`, and — added at review — **`NOT EXERCISABLE`** for a mutant that produced no observable difference on a given trace, with `--check` failing outright unless SOME trace exercises all three. | **The vacuous state is not hypothetical; it is what this tool did on its first run.** Oracle (b) compared a REST body against the **live** book, when a `/api/v3/depth` round trip measures ~1,003 ms at `limit=100` and ~1,481 ms at `limit=1000` — so by the time the body is in hand the stream has moved 10–15 events past the `lastUpdateId` it is stamped with. Nothing matched, nothing failed, and it printed **GREEN against every mutant while grading nothing**. That is precisely the *oracle that cannot fail* the 2026-08-16 clause forbids pinning, produced by an off-by-a-second rather than by a bad idea. **The general form: a detector's pass and its coverage are different facts, and a report that cannot distinguish "nothing was wrong" from "nothing was checked" will eventually be believed about the wrong one.** The same asymmetry has a second instance in the same evening — the committed slice cut *at* the baseline record rather than *containing* it took the oracle from 884/884 green to 250/250 red, because a snapshot must be reconciled **by id, never by position**, which is exactly what the venue's own buffered procedure says and what a position-based slicer silently violates. **And a third instance, which is the one that generalises furthest, found when review asked for a second witness.** A quiet pair was added on the reasoning that a boundary mismatch would be likeliest to show on a thin book. What it showed instead is that **on a book that never churns the bounded-window mutant is not caught, because it is not a mutant there** — ATOMEUR moved so little in 90 s that truncating to 25 levels a side never removed a level later needed, so the mutant's book is byte-identical to the honest one at every graded tick. Had that trace been added to the suite without the distinction, the suite would have gained a witness that could only ever agree. So the general rule, and it outranks the two instances above: **a capture too quiet to break is indistinguishable from an oracle too weak to notice, and a suite whose every witness is too quiet reports green for the same reason an empty one does.** Exercisability is now measured by comparing the mutant's graded books against the honest one's — *not* by a per-mutant heuristic, because the first attempt was one (*did the book ever exceed the window?*) and it answered EXERCISABLE for that very trace, wrongly: the seed held more than 25 levels, but none below rank 20 ever rose into view. |
| 2026-08-25 (M5, owner's ruling) | **BINANCE'S LIVENESS SIGNAL IS ITS WEBSOCKET PING, AND CARRYING IT WIDENS THE DECODER CONTRACT'S INPUT — IT IS NOT A VALUE IN THE VENUE TABLE.** What stamps the liveness clock is `RecordKind::is_liveness`, set per decoder inside `classify()` from the frame's own content and consumed at `replay_driver.cpp:88`; the signature is pinned by a `static_assert` (`trace_decoder.hpp:155`) and **`classify` takes a frame, which a control-frame arrival does not have.** So a ping that stamps the clock is a widening of what a decoder may be asked to classify. **That is the same widening the REST snapshot record needs, and the two are therefore one change, made once, in stage A.** The threshold is **~80 s** — the 4× margin both shipping venues use, against a measured ping median of **19.97 s** (min 19.85, max 20.2, n=23; the single 40.7 s is a deliberate reconnect restarting the schedule). | The alternative was to grey on socket death alone, and B3 priced that: the liveness watchdog beat the transport's own detection by **141 s and 291 s**, with half-open the *plurality* case at 4 of 7 socket losses. **What the ruling accepts in exchange is a 20× regression in grey latency** — ~80 s here against Kraken's ~4 s and Anvil's ~2 s — which is a property of the venue's cadence and not a defect in this object, and which is therefore **named in M5's definition of done rather than left to be discovered at the panel.** The margin is a live lever: the ping's worst/median is **1.01×** against Kraken's 1.12×, so a tighter multiplier is defensible on measured grounds and would land nearer 40 s. It is deliberately **not** taken now — 4× is what the other two venues run, and changing the constant and the venue in one step leaves no way to attribute a regression. |
| 2026-08-25 (M5, owner's ruling) | **THE VENUE'S `@depth20` AUDIT STREAM SHIPS ON THE BOARD AT THE 1000 ms TICK, AND THE MIXED CONFIGURATION WAS MEASURED RATHER THAN INFERRED.** Two independent 90 s deep-seeded captures of `@depth@100ms` + `@depth20` (`limit=1000`): the partial's `lastUpdateId` coincided with a diff `u` on **90/90 and 90/90**, graded **88/88 GREEN** both times. **The oracle stays exact across mixed cadences**, so the board runs a real oracle and not a degraded one — it verifies its own book against the venue's once a second. Cost is a **near-constant 1.33 KiB/s** (1.33, 1.33, 1.35 across every window measured; mean payload 1,362 B at 1000 ms against 1,368 B at 100 ms), which is **+8–14%** on the diff stream against the per-tick oracle's +141% — 9.98× cheaper, exactly the same payload at a tenth the rate. | `@depth`'s own cost is **not stable**: 9.47, 10.94 and 16.16 KiB/s across three 90 s windows, a **1.7× spread on market activity alone with no configuration change.** So the audit stream is not what threatens the byte budget — the market is, by 6.7 KiB/s against the audit's 1.33 — and a decision to save 1.33 in that budget would have been saving the stable term. **The stage-0 headroom figure of 3.25× was a quiet-window number**; in the busiest window measured, `@depth` alone is 1.9× and `@depth` + audit is 1.8×. **A headroom claim at this venue needs a distribution, not a 90-second sample** — that is strain 19's shape arriving in a new place — and **the soak that supplies it is D's** (corrected at M5 stage B2: this clause read *B2's soak* while `M5-the-shape-and-the-two-decisions.md`'s stage table and B2's own *Out of scope* both put the soak at D, and B2 has no soak in it. A stage that opens this row and leaves a stale ownership clause standing in it is how drift survives the one stage placed to catch it). What the ruling buys is the thing §1 claims: a Binance ladder that is honest about its book to the same standard a Kraken one is, instead of a venue-shaped hole in that guarantee. **EVIDENCE DISCHARGED at `a67f2e1` (M5 stage B2, 2026-08-26).** The carried caveat that stood here — that the two mixed-cadence captures this ruling is signed on were untracked, and that the figures above were therefore the one set of numbers in this table with no committed trace behind them — is struck, not annotated. `binance_btcusdt_mixed{1,2}_20260825.ndjson` are committed **whole**, pinned in `taxonomy_pins.inc`, `binance_pins.py` and `dc_binance_oracle_main.cpp`, and mutation-verified before being pinned. Every figure above reproduces from them in two implementations: 90/90 coincidence and **88/88 GREEN** on both, from `tools/binance_oracle.py` and from `dc_binance_oracle` driving the real adapter. **Whole and not sliced, deliberately** — see the guard-versus-evidence row below, which is the general rule this discharge produced. |
| 2026-08-25 (M5 stage A) | **A RECORD'S `rx_ns` IS NOT ALWAYS THE INSTANT IT DESCRIBES, AND THE LIVENESS CLOCK IS STAMPED FROM `TraceRecord::event_ns` INSTEAD.** `rx_ns` still orders the file, still bounds every span, and is still what every gap distribution and every pinned watchdog column is computed from — nothing about those moves, because at Anvil and Kraken the two quantities are the same number. `event_ns` is the new one: `rx_ns` for a WebSocket frame, `req.recv_ns` for a REST body, `ctl.recv_ns` for a control frame. Only the liveness clock reads it. | Binance's capture loop writes a side-channel record when the main loop next flushes, not when the thing arrived, because `rx_ns` is what orders the file and `TraceReader` rejects the first backwards step. The consequence is not cosmetic: **three pings twenty seconds apart share one `rx_ns` in `binance_atomeur_deepseed_20260824.ndjson`.** A clock stamped from `rx_ns` there reads three arrivals 0 ms apart, drives the self-calibrating median toward zero and then fires on every real interval — a threshold measuring the capture tool's flush schedule instead of the venue's cadence. Book and arrival figures deliberately stay on `rx_ns`: mixing the two in one distribution can run backwards, and the pinned columns must keep measuring what they measured when they were pinned. Covered by `test_trace_records.cpp` and mutation-verified. |
| 2026-08-25 (M5 stage A, qualifying the ping ruling above) | **THE ~80 s THRESHOLD THAT ROW ASSUMES IS NOT WHAT THE CODE PRODUCES, AND AT THIS VENUE THE SELF-CALIBRATION IS INERT.** `liveness_clock.hpp` clamps at `kThresholdCeilingMs = 30,000`, so `4 x 19,970 = 79,880 ms` caps to **30 s**. And `kMinSamples = 8` intervals is **~160 s of wall clock** at a 20 s cadence, which **no committed slice reaches** — the longest is 88 s with 5 pings — so all seven run on `kUncalibratedThresholdMs`, which is *also* 30,000 ms. Recorded, not fixed: the multiplier is C's and the ceiling belongs with it. | Nothing misbehaves today and no golden moves, precisely because the calibrated and uncalibrated values coincide here — which is the [coincidence class](#9-amendment-log) this log already names, arriving in a new place. What is inert is the mechanism the 2026-08-17 ruling rests on: at Anvil and Kraken the threshold is derived from the signal's own median within 4-8 s, and at Binance it is a constant wearing a calibration's clothes. The ceiling's stated purpose — stopping a sustained slowdown from disabling the detector — is a 30 s number sized against Anvil's 176,000 ms silence, and it was never sized against a venue whose *healthy* cadence is two thirds of it. **C must decide against measurement whether the ceiling rises, the multiplier falls, or the floor/ceiling become per-venue; changing the constant and the venue in one step leaves no way to attribute a regression, which is the same argument that deferred the multiplier.** Recorded here rather than only in `venue.hpp` because a ruling whose number the code does not produce is exactly the drift this log exists to catch. |
| 2026-08-25 (M5 stage B1, measured) | **A MISSPELLED BINANCE STREAM PRODUCES A FULLY DRAWN, COLOURED, PERMANENTLY FROZEN LADDER, AND THE MECHANISM IS THE EMISSION POINT RATHER THAN A MISSING DETECTOR.** Probed on the wire with a control run: `wss://data-stream.binance.vision/ws/btcusdt@depth@100mss` (one character wrong) returns **HTTP/1.1 101 Switching Protocols**, holds the socket, sends its server PING on the normal ~20 s cadence which the transport answers in **0.107 ms**, and delivers **zero depth frames**. The REST seed is a different host and **succeeds**. The correct-stream control took **121 frames** over the same 12 s. **The capture was then replayed through the real adapter and the real book, and the result is not a subtle one:** `adapter events=1`, `seed bracket ok=0`, `snapshots_adopted=1`, `0 stale episodes`, and a populated 100-level ladder rendered **● LIVE**. | **The freeze is `adopt_seed()`.** It emits the `Snapshot` at seed time, from the REST body, **before any WebSocket event has arrived** — so `Book::adopt` sets `FeedStatus::Live` off the seed alone, while `bracket_checked_` stays false for ever because no diff ever comes to satisfy it. Nothing is missing and nothing failed: every counter reads healthy, because the adapter did exactly what it was told and the thing it was told is a lie. That is why this is not closed by adding a detector — it is closed, if it is closed, by moving *when* the book is allowed to become Live. Kraken's equivalent failure is LOUD (`success:false` → `refused()` → `die()`); Binance names its subscription in the URL path, so there is no subscribe, no ack, no error and no close, and **nothing whatever confirms that the subscription succeeded**. It cannot be closed by arming on book silence: that is the ruling the MINA/GBP 25,843 ms hole retired, and this venue goes legitimately quiet for 10.5 s. **Owner: C.** Four candidate remedies are recorded in DESIGN strain 26 and **none is built at B1**, because choosing between them is a rendered-state decision and inventing a rule here needs a calibration nobody has yet. |
| 2026-08-25 (M5 stage B1) | **THE PING IS A TRANSPORT LIVENESS SIGNAL THAT THE 2026-08-25 RULING ADOPTED AS A FEED LIVENESS SIGNAL. DECISION 1 STANDS; ITS PREMISE NOW HAS A STATED HOLE.** At Anvil and at Kraken the same subsystem emits the liveness record and the book — `summary` and `heartbeat` come from the application that is also publishing the ladder — so *the feed is alive* and *the socket is alive* coincide, and the ruling's language did not have to distinguish them. **At Binance the ping is emitted by the WebSocket layer, BELOW the subscription.** It proves the socket and it never proves the feed. **AND THAT DISABLES BOTH OF THE PANEL'S HONESTY MECHANISMS AT THIS VENUE, NOT ONE.** The grey threshold is the known half. The other is `age_ms`, which is a sliding-window deficit measured against liveness arrivals: at Anvil and Kraken those arrive from the subsystem that also emits the book, so a growing gap between them IS a backlog and the number means what §5 says it means. At Binance the liveness arrival is a ping on a fixed ~20 s timer inside the WebSocket layer — metronomic, **worst/median 1.01x**, the tightest cadence of the three venues, and structurally indifferent to whatever the book's queue is doing. **A signal that cannot itself fall behind cannot show a deficit, so `age_ms` is expected to read approximately zero through an arbitrarily large backlog.** **STATED AS A HYPOTHESIS AND NOT AS A FINDING:** it is derived from where the ping is emitted and from its measured cadence, and nothing has yet driven a Binance replay with a backlog in it to see. **B2 section 5 measures it through a throttled replay and either confirms or falsifies it** — and this row moves either way, which is why it is written down before the measurement rather than after. | This is a correction to the premise rather than to the conclusion: greying on socket death alone was priced at B3 and lost by 141 s and 291 s, so arming on the ping is still the right call and the row above it stands unchanged. What changes is what a green liveness clock is now entitled to mean. **A misspelled stream is merely the cheapest way to stage the gap** — it is a configuration error and therefore looks like our fault — but *a server-side subscription drop on a socket that stays up presents identically*, and no amount of care with stream names prevents that one. The venue publishes no subscription-state signal of any kind on the market-data streams, so the hole is not closable by reading the wire more carefully; it is closable only by requiring evidence that the FEED, not the socket, has spoken. **WHY THE WIDER FORM IS IN THE CONSTITUTION RATHER THAN AT A CALL SITE.** M4's definition of done named BOTH mechanisms as what makes this object honest — it *greys within the calibrated liveness threshold*, and it *shows a book age that is a lag estimate rather than a time-since-anything* (ROADMAP's M4 line; `M4-stage-D-the-bench.md`). **At Binance, on current evidence, it may be neither** — and the two failures are not independent. **Three consequences, one cause:** the grey threshold has nothing to arm on, the age meter has nothing to measure against, and the lying socket of the row above is what happens when a build trusts the ping for both. The note left at `replay_driver.cpp`'s `age_.on_liveness` call is correct and is **the instance**; this row is **the class**, and a class recorded only as an instance is how the next venue rediscovers it from scratch — which this log has already watched happen with the coincidence class and with never-observed frame kinds. **Nothing is acted on here:** the estimator is untouched, the remedies stay C's with the threshold work, and the measurement is B2's. **Owner: C** for the remedy, **B2** for the evidence, and it is the same decision as the row above rather than a second one. |
| 2026-08-26 (M5 stage B2) | **A GUARD TRACE AND A RULING'S EVIDENCE ARE TWO CATEGORIES OF COMMITTED FILE, AND THEY HAVE OPPOSITE SLICING RULES.** A **guard trace** pins a BEHAVIOUR: cut it to the smallest window that exercises the property, which is what M4 stage 0's capture recipe asks for and what every committed Binance and Kraken slice is. A **ruling's evidence** pins a DECISION, and **cannot be cut at all**, because the figure it carries is a RATE OVER A COMPLETE CAPTURE. `binance_btcusdt_mixed{1,2}_20260825` are the first files in this repository of the second kind, and they are committed whole (1.9 MB and 1.4 MB raw; **246,677 B and 178,652 B gzipped**, so ~425 KB of pack). Named as such in their taxonomy rows, so a later stage wanting a mixed-cadence *guard* trace cuts a new one from `_local/` rather than re-cutting these. | **Because the alternative is settling the debt at a discount and rewriting the invoice.** The clause being discharged said the ruling's figures were *the one set of numbers in this table with no committed trace behind them*. Commit 40 s slices instead and the numbers that come back are 40/40 and 38/38 — so the row would have had to be **amended to fit the artefact** rather than the artefact supplied to satisfy the row, and the original assertion would *still* have no committed trace. The row's own wording forbade it: struck **with the commit SHA in its place**, not struck-and-amended. The size argument was also in the wrong currency — the choice was worth ~250 KB of pack against a `.git` of 18 MB that already carries `anvil_101_baseline_20260809` at 10.4 MB raw for 194 KB packed — and the ≤900 KB "convention" was not one: both ATOMEUR files were already byte-identical to their `_local` originals, which was checked rather than assumed. **And slicing had a failure mode this route does not:** B2's own known unknown was whether the coincidence survives a cut, and `slice_trace.py`'s guard exists because a window cut *at* a baseline record took the oracle from 884/884 green to 250/250 red. A whole capture is by construction the artefact the figures were computed from, so the question does not arise. |
| 2026-08-26 (M5 stage B2) | **THE RE-SNAPSHOT MARGIN IS SIZED AGAINST A FETCH DEADLINE THE TRANSPORT IMPOSES, NOT AGAINST A MEASURED PERCENTILE — AND THE DEADLINE IS A REQUIRED PROPERTY OF THE TRANSPORT.** `margin >= walk rate x T`, with **T = `kBinanceFetchDeadlineMs` = 15,000 ms**. A fetch that exceeds T is abandoned and retried. The trigger itself is an OBSERVABLE and not a timer: re-seed when the **seeded coverage** — held levels still inside the price range the seed described — falls below `kBinanceEmitDepth + 192 = 448` on either side. Worst coverage lost over any window of 15 s or less, across all nine captures, is **168 levels**; 192 is that plus 14%. Two further properties are REQUIRED OF THE TRANSPORT and cannot be enforced in `engine/`: T itself, and a bound on re-seed RATE — the adapter has no clock, latches once per seed epoch, and the venue bans on IP-weight breach at 50 per `limit=1000` fetch. Implementing both is C/D's; **the board's re-seed behaviour is D's**. | **A deadline covers 100% of fetches by construction; a p99 covers 99% of a sample of 36.** And the sample says the percentile is the wrong instrument anyway: `limit=100` is n=23, median 1,009.4 ms, max/median **1.053**; `limit=1000` is n=13, median 1,503.1 ms, max/median **1.058**. Choosing max over median moves the margin by 87.6 ms = $0.029 against a $224.52 seeded window, 0.013% of it — **the round trip has no tail worth sizing against on this path**. **THE DECISIVE ARGUMENT IS PROVENANCE, NOT SPREAD:** all 36 are desk-box figures — wired ethernet, CPython, urllib — and the board is an ESP32-S3 on Wi-Fi doing TLS, where M4 stage B3 measured DNS failures at a flat **14,000 ms**. A desk figure must never stand as the board's, which is the same trap the 439 ms record-lag figure set one paragraph away in the same notes. A deadline also converts an unbounded unbracketed window into a bounded one, which is where invariant #5 draws its line and the same move B1's remedies (a) and (b) make. 15 s is not a fresh guess — `capture_binance.py` already runs this fetch on a 15 s cadence behind `REST_DRAIN_TIMEOUT_S = 20` — and it survives the sanity check: at $0.33/s, `limit=1000`'s $224.52 window is ~677 s of walking and 15 s is **2.2%** of it, while `limit=100`'s $15.63 is ~47 s and 15 s is **32%**. That is an independent argument for `limit=1000` arriving from the schedule rather than from the depth sweep. |
| 2026-08-26 (M5 stage B2, measured) | **AN INSTRUMENT WHOSE COMMENT NAMES THE RIGHT QUANTITY AND WHOSE CODE COUNTS A DIFFERENT ONE READS PERFECT HEALTH THROUGH THE FAILURE IT WAS BUILT FOR.** B1's `min_bid_levels` / `min_ask_levels` were described as showing the seeded-window edge. They count levels **held**, which only ever grows — every diff that retires a level near the touch arrives alongside others adding prices the seed never contained — so on the two committed captures where the 82.4% failure class actually happens they read a flat, healthy **100/100** while seeded COVERAGE goes to **0**. On `binance_btcusdt_reconnect_20260824` the bid side collapses 100 → 0 in **1.2 s**, which is faster than a single REST round trip. Both counters are kept and printed one line apart. | **The general form, and it is not the same as a stale comment.** A stale comment describes something that stopped being true; this described something that was never true, in a codebase where the prose is load-bearing and is what the next stage builds on — B2's brief inherited the sentence and instructed *make it the trigger*, which would have shipped a trigger that cannot fire. What caught it was measuring the instrument against the outcome it claims to predict (the oracle's own RED slices) rather than reading it. **A counter is only an instrument if something has checked that it moves when the thing it names moves** — which is `binance_oracle.py`'s NOT EXERCISABLE rule (2026-08-24) arriving one layer up, at a counter instead of at a mutant. The retired counter is not deleted: deleting one whose reading turned out to mean something else is how the next reader repeats the mistake. |
| 2026-08-26 (M5 stage B2, measured — answers the 2026-08-25 B1 row) | **`age_ms` AT BINANCE: THE HYPOTHESIS IS CONFIRMED FOR A FEED BACKLOG AND FALSE FOR A SOCKET BACKLOG, AND THE DISTINCTION IS THE USEFUL PART. SEPARATELY, THE METER HAS NO READING AT ALL FOR THE FIRST ~11 MINUTES OF EVERY CONNECTION.** Measured by `dc_age_probe`, which injects a backlog it knows exactly and reports what the real `AgeEstimator` makes of it, at 25% drain after the baseline has latched on clean stream. **SOCKET BACKLOG** (this client or its path cannot drain the wire): the ping is a control frame on the same TCP stream, so it is delayed identically and the meter **TRACKS** — 1,797.1 s read against 1,796.8 s injected, 1.00x. Physical at all three venues and correct at all three (Anvil 0.99x and Kraken 1.00x of their window ceilings, which independently reproduce `age_estimator.hpp`'s own worked table of 384 s and 768 s). **FEED BACKLOG** (the venue's publisher falls behind, or the subscription stops, while the WebSocket layer keeps its 20 s timer): **BLIND** — 0.3 s peak through the same 1,797 s. **Physical at Binance and NOT CONSTRUCTIBLE at Anvil or Kraken**, where one subsystem emits both. Pinned in both directions at all three venues by `binance_age_probe`; mutation-verified. **AND THE SEPARATE FINDING:** `kBaselineSamples` = 32 intervals is 16 s at Anvil and 32 s at Kraken; at Binance's 19,964 ms ping cadence it is **639 s after the first ping, ~11 minutes after connect**, during which the meter reads `-`. | **This narrows the B1 row rather than overturning it, and the narrowing is what C can act on.** That row said a signal which cannot itself fall behind cannot show a deficit, and predicted ~zero through an arbitrarily large backlog. Right about the mechanism, wider than the truth: the ping *can* fall behind, whenever the thing it shares a TCP stream with does. So the meter is not broken at this venue — it answers a narrower question than the panel's label implies, and the question it stops answering is exactly the one the transport-versus-feed row is about. **Three consequences from one cause is now four:** the grey threshold has nothing to arm on, the age meter is blind to a feed-only backlog, the lying socket of the B1 row, and the age meter additionally has no reading for eleven minutes — and the fourth is independent of the ping's emission point, which is why it had gone unnamed. The eleven minutes is the same shape as the 20x grey latency regression the 2026-08-25 ruling accepted deliberately, arriving in the mechanism nobody costed. **Nothing is acted on here:** the estimator is untouched, no constant moved, and what the panel renders in any of these states stays **C's** — including whether the header should show *no reading* rather than a number it cannot support, which is the shape M4 stage A2 already used for a reconnect. |
| 2026-08-26 (M5 stage B2, found at review) | **THE MEDIAN CONVENTION HAS TWO HOMES, NOT ONE, AND EVERY BINANCE CADENCE FIGURE THIS PROJECT HAS QUOTED CAME FROM THE WRONG ONE. RECORDED, NOT FIXED.** `sample_window.hpp` states the rule and says it "matters enough to have one home": a **lower median by nearest rank**, because an interpolated median invents an interval that never occurred on the wire. `LivenessClock` and `AgeEstimator` use it. **`harness/src/trace.cpp`'s statistics pass carries a second copy and interpolates** — `median_liveness_gap_ms` and `median_gap_ms` both. Measured over every committed trace: the two agree to 0.1 ms at Anvil (500.1/500.1) and Kraken (499.9/499.9), and disagree on **every Binance capture with an even interval count** — 19,947.7 vs 19,951.7, 19,962.8 vs 20,011.6, 19,973.9 vs 20,013.3, 19,950.6 vs 20,004.8, 19,964.0 vs 19,969.4. Those right-hand figures are the ones in `taxonomy_pins.inc`'s Binance comment and in B1's session log, so **the cadence this project has quoted for this venue is not the cadence the clock on the board computes.** Owner: whichever stage next touches `trace.cpp`'s statistics pass; if it ships without fixing this, the clause moves rather than lapsing. Pinned meanwhile by `test_binance_adapter.cpp`'s two-conventions case, which **must be inverted rather than deleted or relaxed** when the fix lands. | **It surfaced because B2's own new code got it wrong first.** `dc_age_probe` was written with its own interpolated median, quoted 19,969.4 ms as the venue's cadence into five documents and a test — and the test passed, because a relative epsilon of 0.001 on 19,969.4 spans 19,964.0 as well. **A tolerance wide enough to accept both candidate answers is a test that cannot fail**, which is the 2026-08-16 clause arriving as a tolerance instead of as an oracle. The probe now calls `lower_median` and the assertion is tight enough to name which instrument produced the number. **Why the older copy is not fixed tonight:** `median_gap_ms` shares that code path and its figures are quoted across three NOTES files and a dozen briefs, so adopting the shared convention is a documentation sweep with its own blast radius — and doing it at the end of an evening, in a stage about the re-snapshot schedule, is how a correction becomes a second silent drift. The divergence is ~5 ms at worst on this corpus and changes no decision; what it costs is the property `sample_window.hpp` claims outright, that a C++ figure and a Python figure over the same trace are comparable. **That claim is false on the report path today**, and saying so is the point of the row. |
