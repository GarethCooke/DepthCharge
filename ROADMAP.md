# DepthCharge — Roadmap

Semi-stable. Update the **Status** column as milestones complete; anything structural
belongs in `ARCHITECTURE.md` §9 instead. Sessions: your milestone's brief in
`docs/briefs/` overrides the one-line summary here.

**Standing priority note:** Anvil (In Progress) and FrontierView interview prep rank ahead
of DepthCharge in the owner's queue. DepthCharge sessions are opportunistic; keep
milestones evening-sized.

## Tracks

- **[A]gentic** — Opus / Claude Code sessions, converging on the harness's red/green.
- **[B]ench** — owner at the bench (soldering iron, KiCad, printer); Claude assists in
  review mode only.

M1 and M2 share no dependencies: software sessions and bench time run in parallel.

## Milestones

| #  | Track | Milestone                     | Goal / definition of done                                                                                          | Depends on | Status |
| -- | ----- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ------ |
| M0 | A     | Trace + harness               | Live Anvil WS traces captured & vendored; replay harness parses them; golden-test + CMake skeleton; ctest green.    | —          | ✅ Done (2026-07-23), in-tree green — brief: `docs/briefs/M0-trace-and-harness.md` |
| M1 | A     | Console ladder off replay     | `FeedEvent` types real; Anvil adapter (frames→events); phase-1 book (adopt snapshot + trade ring); console ladder renders a replay; goldens green. | M0 | ✅ Done (2026-07-26), in-tree green — brief: `docs/briefs/M1-console-ladder-off-replay.md` |
| M2 | B     | Panel smoke test              | ESP32-S3 DevKit + 64×64 HUB75 + PSU wired; HUB75 DMA library demo runs; photo in `hardware/`.                        | —          | ✅ Done (2026-07-26), in-tree green — brief: `docs/briefs/M2-bench-bringup.md` (bench; owner-driven). Agentic sessions have no blocking software work until M3 — MP stage 1 is available in parallel. |
| M3 | A+B   | Live Anvil on the panel       | Firmware net task (TLS WS, nominated `Origin` header) + render task; engine unchanged from M1; live ladder on the panel; pull-the-Wi-Fi test shows grey stale state then clean resync. | M1, M2 | ✅ **Done (2026-08-16)** — the pull-the-Wi-Fi acceptance passed on its second run: panel greys with no hue, station rejoins **unaided** (no reset in the log), deauth → LIVE in **18.7 s**, one rejoin call. Record: [`hardware/bench-2026-08-16-pull-the-wifi-acceptance.md`](hardware/bench-2026-08-16-pull-the-wifi-acceptance.md). The first run **failed** and found the rejoin livelock (ARCHITECTURE §9 2026-08-16 pm, DESIGN strain 20) — that failure is the best argument this project has produced for keeping a bench criterion in a DoD. Stages A ✅ (2026-08-07, wait-free `SnapshotChannel`), B ✅ (2026-08-08, streaming allocation-free parser, goldens unchanged), C ✅ (2026-08-09, feed proven on the board), **D ✅ (flashed and long since proven — the 23.6 h soak of 2026-08-15/16 ran the panel continuously; the "not flashed" note this cell carried until 2026-08-16 was stale by a week)**. The transport rewrite is **done and accepted** (owned WS client over esp-tls; 10.9 h on one connection, zero errno-silent deaths, zero framing rejects — brief closed 2026-08-16), the TCP window is rebuilt and is now the default build, and the supervisor recycles a silent socket after five minutes. Two items remain owed against M3's *brief* and neither gates this tick: the weak-node 1 h soak (transport brief DoD, never run on the final build), and the ghosting re-check with `clkphase = false` (`hardware/BRINGUP.md`). |
| M4 | A     | Kraken adapter                | Delta application + CRC32 verification; dense-window book lands here; Kraken traces + goldens; panel switches venue. | M3 | ✅ **Done (2026-08-24)** — closed at the bench sitting, brief: [`docs/briefs/M4-closing-bench-sitting.md`](docs/briefs/M4-closing-bench-sitting.md). The panel renders a Kraken book off the wire, greys within the calibrated liveness threshold, holds colour through 26 s of legitimate book silence, heals from a checksum failure rather than greying permanently, and shows a book age that is a lag estimate. **The last two bars were the two rendering decisions stage C deliberately did not resolve, and both were taken by eye because nothing a host test asserts could take them.** *Item 1 — uninitialised rendering is **grey**, not 27 rows of unknown: grey already means “do not trust this” and the boot frame carries a stale reason, so unknown rows add nothing read at desk distance. It also exposed the ramp failing a SECOND time at six-bit depth — the stale reason was legible only at an oblique angle — fixed by giving the header its own black bed (`Ink::HeaderBed`, a named and compile-checked exception to “grey, never blank”) and then dropping the wash to 8. Item 2 — **depth stays 25**: 25 red rows above the midpoint and 25 green below, counted on the panel, which is the whole subscribed book against `kLevels = 26`, so the window is a no-op at the shipped configuration, the three policies stay byte-identical and D6 does not move.* **What the evening actually cost, recorded because it is the reusable part:** the panel was drawing the ladder displaced upward and it was a loose **E address line**, not firmware — `hardware/BRINGUP.md` now carries the signature (a dead address bit of weight *w* gives two dark bands of *w* rows and superimposes logical *j* with *j+w*; the band width names the bit) and records that `clkphase` and `cfg.driver` cannot reach the address bus, which is where two reverted experiments went. Owed but NOT blocking this tick and carried forward: D7's scope trace (item 3, an M7 sequencing question), D1 and the weak-node D2 hour (M3 backlog). — and it inherited two things from M3. A venue's byte rate is a design input (DESIGN strain 19: the object already runs at 51–79% of Anvil's wire and Kraken's full-depth stream is larger), and its DoD should include at least one criterion the harness structurally cannot stage (strain 20). **Stage 0 ✅ done 2026-08-16** — brief: [`docs/briefs/M4-stage-0-price-the-kraken-wire.md`](docs/briefs/M4-stage-0-price-the-kraken-wire.md), observations: [`harness/replay/NOTES-kraken.md`](harness/replay/NOTES-kraken.md). **What it fixed, before an adapter existed to be wrong: (1) the premise that Kraken's stream is larger than Anvil's — it is a third the size at *four times* the rendered depth** (depth 25 = 6.12 KiB/s, depth 100 = 9.91, against Anvil's 30.8 at depth 27; 5.0× and 3.1× headroom), so strain 19's sizing worry does not transfer to this venue and the depth choice is a design decision, not a transport one; **(2) `depth: 27` is REFUSED, not rounded up** the way A7's tier ladder rounds — and the refusal leaves a live, heartbeating socket with a permanently empty book unless the ack's `success` is checked; **(3) `kRxWatchdogMs = 1000` is wrong here by 4.5×** — worst *book* silence is 4,535 ms on BTC/USD and 9,007 ms on a quiet pair, while a 1 Hz heartbeat floors *byte* silence at 998–1,026 ms, so the two counting rules that agree at Anvil disagree 9× here; **(4) invariant #3 now has a number** — 8,677/8,677 checksums reproduce from the verbatim text and **0/2,786** from float-parsed text; **(5) the client-side truncation rule is load-bearing and hides at depth** (checksum matches 379/1,412 at depth 10, 4,435/4,435 at depth 100 — untestable in a short trace at the depth most likely to be developed against). Four 60 s slices committed (121 KiB gzipped); `dc_replay` deliberately cannot read them yet. **Both decisions are now TAKEN** (stage-0 brief session log; ARCHITECTURE §9 rows dated 2026-08-16 stage 0): **depth 25 with `kDisplayLevels` staying 27** — the two unfilled rows a side are §4's "depth beyond N is unknown, not zero" *rendered*, so a ladder differing in height by venue is correct rather than a wart to reconcile at M7 — and **strain 3 resolves as `static_assert`s, not a `concept`**, because host and target would compile one spelling under two dialects, which type-checks and is therefore worse than convention. Stage 0's own tooling is now pinned too: `ctest` runs `kraken_tool_selfcheck` (mutation-verified, add-rows-only pin table with a `--pin` mode that refuses to overwrite one). **The d10 and d25 BTC/USD slices are the truncation goldens, and which traces qualify is not predictable from depth** — d25 catches on BTC/USD and misses on the quiet pair; depth 100 evicts levels *hardest* of the three and detects nothing. Two plausible heuristics were fitted and falsified, so the criterion is the direct measurement (`ok_never_truncating < checksummed`), and M5 inherits it as a capture recipe: liquid pair, shallowest offered depth, **no reconnect in the committed window** — a resync heals a non-truncating book and destroys the trace's value as a guard. **M4 now runs as four evening-sized stages** (ARCHITECTURE §9, 2026-08-17): **A** the replay dialect · **B** the adapter · **C** the dense-window book in `engine/` · **D** the bench. **Stage A ✅ done 2026-08-17** — brief: [`docs/briefs/M4-stage-A-the-replay-dialect.md`](docs/briefs/M4-stage-A-the-replay-dialect.md). **It precedes the adapter because invariant #6 says so:** no merge without replay coverage, and `dc_replay` could not read a Kraken capture at all — it rejected the metadata header on the required `ticker` and would have rejected 61 of the depth-25 slice's 1,599 records for carrying no string `type`. An adapter written before the reader is an adapter that cannot be covered, and the pressure at that point is to reshape the committed traces to suit the reader. **The reader learns the wire; the wire does not learn the reader.** Stage A ships: the `venue` metadata tag (additive, absent = `anvil`, both writers emitting it); one reader dispatching to per-venue decoders with the sink shape pinned by `static_assert`s (strain 3, option (ii), first use); Kraken's decoder as a **classifier that emits no `FeedEvent`s**; the record taxonomy measured and pinned by `trace_taxonomy_selfcheck`; and the replay driver's disconnect threshold made **venue-declared** — Anvil 1,000 ms, Kraken 15,000 ms — so the quiet pair's legitimate 9,007 ms silences stop reading as disconnects. **Measured on the committed slices: the Anvil constant invents 25 disconnects in 60 s on the quiet pair by the record-arrival rule and 12 more by the book-event rule; at the declared 15,000 ms both are 0, on all four slices.** The four Anvil traces are byte-identical through the changed readers — 12 outputs across three programs, diffed. **Reviewed and accepted 2026-08-17; the review's four follow-ups are done and the tree is green at 21/21.** Three things are now written down as **stage B's inheritance** (stage-A session log, § *Owed by stage B*, and DESIGN strain 22): **(a)** the dispatch must prove it dispatched right — *a Kraken golden fails if it was produced by the Anvil parser*, with the decoder's identity in the pinned output and a mutation that swaps the dispatch expecting red; **(b)** a **deliberately captured Kraken resync slice** is owed, and it cannot be one of the four truncation traces, because a resync heals a non-truncating book and the two capture purposes are mutually exclusive; **(c)** the venue table's duplication of `kRxWatchdogMs` closes when stage B lifts it into the shared firmware table. The review also confirmed the resync rule's Anvil agreement is **not** agreement-by-absence — the reconnect trace scores 1 under both the old and the new rule, while all four Kraken slices score 0 under the new rule and **1 under the old one**, which is the bug stage A fixed, quantified. **RULING 2026-08-17, and it supersedes stage A's own decision 2 in part (ARCHITECTURE §9): staleness stops counting book events and counts the venue's declared LIVENESS SIGNAL instead** — Kraken's 1 Hz heartbeat, Anvil's 2 Hz `summary`, which the read-and-report confirms is a fixed engine-thread deadline firing on an empty queue (62 byte-identical idle frames with `seq` advancing, nominal 500 ms, worst 547). The two-clock split stands; what the staleness clock counts does not. **No threshold on book silence can be correct** — a quiet market and a dead subscription are identical on the wire, and MINA/GBP's healthy 25,843 ms proves the bound is a market property. Book-event silence becomes `age_ms` (stage A2), rendered as a number and never as grey; the threshold becomes **self-calibrating** from the observed median of the liveness signal, because both venues' intervals are values DepthCharge cannot read back. **`docs/vendor/anvil-protocol.md` re-pinned at `4801ed8` (2026-08-17), closing §9 item 8** — Anvil corrected the keepalive clause that denied idle emission, and §3.5 gained a *guarantee* that emission is timer-driven with the interval explicitly excluded from it: *"A client that needs a staleness threshold must derive it from the cadence it observes on the connection it is using, not from a number read out of this document."* The self-calibrating threshold was written the day before that sentence existed and satisfies it unchanged — **not independent convergence, since both descend from the same instruction to Anvil (*promise the mechanism, not the interval, and refuse the number*), but evidence that the ruling survived a second implementer who could have found it awkward.** **Stage A is complete and COMMITTED** (`5745b52`…`93c6957`, the ten-commit split from the stage-A ruling brief, executed). **The twelve items stage A left owed are now triaged** — brief: [`docs/briefs/M4-triage-of-the-twelve.md`](docs/briefs/M4-triage-of-the-twelve.md) — and the sort moved five of them out of stage B, so **the rest of M4 runs as A2 · B1 · B2 · C · D** (ARCHITECTURE §9, 2026-08-17 M4 triage). Three questions decided there and recorded rather than left to look like oversights: **the client ping is deferred to M6** (transport work; the windowed deficit is enough for a numeric header), **the uninitialised-book question splits** (the engine state is C, the rendering is D, because the two candidate renderings differ in nothing a host test can assert), and **the defensive resubscribe is not built** (its trigger would be book silence, which the ruling established carries no information — it would fire every ~26 s on a healthy MINA/GBP). **Stage A2 ✅ done 2026-08-17** — brief: [`docs/briefs/M4-stage-A2-the-age-meter.md`](docs/briefs/M4-stage-A2-the-age-meter.md). §5 gains `age_ms` + `has_age`, both landing in padding the hand-off already had (`sizeof(DisplaySnapshot)` unmoved at 1,168 B and now pinned by `static_assert`, because three documents quote a byte count derived from it); the estimator is a **sliding-window supremum** — Lindley's backlog recursion over 256 liveness arrivals — and it is measured against a **baseline latched once per connection**, not against the rolling median the grey threshold uses, because sharing that median makes the meter read **0.0 s through a 111 s backlog**. The header renders to minutes and hours (`2m56s`, `1193h02m`), and nothing branches on the number. **The four healthy Anvil traces read a 0.5–0.9 s noise floor with zero grey episodes**, the reconnect trace peaks at 2.3 s and then reports *no reading* until the new connection has re-measured the venue's clock. **The blind spot is pinned as a test rather than described:** a socket already behind when it latched cannot detect it — on `_local/drain-120ms.ndjson` the meter reads **12.5 s against a true ~122 s** — and that case is uncoverable by any capture, because a backlog belongs to one client's socket and a trace records only when that client got the bytes. It is what the deferred client ping exists to close. **Reviewed before commit, and the review paid for itself twice:** the first implementation latched its baseline from the threshold clock's rolling median, which is deliberately *not* reset at a disconnect — so a reconnect away from a backlogged socket would have handed the fresh healthy connection the throttled cadence and reported no lag for ever; and fixing that exposed the resumption burst (`253 871 174 475 …` ms against a 500 ms broadcast), which an 8-interval latch reads as 478 ms and a 32-interval one as 499.3. Green at 24/24, **committed as six** (`52af6e0`…`91c7a8d`), each verified green in a worktree of its own. **Stage B1 ✅ done 2026-08-18** — brief: [`docs/briefs/M4-stage-B1-the-adapter.md`](docs/briefs/M4-stage-B1-the-adapter.md). The second adapter links: `engine/kraken/` decodes the wire with **one** parser (allocation-free, target-bound — Anvil's reference/streaming pair exists only because M1 shipped an allocating reference M3 had to prove a rewrite equivalent to, and Kraken has no such reference to invent an obligation from), the venue's book is held and **truncated in the adapter** because the CRC32 covers the top 10 of the *truncated* book and whoever truncates is whoever can verify, evictions leave as ordinary `Delta{qty=0}` so **no new `FeedEvent` variant and no new `GapReason`** (third asking, third no), and the phase-1 book learned `apply(Delta)` as an ordered insert/amend/erase over the array a Snapshot already fills — **explicitly not the dense window, which stays C**. `run_replay`'s venue guard is gone and the two defects it was hiding are fixed in the same sitting, as the triage said they must be: `symbol_for()` returned Anvil's scale for every venue, and `console_ladder` printed a hardcoded `" ANVIL "` and a raw step count for every quantity. **`SymbolSpec::qty_step` becomes `qty_decimals`** — a semantic replacement, not a rename: the old field divided and could only express a step *coarser* than the wire unit, and Kraken's `1e-08` is **not representable in it at all**, so every scaling path in the tree had been correct only because the value had always been 1. **Three measurements taken before the adapter was written, each because it decided its shape: (1)** every price and qty in all five slices carries **exactly** its declared precision (8,172 level entries, zero exponent notation on the book channel); **(2)** therefore **Kraken's CRC32 reproduces from integer ticks alone — 4,878/4,878** on the four slices with an opening snapshot, with no wire text retained anywhere, which is what makes B2 add a *check* rather than a parse and why `KrakenFrame` has no text buffer (the fifth reads 0/49 correctly: it starts mid-stream, and so can never be a CRC golden); **(3)** the adapter's eviction counts reproduce stage 0's independent Python figures exactly — **275 / 585 / 1,156 / 19**, two languages, no shared parent, which is the independence test the 2026-08-17 close-out row demands before calling agreement corroboration. **Absence of a subscribe is not failure of a subscribe** (owed item 10): the state machine starts at `Unknown` and accepts book messages there, or the extreme slice — the only artefact carrying the 25,843 ms healthy silence the staleness ruling rests on — would emit nothing; a *refused* subscribe is the opposite and terminal. Separately, **updates before the first snapshot are counted and dropped**, because replaying that slice's 49 onto an empty ladder reproduces 0 of 49 checksums — a different book that merely looks plausible. **The review found two defects after the suite was green, and both are the coincidence class** (ARCHITECTURE §9, 2026-08-18, from the review): `adopt_snapshot` truncated nothing and all five slices passed because Kraken serves exactly the depth requested so our cap coincided with its cap on every file; and a use-after-move that passed direct construction and failed only through `run_replay`, the one path that moves its decoder. Both fixed with synthetic coverage, because **a captured trace cannot reach either case**. Also extracted at review: `engine/ladder.hpp`, one definition of *better* plus the three array operations, shared by the adapter's ladder and the book's — a mirror that sorts differently from its original draws a book the adapter never held, and both had been written with their own private copy that agreed. **The dispatch mutation was RUN, not written:** swapping `case Venue::Kraken` to construct `Replay<AnvilTraceDecoder>` went red, and `decoder == "kraken"` was the *first* assertion to fail on every slice, before the counts. Green at **29/29 ctest, 326 doctest cases**, from a scratch build directory; `dc_engine_target_check` compiles every new engine file with **xtensa GCC 8.4** under `-Werror -fno-exceptions -fno-rtti`, and the firmware links unchanged. **Part B (the firmware lift) is NOT deferred to a B1b evening — it moves to D's first act** (2026-08-18 approval): rewiring the RX watchdog onto the calibrated threshold changes **when the panel greys**, the behaviour it replaces was established over M3's 23.6-hour soak, and the desk can compile it but cannot show it correct. A *partial* lift is worse than none — clocks in `engine/` while firmware still includes `staleness.hpp` is a third copy of the thing deletion exists to remove. D is already the bench stage and its soak is the same soak. **So M4's remaining evenings drop from five to four: B2 · C · D (item 8 first, then the bench).** **Stage B2 ✅ done 2026-08-18** — brief: [`docs/briefs/M4-stage-B2-the-healing-path.md`](docs/briefs/M4-stage-B2-the-healing-path.md). **The healing path, and all four sections including the one the brief cut-pointed.** The venue's CRC32 is now COMPARED rather than carried — `engine/kraken/kraken_checksum.hpp`, incremental, `constexpr`, allocation-free, xtensa-compiled — and it reproduces B1's figure from the shipping adapter: **4,878 / 4,878 on the four gradeable slices, from integer ticks with no wire text retained**, while the mid-stream slice reads **49 unverifiable, not 49 failed**, which is the distinction that keeps `NOT_A_CHECKSUM_GOLDEN` honest. **The mismatch path needed no new vocabulary — the fourth asking and the fourth no**: `Gap{ChecksumFail}` has been in §4 since M0, the book is dropped, and the one new thing is a latch (`resync_wanted()`) the transport reads and clears, because Kraken never re-snapshots unasked and a healthy socket over a corrupt book would otherwise stay grey for ever. **What the check CANNOT see is now stated rather than inferrable**: it validates the top 10 levels a side — 20 of the 50 the ladder holds — and the discriminating measurement is synthetic by necessity, since a book edited below level 10 is an input no capture contains. **The resync slice is captured, committed and pinned** (`kraken_minagbp_d25_resync_20260818.ndjson`, 99 records, one connection, one deliberate unsubscribe/re-subscribe), and it answered the stage's open question in the strong form: **a mid-stream snapshot is byte-shape IDENTICAL to the on-connect one**, so position is the only signal there is and the venue-free predicate is not merely sufficient but the only thing that could work. **Taking that capture found two defects nothing else could**: the parser filed every `method` frame as a subscribe ack — one refused *unsubscribe* away from latching `refused()` and `die()`ing the board — and the new capture mode wrote its `tx` record out of rx_ns order, rejected by `TraceReader` on replay. Both are a third blind-spot class beside the missing golden and the missing synthetic case: **a corpus that has never contained a frame kind**. **And section 4 ran rather than being deferred: KRAKEN QUEUES** — 331/331 checksums clean under a throttled socket while the venue's own timestamp showed lag growing +0.690 s/s to 26.0 s, and the backlog then drained at 3.3× the baseline rate on release. So the deficit is a lag, **`age_ms` stands at this venue**, and the assumption B1 pinned with B2 named as its owner is discharged by measurement rather than carried to M5. Five mutations run and red, including `kChecksumLevels` widened 10 → 25, which proves the corpus discriminates the *venue's* rule and not just our implementation of one. `slice_trace --mode reconnect` now enforces containment AND self-containment or refuses with the number to use, and gained the first test it has ever had. Green at **32/32 ctest, 335 doctest cases**. **Awaiting split approval; nothing committed.** **M4's remaining evenings: C · D.** **Stage C ✅ done 2026-08-19** — brief: [`docs/briefs/M4-stage-C-the-dense-window-book.md`](docs/briefs/M4-stage-C-the-dense-window-book.md), measurements: [`harness/replay/NOTES.md`](harness/replay/NOTES.md) stage C addendum. **The render window, and a measurement that reshaped the stage before a line of it was written.** The brief proposed three row policies of which two were PRICE axes; measured over every committed trace, **only one adjacent level pair in five is a single tick from its neighbour** (19.5% on the densest book here, 10.0% on the sparsest) and one side of 25 levels spans **182 ticks on BTC/USD and 6,613 on MINA/GBP** — so a 27-row panel used as a 27-tick axis renders about four of twenty-five levels on the liquid pair and **one, the touch,** on the quiet one. The axis is therefore RANK: `top` (best 27), `largest` (touch plus the biggest behind it) and `thinned` (best 13, tail sampled at a stride the book supplies), all behind one interface, compile-time selected on the board and runtime-selectable in the harness so a golden per policy is a test rather than a rebuild. **A price-axis window is blocked rather than refuted**, on a per-row present/absent marker `DisplaySnapshot` cannot express — two `uint32_t` masks would carry it for 8 bytes on the struct and 24 in the mailbox, raised with its cost rather than taken. **The headline is that at the shipped depth the choice is not a choice**: 25 levels into 27 rows drops nothing, so all three policies are byte-identical on every committed Kraken slice, and the question D actually inherits is whether to subscribe deeper than the panel can draw. B2 supplies the other half of that trade — at depth 100 `top` and `thinned` render 37.0% of rows within the venue's checksum and `largest` renders **11.9%**, while Anvil's number is **0% because that protocol has no checksum at all** (DESIGN strain 24). *Uninitialised* is now an explicit engine state published in one byte of existing padding — `sizeof(DisplaySnapshot)` verified unmoved at 1,168 on **both** host and xtensa — because the cheap inference `!live() && !has_book()` is a lie for a book that adopted an empty snapshot and then gapped. Every §1 invariant is property-tested across all three policies over generated books (the generator is sparse by default, because a contiguous one would test a book this project has never seen), row counts pinned per policy with two derivable identities beside them, and **three mutations run red**. Also recorded: **§5's tick-indexed dense-window STORAGE is still unbuilt** — it is a different thing from the render window that shares its name, and it would optimise ~1,000 16-byte shifts a second. Green at **32/32 ctest, 343 doctest cases, 960,852 assertions**. **Awaiting split approval; nothing committed.** **M4's remaining evening: D.** **Stage D Part A ✅ done 2026-08-20** — brief: [`docs/briefs/M4-stage-D-the-bench.md`](docs/briefs/M4-stage-D-the-bench.md). **The board Part B benches: it builds, and it has never been powered on.** A1 deletes `firmware/src/staleness.hpp` and `kRxWatchdogMs` and lifts the two clocks (three headers — `age_estimator` pulls both others) into `engine/include/depthcharge/`, so host and target compute the book's age with **one implementation** and the panel greys on the venue's own liveness signal against a threshold that signal calibrates — ~2 s at Anvil, ~4 s at Kraken, no constant anywhere. **Strain 23 closes by deletion, which was always the only way it could close**; 22's first clause and 17's residue (1) close with it; and **strain 10 is CORRECTED rather than executed**, because its prescribed resolution predates the 2026-08-17 ruling and contradicts it. The rewire is host-tested after all — the brief says "no host test can check" it, which is true of the BEHAVIOUR and not of the POLICY, so `firmware/src/liveness_watchdog.hpp` holds the rule and what is left in `feed_task.cpp` is a queue timeout and a clock read. A2 wires the healing path; A3 puts one venue per build behind `DC_VENUE` with `dc_tests_kraken` as the second host arm and the TLS anchor **measured off the live server twice** (the server sends leaf + WE1 + a cross-signed GTS Root R4; the self-signed R4 is pinned because it lives to 2036 against GlobalSign's 2028, and the two were checked to carry the same P-384 key); A4 puts the age on the panel and a `SOAK` line on the wire. **The adversarial review is the headline: 42 findings, three critical and all one species** — *a cross-task request that can be refused must be a LEVEL, not an edge.* The first healing design lost a request on four separate paths, including the liveness watchdog firing over a LIVE socket, and every one of them left the panel grey over a healthy heartbeating socket **for the rest of the run**. Also fixed from review: a stall re-latching the age baseline from the drain burst that ends it (a permanent phantom ~100 s age on a current feed); `kThresholdFloorMs` sized against book-event silence and therefore **below Kraken's worst healthy heartbeat**; `refused()` unwired; and the console walking the estimator's 256-entry ring across a core boundary. Green from a wiped tree at **33 ctest, 367 doctest cases, 895,787 assertions**, 19 xtensa objects, both images building (Anvil 880,885 B flash / 144,488 B RAM; Kraken 883,357 / 152,816). **20 mutations run, 20 red — five only after a first attempt showed the check did not catch them.** The cost is stated rather than hidden: `engine/` gains floating point, and M3's recorded "zero floating-point instructions" property is now false on purpose. **A5 RAN, and it is the part that earned the stage.** Flashed and captured for 1,860 s on MINA/GBP: the first Kraken image could not connect at all — Wi-Fi up, DNS resolved, **TLS verified against the pinned GTS Root R4**, and every attempt failing at the HTTP upgrade because the header buffer was a 512-byte Anvil-sized array on the RX task's stack and Cloudflare's 101 response is **1,002 bytes** (measured against both live endpoints, two `set-cookie` headers being most of the difference). Fixed to 2 KiB in BSS with the used size printed on every connect. Then, over one socket that never dropped: **the panel held colour through a 160,373 ms book-event silence** against a 26 s criterion, with **183 book silences over one second and `wd_gaps=0`** — the deleted `kRxWatchdogMs = 1000` would have greyed this panel 183 times in 31 minutes, which is the 2026-08-17 ruling quantified on hardware. **Three genuine CRC32 failures, three heals, three recoveries** (1,814 / 1,634 / 2,144 ms of grey, against B2's predicted 3,548 ms hole), `connects=1 sock=0 refused=0`, checksum ledger closing at `seen=597 ok=564 FAIL=3 unverifiable=30 unchecksummed=0`, heap flat at 61,136 B across all 185 soak lines, calibrated threshold settling at **4,006 ms** from a measured 1,001 ms heartbeat median. **M4's second, third and fourth DoD clauses are now all observed.** The second closed on 2026-08-20: the bench cannot produce a stopped heartbeat over a live socket — interface-disable and the mesh's per-client pause **both deauthenticate**, measured twice with `sock` incrementing and `wd` not — so it was staged in firmware with `DC_TEST_MUTE_LIVENESS`, a flag that lives in no `platformio.ini` environment and suppresses exactly one call. **`wd` 0→1 with `sock=0 connects=1`**, heartbeats still arriving throughout (`beat` 77→177), grey inside the calibrated 3,998 ms. The same run **demonstrated the adversarial review's critical finding on silicon** — the watchdog firing over a live socket is loss path #4, and the resync LEVEL healed it in 3,497 ms where the edge design would have left the panel grey until a power cycle — and **opened DESIGN strain 30** (opened as 26 and renumbered at M5 stage C, when a second card 26 turned out to carry 14 of the 15 references): `armed_` is set only by a liveness arrival, so a permanently dead heartbeat fires the watchdog exactly once, after which a book that then froze would render LIVE for ever. Narrow, unfixed, and B2's to decide. Three things the run did not settle are recorded rather than guessed: what caused the three checksum failures (NOT the pipe — `no_slot` was 0 at the first failure and rose only *during* each heal's snapshot burst, so the drops are a consequence, not a cause); that the four-slot pipe overruns on a heal; and that the Kraken build costs a panel colour rung (`depth=5` against the Anvil control's 6). **Committed as four, each verified green in a detached worktree with `CMAKE_HOME_DIRECTORY` confirmed, and since PUSHED — `origin/master` is at `ee9f2ec`; the "nothing pushed" this entry used to carry was never checked against the remote and was false.** **B1 part-scored the same evening** (owner at the bench, brief's session log): the board ran **2 h 19 m unattended on one flash** with `crc_fail=13 heals=13 refused=0` and heap flat to 52 bytes, and **two of B1's three clauses PASS** — 150,179 ms of book silence held colour with `wd=0`, and 13/13 checksum failures healed. **The third does not, on a technicality worth keeping**: blocking Wi-Fi kills the TLS read, so `on_disconnected()` raises the Gap before the 4,006 ms threshold can expire (`sock` 4→5, `wd` unmoved at 2) — both paths render as `STALE (disconnect)`, so only the counters can tell them apart, and **exercising the liveness watchdog needs a packet-DROP rule rather than an interface disable**. The same block also reproduced **strain 21 on the Kraken build**: the recovery re-rolled the mesh lottery (−37 → −71 dBm, now −72/−77, run min −82), though it cost nothing measurable this time. **And it produced an instrument finding that nearly put a fiction in the log**: this firmware emits TWO clocks — `[nnn]` is Arduino `millis()` (= `esp_timer`) and `I (nnn)` is ESP-IDF's FreeRTOS tick prefix — and they were **~14.6 s apart** after 2 h 19 m, the tick clock running ~0.22% fast (measured quantisation-free from consecutive `SOAK` lines: 10,021–10,022 ms of prefix per 10,000 ms of `up=`). Two figures first recorded from subtracting one from the other were wrong by exactly that and are corrected in the brief; the grey was ~0–1 s after disassociation, not 14.5 s, which *strengthens* the reading that the socket path beat the threshold. **Rule: never subtract one prefix family from the other; convert through the `SOAK` line's `up=`.** **B3 RAN 2026-08-22/23 — 25.39 h, parity with M3's 23.6 h met, no reboots, `live=1` at the end, zero capture gaps.** Evidence committed raw for the first time in this project (`hardware/bench-2026-08-22-kraken-b3-soak.log.gz`, sha256 `6a9139f6…fa8`) because three findings are statistical: **(1) the half-open case is real and happened twice** — the liveness watchdog fired 141 s and 291 s before the transport noticed the socket was dead, the condition B1 had to mute the firmware to stage, and it is 2 of 3 rather than universal; **(2) "193 CRC failures, all healed" is NOT benign** — P(next failure within 10 s of a heal) is 17.30% against a 2.07% Poisson baseline, 8.36×, with `no_slot` at **1,594**, so the four-slot pipe's heal/drop loop is real and turning over slowly (DESIGN strain 27); **(3) both outages were DNS** — 105 failures all at 14,000 ms and both successes paying the full timeout too, a dead primary resolver, with `assoc=1` throughout and zero Wi-Fi events in 25.39 h. **M4's five DoD clauses are now all observed.** A heap alarm raised from endpoint samples was retracted by medians and the largest-block "ratchet" proved a sawtooth; `tools/soak_report.py` now derives every figure so none is hand-read again. Nothing fixed — the DNS fallback and the pipe overrun are priced and parked. **What remains of M4 is B2's three decisions and B4's residues.** |
| M5 | A     | Binance adapter               | Partial-depth easy mode, then full diff stream with REST-snapshot bracketing and gap recovery; traces + goldens.     | M4 | ☐ **Next** — **and it needs its own detector before it can have a golden.** M4 stage 0's trace recipe (liquid pair, shallowest depth, no healing event in the window) works at Kraken because that venue publishes a **CRC32 over the top 10 levels** — an independent oracle that answers "is my book the venue's book?" outright. **Binance publishes no checksum**; `U`/`u` bracketing detects lost or misordered messages and says nothing about whether the resulting book is correct, and it can be satisfied while the book is wrong. So inventing the oracle is part of M5's work, not a detail of it — see [`harness/replay/NOTES-kraken.md`](harness/replay/NOTES-kraken.md) § "What transfers to M5, and — more importantly — what does not". **The one clause that does transfer, unchanged: whatever the oracle is, prove the trace catches a deliberately broken implementation before pinning it** (ARCHITECTURE §9, 2026-08-16 stage 0). Binance's own healing event is the REST re-snapshot on gap recovery, and it belongs outside any guarding window for the same reason a Kraken resync does. **STAGE 0 DONE (2026-08-24)** — brief: [`docs/briefs/M5-stage-0-price-the-binance-wire.md`](docs/briefs/M5-stage-0-price-the-binance-wire.md), measurements: [`harness/replay/NOTES-binance.md`](harness/replay/NOTES-binance.md). **What it settled.** *The oracle is the venue's own `@depth20` stream*: its `lastUpdateId` coincides exactly with a diff `u` on **every payload of every complete capture (899/899, 901/901, 90/90, 29/29)** — a committed *slice* can hold one trailing partial that cannot be graded, and one does, which is a cut artefact chased to its line and not a venue behaviour — it grades a correct client **884/884**, and it is RED against all three mutants while **`U`/`u` bracketing catches 0 of 3** — `U`/`u` detects transport loss (it caught the deliberate reconnect's 2,204 missed updates) and says nothing about book correctness. It caught an undesigned defect on its first run: seeded at the obvious `limit=100`, a correct client scores 82.4% while `U`/`u` is 890/890 clean, because 100 levels is a **$16 price window** and BTCUSDT moved **$29.85 in 90 s**. Costs, both stated: the oracle is **blind below rank 20** while the panel draws 25, and it is **+141% wire** (58.6% of the stream). *Wire price*: **22.84 KiB/s** both streams at 100 ms (1.3× / 2.5× headroom), **9.47 KiB/s** diff-only, **7.84 KiB/s** at the 1000 ms tick. *Numbers*: precision is a uniform **8 decimals**, NOT `tickSize` — 202,012/202,012 entries, zero exponents, zero floats — and **`kMaxSnapshotLevels = 256` is not enough** (33/884 failures in 90 s), so the board needs `limit=1000` **and a re-snapshot schedule**. *And the constraint with teeth*: **Binance declares no liveness signal at all** — both depth streams go silent (30 messages in 90 s on a quiet pair, worst gap **10.5 s**), so the 2026-08-17 grey ruling has nothing to arm on and M5 must either publish the venue's 20 s server PING across the §4 boundary or grey by socket death alone (ARCHITECTURE §9, 2026-08-24). Strain 25 answered: **no third JSON scanner needed** (zero floats, zero exponents, zero string escapes, max nesting 4 — a strict subset of both existing scanners). **The easy-mode question is deliberately left to the owner**, and section 4(a) has changed its terms: `@depth20` may now be worth keeping as the *oracle* rather than as a stepping stone. **Stage A done 2026-08-25** — *the two records that are not frames*: the trace record grew an optional `kind` (absent = `frame`), so a REST body carries the `req` that produced it and a WebSocket PING becomes a record at all. **One widening, not two** — `classify()` had to become answerable about a record with no frame, and the `static_assert` at `trace_decoder.hpp` moved with the contract. `Venue::Binance` lands with its decoder a **classifier** (0 FeedEvents, counted rather than claimed) and `run_replay` refusing the venue by name — exactly where Kraken sat between M4 stage A and B1. **All seven committed slices read end to end, none reshaped; the eleven pre-existing traces are byte-identical and no golden moved**, with seven new taxonomy rows and a shared record-shape corpus holding the C++ and Python readers to one contract. **Strain 22 closed**: an unmatched venue is now a hard failure in both languages. The ping is counted in a committed file for the first time — **17 across the seven, every one answered** — and reproduces stage 0's 19,970 ms cadence from the files themselves, obtainable only via the new `event_ns` (three pings 20 s apart share one `rx_ns`). **Two findings handed to C**: the ruling's ~80 s threshold is clamped to **30 s** by `kThresholdCeilingMs`, and at a 20 s cadence `kMinSamples = 8` needs ~160 s, so **no committed slice calibrates at all** — the two values coincide, so nothing misbehaves and the self-calibration is simply inert (ARCHITECTURE §9, 2026-08-25). And the pre-`48ba299` trailing-snapshot question is answered: **one of the seven lost one** (`binance_atomeur_d100ms`), it costs nothing, and re-capture stays B2's. **Stage B1 done 2026-08-25** — the third adapter, and the first written with an independent answer already in the room. `dc_binance_oracle` drives the REAL adapter over every committed slice and grades its ladder against the venue's own `@depth20`; **every matched count equals `tools/binance_oracle.py`'s for the same file** — two implementations, two languages, identical grades, **235/235 GREEN on both deep-seed witnesses**. Mutation-verified against that oracle: qty-0-not-a-removal, sides-swapped and book-bounded-at-256 each take 235 matched to **0**. **Strain 25 closed by the opposite of what it expected**: no third scanner: Kraken's was lifted verbatim into `engine/include/depthcharge/json_scan.hpp` and reused with **no venue flag**, 327 lines moved and none changed, held still by a suite assertion count identical to the byte (**3,609,744**). **Two truncation boundaries**, which is new — storage must be 1,024 deep because the sweep says the requirement is a property of how far the market walked (**the two witnesses disagree by 5×**), while emission cannot exceed the engine's 256; cost stated at **~96 KiB** fixed and never allocated. The scale is a **venue constant of 8 decimals** and `tickSize` is a **validator**, verified against the corpus GCD before being declared. **Crossed book answered with a count: 0 crossed, 0 touched across all seven.** And the finding with teeth, now **DESIGN strain 26** and an ARCHITECTURE §9 row: a **misspelled stream returns HTTP 101, answers pings in 0.107 ms and delivers nothing for ever** — invariant #5's frozen ladder arriving through the mechanism installed to prevent it, which the liveness ruling cannot see. Owner: **C**. **Stage B2 done 2026-08-26** — the seed, the walk and the schedule. **§9's untracked-evidence clause is discharged**: the two mixed-cadence captures the audit-stream ruling was signed on are committed **whole** (not sliced — a ruling's evidence pins a decision and its figure is a rate over a complete capture, which is now a §9 rule of its own), pinned in four tables, mutation-verified before pinning, and they reproduce the ruling's **90/90 coincidence and 88/88 GREEN** in two implementations. They also close a gap nobody had named in the mutant suite: **the board runs `@depth20` at 1000 ms and no mutant had ever been run at that cadence.** **The re-snapshot trigger is an observable**, and it is not the instrument B1 built: `min_bid_levels` counts levels HELD, which only ever grows, and reads a flat, healthy **100/100 through the two captures where the 82.4% failure class actually happens** — seeded COVERAGE is what erodes, and on the reconnect capture it goes **100 → 0 in 1.2 s**, faster than one REST round trip. **The margin is sized against a bounded fetch deadline rather than a percentile**: `margin >= walk rate x T`, T = 15 s, worst 15 s coverage loss **168 levels**, trigger at **448**. The p99 was dropped rather than softened — 36 committed REST records over two tiers give max/median **1.053** and **1.058**, so the round trip has no tail worth sizing against, and *every one of them is a desk-box figure against a board where B3 measured DNS at 14,000 ms.* **`age_ms` measured through a throttled replay** and the B1 hypothesis narrowed rather than confirmed: **TRACKS** a socket backlog at 1.00× (the ping shares the TCP stream), **BLIND** to a feed backlog — which is physical only at this venue — and, separately, **no reading at all for the first ~11 minutes of every connection**, because 32 baseline intervals at a 20 s ping cadence is 639 s. Two captures committed: the **221 s calibration trace** that finally lets a host test enter the self-calibrated liveness branch C has to tune (11 pings, 10 intervals against `kMinSamples`' 8), and the ruling's two. `req.sent_ns` was already on **36 of 36** REST records and is now guarded by an assertion rather than a pin. Remedies, thresholds and what the panel renders remain **C's**; the board's re-seed behaviour and the soak remain **D's**. **And M5 gains a close-out stage, added here because B2 assigned a clause to it and found it did not exist** — M3 and M4 both had one and M5's stage table did not, so *owner: M5 close-out* named nothing. It carries the **median-convention** change: `trace.cpp` interpolates where `sample_window.hpp` says nearest rank, so every Binance cadence figure this repository quotes is not the one the shipped clock computes (19,951.7 against 19,947.7, 20,011.6 against 19,962.8, 20,013.3 against 19,973.9). It is deferred rather than fixed because it rewrites figures inside `taxonomy_pins.inc`, and **a convention change that moves pins must be its own stage so the moved pins have nothing else in the diff to hide behind.** **Stage C done 2026-08-26** — *what a green liveness clock is entitled to mean*. **The threshold is settled, and the measurement said neither of the two numbers on the table.** §9's three shapes were *ceiling rises, multiplier falls, or both become per-venue*; the answer is **both**, because an absolute duration in a venue-agnostic object is a per-venue multiple — the same 30,000 ms ceiling is **60x** Anvil's median, **30x** Kraken's and **1.50x** Binance's, and a ceiling at 1.5x the healthy median is not a ceiling, it is the threshold. The multiplier is **derived by `liveness_clock.hpp`'s own rule rather than inherited** — worst healthy as a multiple of the venue's median, times ~2 — giving **2.0** against this signal's measured **1.005x**, and a threshold of **39,927.94 ms, calibrated and unclamped for the first time**, with the test that enters the branch and asserts what comes out. **Anvil and Kraken do not move at all, by construction**: their rows are the shipping defaults, `firmware/` passes nothing, every Anvil and Kraken `dc_replay` report is byte-identical, and no pin or golden moves — which satisfies §9's attribution constraint in the strongest form, *nothing to attribute*. Costed and rejected: a global ceiling raises the **uncalibrated default at the two venues where nothing is wrong** (it IS the ceiling), and a global multiplier cannot reach a live calibration here at all without falling to **≤ 1.503, below Anvil's 1.937x worst healthy multiple**. Cost of what was chosen, stated: Binance's pre-calibration threshold goes **30 s → 60 s** for the 159.7 s `kMinSamples` takes here. **Strain 26's seed limb is closed by remedy (a)** — the `Snapshot` is published when a diff brackets `lastUpdateId + 1` and never before, so the lying socket greys with **no threshold, no detector, no new `GapReason` and no §4/§6 change**; the defect fixture went from a populated **LIVE** ladder to `STALE — book unknown`, and **its two THE LIE assertions were inverted in the commit that made it pass**, as its expiry clause required. **(b) was rejected on a finding, not a preference: it does not work on the board** — `expired()` is gated on `armed_`, only the first `on_liveness` sets it, so withholding liveness leaves the watchdog never armed and the lie permanent *and* invisible to the instrument (b) relies on. The coupling ran one way: **(b)'s cost IS the threshold, so the threshold decision constrained the remedy and the remedy constrained nothing.** **The transport-versus-feed hole has no fix and is now stated at the point of use**, beside `on_liveness` in `liveness_clock.hpp` — the board's copy in `firmware/src/liveness_watchdog.hpp` is **D's, because C is desk-only**. **Strain 28's C-half done**: `DisplaySnapshot::reseed` `{None, Wanted, InFlight}` in the four pad bytes the struct already carried, `sizeof` **1,168 / 3,528 verified on host AND on the pinned xtensa-esp32s3 GCC 8.4.0** against a negative control, with `InFlight` deliberately unreachable so an open card is visible **on the panel** rather than on a report line. `kAgeWindowSamples = 256` **decided and unchanged** — the window bounds REACH, and wider is the safe direction; the constant that bites is `kBaselineSamples`, and 32 is what a measurement set. **And M5 CANNOT claim parity with M4's definition of done** — the reduced claim is written out rather than left to be discovered at the bench: the panel greys within the calibrated threshold of **the socket** falling silent and refuses to colour a ladder the feed never confirmed; it cannot see a server-side subscription drop, and `age_ms` is a socket-backlog estimate that reads nothing for the first ~11 minutes. **The duplicate strain card 26 is resolved by moving the OLDER one: M4 stage D's becomes DESIGN strain 30**, because 14 of the 15 references across 8 files meant the M5 card and *both meanings lived in this table two rows apart* — card 23's precedent followed by its reason (fewest broken citations) rather than its letter (newer moves), which at 22→23 could not tell the two apart. **And the two cards interact rather than merely colliding:** remedy (b) is blocked by card 30 being open, not wrong in principle. **Raised, not executed:** **strain 29's tripwire, which is written against the wrong event** — it fires on *quoting* a cadence figure when the hazard is quoting one from the wrong home. Also found while scoping: the corpus's **two oldest traces had been CRLF in the working tree since 2026-08-07** with `git status` clean throughout (a stale index stat cache) — restored, and because the repair has no diff, **what ships is the guard**: no committed trace may contain a CR byte, mutation-verified. **STAGE D-A1 DESK WORK DONE (2026-08-27)** — brief: [`docs/briefs/M5-stage-D-A1-the-footprint-and-the-venue-build.md`](docs/briefs/M5-stage-D-A1-the-footprint-and-the-venue-build.md). `[env:depthcharge-binance]` exists and builds, `DC_VENUE=3`, one TLS anchor for both hosts (a shared **wildcard leaf**, verified against each server with `-verify_return_error`). **The stage's first lever was void and the arithmetic behind it double-counted:** *“the four WS buffers to PSRAM, +16,384 B”* names buffers that went with `esp_websocket_client` on 2026-08-16 — the 16 KB had already been reclaimed and was already inside the baseline, so the projected +3,488 B clearance of the double-buffer floor was really a **12,920 B deficit**. Covered instead by `kReserveInternalBytes` 96→80 KiB, which is *the same obsolete sentence* (it is sized for the deleted two-handle client) and is recoverable: the reconnect-heavy 08-24 soak puts the owned stack's worst-ever draw at **62,140 B through `connects=4`**, leaving 36,164 B untouched. `buf_lvl_` (32 KiB) is the project's **first deliberate PSRAM placement** — §5's *“the tail in PSRAM”* was already on side. Guard shipped so this cannot recur silently: `venue_budget.hpp` asserts the venue adapter's internal footprint against the reserve and the d3 rung at **compile** time, mutation-verified. **Found while pricing FramePipe:** `kFrameCapacity` is **0.57×** this venue's largest message (28,639 B against a 16,384 B slot; 13 of 3,119 over) where it is 1.9× at Anvil — each overflow costs a `Gap{SeqGap}`, a grey and a re-seed, and it cannot simply be raised (4×32 KiB is +65,536 B against a 35,464 B budget) while strain 27 contraindicates fewer slots, so **moving FramePipe to PSRAM is the only lever relaxing both** — priced, not built, and D-B's to decide. **Outstanding: deliverable 7, the flash.** Also owed — Binance's liveness clock is the **server ping**, a control frame the adapter cannot see, so `liveness_count` honestly returns a constant and the watchdog is never armed on this build; wiring it crosses the RX/feed task boundary and is needed before **D-C** can test *Owed by stage D* item 5's parity claim. **FLASHED 2026-08-27 — deliverable 7 done, both readings pass.** Anvil control moved `budget=` by **exactly 16,384** with the rung unchanged; the Binance board boots **`depth=3 double-buffered`** (`free=117,548 budget=35,628`), **156 B off the desk's projection**, and holds an honestly grey ladder for 70 s over a socket carrying 580 real depth frames. **One DoD clause was unreachable and the brief was what was wrong:** `seeds_unconfirmed` counts only *withheld* seeds, and this stage's own *Out of scope* removes the REST client that would produce one — corrected at source, with the `-- seed` line (`buffered=572 overflow=8 reseeds=8 | bracket ok=0 FAIL=0`) standing as remedy (a)'s evidence instead. **The reserve cut owes D-C a named check:** it buys Binance's second buffer out of the network heap, leaving largest-block **17,396 B against mbedTLS's 2×16,717 B** — a **679 B** margin at `connects=1`, so D-C records largest-block **at every reconnect** and **below 16,717 B the cut is wrong at the second socket**. Bench condition recorded in `hardware/BRINGUP.md`: `upload_speed=921600` fails **mid-write after the erase**, leaving a partly-erased board that reads as a firmware fault; 115200 is clean. **STAGE D-A2 DONE (2026-08-29) — A LIVE BINANCE BOOK ON THE PANEL**, `live=1 rows=54/54` held 60–140 s with `bracket ok=4 FAIL=0` and `oversize=0`. Four findings worth the row. **(1)** The pre-seed buffer was sized against a *round trip* where it had to survive a *deadline* 10× larger: **0 of 2,668 fifteen-second windows survived** at 64/2,048, so the 15 s deadline was unkeepable in principle. Re-sized to 256/32,768 against a measured worst window of 152 events / 12,458 levels, and the adapter's INTERNAL footprint FELL 2,040 B because both halves went to PSRAM. **(2)** `esp_tls_conn_new_async` **does not converge on this build** — 1,780 polls over 15 s, four runs, stuck at `conn_state=1`/`errno=119`; the synchronous call works first time, so the fetch got a **third task** (the first new cross-task hand-off since M3, §9) at priority 4 so the feed preempts it. On the feed task it stalled 1.93 s and **broke the very bracket it existed to satisfy**. **(3)** A seed fetch and a TLS reconnect **never overlap**, on two independent grounds — a fetch takes the largest free internal block to **~4 KB against the 16,717 B one TLS session needs**, and a body whose diff stream has ended is worthless before it arrives. Host-tested policy, not transport discipline. **(4)** `kFrameCapacity` 16→64 KiB (**2.29×** this venue's largest observed message), which stopped the board re-greying every ~23 s — and the constant's own comment (*“a dropped frame costs one refresh”*) turned out to be **Anvil's reasoning, false at a diff venue**. Affordable only because the slabs moved to PSRAM. **`worst_frame` CLOSED (2026-08-29): it was the wrong instrument, not a regression.** It is wall-clock, so it absorbs preemption of a task Wi-Fi and lwIP sit far above; its peaks do **not** coincide with a fetch (worst fetch-concurrent frame 2,392–6,551 us over 93–133 such frames, three captures); and as a **distribution** the modal frame is **1–2.5 ms**, p99 is **10–25 ms**, and only **10 of 1,808 (0.55%)** are slow enough to cost pipe slots. The bare maximum was reporting 0.2% of the population as though it described the feed path. Three hypotheses tested and discarded — previously-dropped messages now parsed (refuted by onset), `ladder::rank_of` scanning a deep book (refuted on the host: ~0.13 µs/level FLAT across seeded depths 0/100/500/1,000), and network stalls (Pearson r = **0.043** between `worst_gap` and `worst_frame` over 62 board logs). PSRAM slab scanning is the named, untested residue: four frames in 1,808 exceed 50 ms. **D-C watches `slow(>25ms)`, not the maximum** — its edge is derived (four consecutive frames at 25 ms consume one ~99.2 ms arrival interval) and the derivation is a `static_assert` in `frame_pipe.hpp`. **STAGE D-B DONE (2026-08-30) — the four rendering decisions, taken at the panel because no host test can take them.** Brief: [`docs/briefs/M5-stage-D-B-the-four-rendering-decisions.md`](docs/briefs/M5-stage-D-B-the-four-rendering-decisions.md). **(1) A silent feed renders IDENTICALLY to an ordinary pre-seed grey** — chosen rather than defaulted, because a client cannot tell a silent stream from a slow one and a panel that distinguished them would be claiming knowledge the firmware does not have; recording it as a decision is what stops a later session *improving* it, which would be a reversal rather than a gap being filled. **(3) Remedy (a)'s grey READS RIGHT** at desk distance, at 224 brightness, with the header carrying the stale reason — *not trusted yet*, not *broken* — so the ~20 s boot grey stands unchanged and wants **no distinct seeding appearance**. **The two interlock and reopen together**: *identical* is only acceptable while the grey reads as withholding, and the other way the same pixels would mean a panel that looks broken on every ordinary boot. **(2) A re-seed in flight changes the HEADER ONLY and the ladder keeps its colour** — greying a book that has *not* gone wrong would lie in the safe direction for the **3.1–5.6 s** a fetch measures on this board, and B2's **0 of 7 adoptable at `limit=1000`** says that is not a rare event; **the slot is pinned to the SYMBOL's** under the standing **VALUE > AGE > SYMBOL** priority, so D-A3 inherits constraints (at most eight characters, asserted against the real header width in `test_ladder_render.cpp`, drawn in `Ink::Symbol` rather than a new `Ink`) rather than a choice. **(4) The panel shows unvalidated levels and that is fine**, said out loud once on DESIGN card 24 where a reader meets it — **strain 24's tripwire is CROSSED for the first time** (seed `limit=1000`, emitted window 256, `validated_depth` **0**), and the per-row marker stays **deferred to D6 rather than rejected**, `sizeof(DisplaySnapshot)` unmoved at **1,168**. **And the sitting's most-paid-for finding was about EVIDENCE rather than rendering, which is why it cost two §9 rows.** *The first time the forbidden output has been prevented on hardware rather than in a test* is recorded from the two committed captures (`hardware/bench-2026-08-30-D-B-silent-stream-*.log.gz`), which hold the defect image grey for the full **480 s** at `rows=0/54 unknown=54 frames=0` and never reach `live=1` — **not** from the photographs, four of which cannot be attributed to a build at all: EXIF stripped from every one, **no capture of `depthcharge-binance` anywhere that day**, and the four saved *inside* a defect run. **The fifth attributes itself**: one frame reads `SEQ GAP`, whose every raise site sits downstream of a received frame or buffered event while both captures hold `msgs_in=0 frames=0 seqbreak=0` throughout — a feed that never speaks has nothing to gap — so it is the working image proved from its own pixels, and its save-time is therefore **provably not its shutter-time**. The files are renamed to drop the build claim and keep only the question each was offered for. **The rule that came out of it: before recording an absence or a difference as a property, establish that the observation window was the same for both sides** — a clock was mistaken for a defect **twice in one sitting** (`firmware/logs/` looked empty because the search ran before the flush, and the *fix* being drafted for that phantom would have broken the task, since `pio run` has no `-f` option; `RESYNC` versus `NO LINK` looked like a firmware discriminator and was uptime, both captures crossing `sock 0 → 1` at `up=310–320s`) and **a third time in the sentence written to record the second**, which is how `SEQ GAP` was found. **Also repaired**: four live citations of a `capture_noreset.py` that has **never existed in this repository**, the real mechanism being a bare `pio device monitor` — which does not reset the board — plus `monitor_filters = esp32_exception_decoder, time, log2file` in `[env]`. **Owed: the soak — D-C.** |
| M6 | B     | Carrier PCB                   | KiCad carrier (WROOM-1-N16R8, 2× 74HCT245, HUB75 IDC, USB-C 5 V/3 A with CC pulldowns, bulk caps, EC11); DRC clean; fabbed & bring-up. | M3 | ☐ |
| M7 | A+B   | Enclosure + board mode        | Printed enclosure, smoked acrylic front; encoder modes: ladder (symbol/venue/zoom) + Anvil 12-ticker summary board mode. | M4, M6 | ☐ |
| MP | A     | Portfolio portal              | **Executes in the `garethcooke-portfolio` repo.** Stage 1 (any time after M0): `/projects/depthcharge` live with In Progress badge, concept art, tags, repo/architecture links, plus two drive-by fixes. Stage 2 (after M3): real hardware photos/video. Stage 3 (any time): the design doc reaches the portal — `/depthcharge/design` renders `docs/DESIGN.html`, tracked by its own `writeup-sources.json` entry so it drifts independently of the architecture page. Briefs: stages 1–2 in the **portfolio** repo at `docs/briefs/MP-portfolio-portal.md`; stage 3 here at [`docs/briefs/MP-design-doc-on-the-portal.md`](docs/briefs/MP-design-doc-on-the-portal.md). | Stage 2: M3 | ◐ Stages 1 ✅ (2026-07-22) and 3 ✅ (2026-08-08, pending commit in garethcooke-portfolio); **stage 2 is UNGATED as of 2026-08-16** — M3 is done, so the real hardware photos/video can go up, and the stale-panel shot from the acceptance is a good one |

## Backlog (not scheduled)

Items carry a stable ID — `A*` Anvil-side, `D*` DepthCharge-side — so briefs and session
logs can cite one without quoting it. Each is a one-line **what**, then indented detail only
where there is evidence to carry. Closed items move to the bottom rather than being deleted.

### Anvil-side (cross-referenced only)

**Anvil is a separate repo and is not modified from here.** Nothing below is a proposal to
Anvil; each is a note of what DepthCharge is waiting on, working around, or has measured
from the outside. They live on Anvil's own backlog.

**The ask was [`docs/anvil-handover-2026-08-16.md`](docs/anvil-handover-2026-08-16.md); the reply
is [`docs/anvil-handback-2026-08-16.md`](docs/anvil-handback-2026-08-16.md).** Anvil answered the
same day: **A7, A2, A3 part 1 and A6 are done, pushed and deployed**, its backlog is rebuilt
around these IDs, and one item is new (**A2b**). Both sides now cite the same letters.

| ID  | Item                            | Standing                                                      |
| --- | ------------------------------- | ------------------------------------------------------------- |
| A7  | `depth` parameter on `/ws`      | ✅ **DONE + INTEGRATED 2026-08-16** — 112.6 → 30.8 KiB/s       |
| A6  | TLS-chain rotation              | ✅ **DONE** — in Anvil's `deploy/README.md`, lock-step flagged |
| A2  | Client pings / keepalive        | ✅ **DONE** — and the ordering is now *measured*, closing D5   |
| A3  | Per-socket send-queue behaviour | ◐ **pt 1 documented**; pt 2 (bounding it) open, owner's call  |
| A2b | The 2 min 56 s stall            | ☐ **NEW, open** — split out of A2; two Anvil hypotheses       |
| A1  | Sequenced incremental L2 feed   | ☐ Sized, not started — **and A3 pt 2 must be decided first**  |
| A4  | Chaos flag for gap testing      | ☐ Open; near-required alongside A1                            |
| A5  | Feeder realism                  | ☐ Open; nothing here is blocked on it                         |
| B1  | `summary.last` = traded price   | ✅ **DONE Anvil-side** — semantic change, **no action here**   |

**Nothing on this side is blocked on Anvil any more.** The two integration tasks the hand-back
asked for are both done: `&depth=27` is in `kAnvilPath`, and `docs/vendor/anvil-protocol.md` is
re-pinned — now at **`04db612`**, not the hand-back's `b4d31c2`, because Anvil landed **B1** the
same evening (below).

**B1 · `summary.last` is a traded price, not the book mid.** *Anvil `864ee2f`/`04db612`, found by
checking their repo rather than by being told.* A **semantic** change inside wire version 1 with
**no version bump**, which is the class a shape check cannot catch: same key, same JSON string
type, same `""` sentinel — but `""` now means *this ticker has not traded yet* rather than *the
book is empty*, and the value **persists after the book empties**.

- **No action here, and it is structural rather than lucky.** `AnvilAdapter` files `summary` under
  `FrameKind::Summary → ++stats_.summary_ignored` and returns, so no field of a summary frame has
  ever reached the book, the ladder or the panel. The only thing this project derives from
  `summary` is its **arrival count as a 2 Hz clock** (`staleness.hpp`), and B1 moves a value, not
  a cadence. No engine change, no firmware change, no golden moves.
- **M7 is the milestone that will care.** The board mode is what finally *reads* `summary`, and it
  must treat `last` as a traded price with a persistence rule, not as a mid — a board built on the
  old meaning would show a price on an empty book and be correct to.
- **The lesson repeats within eight hours:** the vendored header already argued for re-vendoring
  *on a schedule rather than on a symptom*, having sat three weeks stale once. It was stale again
  by the same evening. Worth a standing check rather than a resolution.

The detail below runs in ID order **except A7, which comes first because it was the cheapest
item here and the largest win** — and it is now closed.

**~~A7 · A `depth` parameter on `/ws`~~ — DONE Anvil-side, DEPLOYED, and INTEGRATED here
2026-08-16.** `kAnvilPath` is `/ws?ticker=101&depth=27`. That one constant is the entire
integration; `engine/` did not move, exactly as the ask predicted.

- **Verified on this side rather than taken on trust** — the ask's own lesson was that a nearby
  measurement gets read as answering the question that mattered.
  `harness/replay/anvil_101_depth27_20260816.ndjson` (90 s, 1,399 frames, committed and now a
  ctest case) priced by `tools/anvil_frame_economics.py`: **`book` mean 8,428 → 2,471 B**,
  **112.6 → 30.8 KiB/s**, **27.3% of the 2026-08-09 baseline**. This repo predicted 27.8%.
  Anvil's own deployed-server figure of 2,476 B agrees to **0.2%**.
- **Against the 23.6 h soak's worst measured hour (56 KiB/s): 1.8× headroom, where it was 0.5×.**
  That is the staleness fix, and it needed no protocol redesign, no sequencing and no resync.
- **DEPTH IS SERVED IN TIERS** — 1,2,3,5,8,10,15,20,30,40,50,75,100,150,200,300,500,1000, then
  unlimited. A request rounds **up**, never down, so asking 27 is **served 30** and the adapter
  truncates the last three. Not in the ask; it came out of Anvil's review, and the reason is
  sound — `depth` arrives from an unauthenticated query string, so free-form depth would have
  cost one serialisation per distinct depth in use, per ticker, per tick.
- The tier's cost, stated because Anvil sized it as "a few percent": 30 served against 27
  rendered is **9.7% of `book` bytes** the panel never draws. Trivially worth paying; if a later
  ladder ever offers 27 exactly, that is where the remaining tenth is.
- **A premise in our own ask was wrong, and it is worth recording.** We cited
  `GET /api/book?depth=` as proof that "the name, concept and validation already exist". The
  parameter existed and was **silently ignored** — there was no validation to reuse, and Anvil
  fixed the REST surface as part of A7. Arguing from the existence of a parameter is not the
  same as arguing from its behaviour.

**A1 · Sequenced incremental L2 feed.** Promoted 2026-08-11 to the thing that makes the
hardware work at all; **demoted 2026-08-16 by A7, which has now shipped** — the query parameter
took the stream to 27.3% for one constant on each side, so this is not blocking anything and is
judged purely on its own merits. On those it is still the better design.

- **Anvil sized it 2026-08-16 and the prerequisite is small.** The per-ticker monotonic book
  sequence is **S and independent of Stage 4 in both directions**, so it lands well before it:
  the publish loop already stamps per ticker each tick, and a second counter adds no cross-thread
  state, no lock and no engine change. One scoping point buys that size — **books only**. A
  sequence that also orders *trades* against books needs the broadcaster to merge-sort two
  independent sources, and a delta feed does not need it.
- **NEW AND ORDER-CHANGING: A3 part 2 must be decided BEFORE A1, not alongside it.** Our own note
  said the two "have to be answered together"; Anvil sharpened it into a direction. The
  per-ticker sequence is genuinely gap-detectable end-to-end **because** the per-socket queue is
  unbounded and therefore lossless. If A3 part 2 is resolved by *dropping* `book` frames, that
  property dies — benign under full replaces, **fatal under deltas**. So the queue decision
  constrains the sequence design, and taking A1 first would risk building on a guarantee A3 is
  about to remove.

- *Sized 2026-08-16.* "A tenth the bytes" was a 3× under-claim: **a median of one level changes
  between consecutive `book` frames** (mean 1.1, p99 3, max 4 over 90 s), so an upper-bound
  delta encoding is **1.3% of current `book` bytes** — against A7's 26.5%.
- Its load-bearing prerequisite is a **per-ticker monotonic sequence**: the global stamp is
  sparse *and* non-monotonic per socket by design, and unlike a full replace, a missed delta
  never heals. PROTOCOL §1 already names this as the change "if strict gap detection is ever
  wanted". Worth doing first and alone, even if deltas never follow.
- **It is not Stage 5** and must not wait for it — Stage 5 is binary ITCH over LAN UDP
  multicast, gated behind Stage 4.

- *Rewritten 2026-08-16.* The three claims this item used to make — that a 5,744-byte window
  caps the board at 65.5 KiB/s, that no firmware lever reaches it, and that the alternative
  is a rebuilt ESP-IDF — are all superseded, because the rebuild happened.
- **What shipped instead:** `liblwip.a` rebuilt from the shipped esp-idf v4.4.6 vintage with
  `TCP_WND` 5744 → 17232; now the default build.
- **Soak (23.6 h, 2026-08-15/16)** says it is load-bearing: **56–87 KiB/s at every hour of
  the day** (floor 56 mid-morning, ceiling 87 at midnight) against Anvil's 110.4 KiB/s wire —
  **51–79%** of it (corrected 2026-08-16 from "60–80%", which was arithmetic, not measurement:
  56/110.4 = 50.7%), and most hours *above* the stock window's hard cap. Lag slope fell from
  +0.57 s/s on every stock-window run to **+0.083 s/s over the final 10.9 h**.
- **RX loop instrumented and exonerated:** `wait 0 / read 98–99 / feed 0` in every hour, so
  the board is bound by how fast bytes come off the wire into it, and there is nothing left
  to optimise on this side.
- **Residual is path bandwidth** on the transatlantic hop. Until a smaller feed exists the
  object runs tens of seconds behind at UK peak and honestly says so (ARCHITECTURE §9,
  2026-08-16) — but the *smaller feed* no longer has to be this item. A7 is.

**~~A2 · Client pings / keepalive~~ — DONE 2026-08-16, and the ordering is MEASURED.**
`PROTOCOL.md` §3 gains a **Keepalive** subsection: a client ping is always answered, no rate
limit and no payload cap (our 10 s cadence is explicitly fine), the server **never initiates** a
ping in v1 and **never uses pongs to detect dead clients**, and — the load-bearing sentence —
**the pong is queued behind whatever is already waiting for that socket.**

- **D5's "still owed" is closed, and closed properly.** This repo had recorded that the ordering
  was read from Anvil's source and *never captured under induced backpressure*, so the number
  was evidence rather than a guarantee. Anvil captured it: `tests/tools/pong_ordering_probe.py`
  stops reading until frames back up in the send queue, then sends one ping and reports the
  pong's **position in the byte stream** rather than its RTT — because a slow reader inflates the
  round-trip whether or not the server reordered anything, so timing alone cannot separate a
  server-side queue from a client-side one. **425,890 bytes drained as 149 frames; the pong at
  index 148 — last, with zero frames after it.** Read it as a guarantee.
- Two things would still silently break it, recorded as "please don't change" rather than as
  defects: a ping after a two-way close handshake is never ponged, and the behaviour depends on
  `max_payload` staying default with `CROW_ENFORCE_WS_SPEC` undefined. `PROTOCOL.md` now says to
  re-run the probe on a Crow upgrade rather than assume it survived.
- The request for a **server-side heartbeat is withdrawn** and recorded as such on both sides.

**A2b · The 2 min 56 s stall — NEW, open, and Anvil split it out for a good reason.** Our A2
carried two unrelated things: a doc confirmation and an open-ended investigation. Sizing the pair
as "S, mostly withdrawn" hid the second behind the first.

- Our framing is preserved intact: nginx is exonerated, but a successful request on a
  **different** connection cannot prove that flow was healthy, so from outside the evidence
  cannot separate a server stall from one transatlantic flow in RTO backoff.
- **Two Anvil-internal hypotheses fit the signature** (total silence on a healthy TCP connection,
  spontaneous recovery, HTTPS unaffected), neither confirmed: **fan-out head-of-line blocking**
  — the broadcaster holds one mutex across the *entire* fan-out, so one stalled socket's
  unbounded queue stalls every socket on every ticker, and it would free **spontaneously** when
  that socket disconnected, which matches the unexplained recovery; or **engine-thread publish
  starvation** — the ~2 Hz summary walk touches every resting order, and the broadcaster skips a
  ticker whose sequence has not moved, producing silence on a healthy connection.
- The first hypothesis is Anvil's leading candidate and **makes A3 part 2 look much less
  optional**.
- The fix Anvil recorded is **server-side telemetry rather than a wire heartbeat** — broadcaster
  last-tick and worst fan-out duration, engine last-publish per ticker, on `/api/health` — so the
  next stall is diagnosable from outside, including by us. **D5 already splits half of this from
  our side** (`age` high + `rtt` high = our queue; `age` high + `rtt` low = upstream); the
  telemetry would say *which* upstream cause, which our instrument structurally cannot.

**A3 · Per-socket send-queue behaviour.** *Part 1 (document it) is **DONE 2026-08-16**; part 2
(bound it) remains open and is the owner's call.*

- **Part 1 shipped.** `PROTOCOL.md` §4 carries **"Slow consumers — the server queues, it does not
  shed"**: no cap, no drop rule, no coalescing, no disconnect threshold, unbounded in v1, lag
  linear with no plateau — with our measured figures cited in Anvil's text. The client rule we
  asked for is now **contract**: measure freshness against your own clock, never infer it from
  message rate. Our withdrawn bullet is retired into a *Closed* section with the record intact,
  and it names the distinction the original conflated — coalescing *does* exist, but it is the
  ~14 Hz book tick, which is upstream and global, not per socket.
- **Part 2 is sized M, not S.** Crow exposes no queue depth and no send-completion callback, so
  any bound needs a patch to the vendored header plus an atomic counter to keep the check off the
  broadcaster thread. Our stated preference — disconnected rather than served 100-second-old
  data — is recorded.
- **A7 narrows it for us but does not close it:** a *full-depth* socket that stops reading still
  costs the server the original ~110 KiB/s. Our own socket is now a third of that.
- **And it is now coupled to A1 in a direction we had not seen** — see A1.

- The earlier wording here said "coalescing / even backpressure-shedding", on the strength of
  a 2026-08-09 rate-and-gap measurement; that conclusion is **withdrawn**
  (ARCHITECTURE §9, 2026-08-11).
- **Measured 2026-08-11** with `tools/anvil_freshness_probe.py`: a desk socket throttled to
  25% of the stream sees **every** frame kind thinned by the same fraction and its lag grow
  **linearly to 111 s over 150 s with no plateau**, implying **~12.4 MB still queued for that
  one socket** and rising. A DepthCharge board on one socket accumulated ~98 s of backlog in
  210 s of uptime.
- **Confirmed in Anvil's source 2026-08-16**, so it is construction rather than weather:
  `CrowWsSubscriber::deliver()` → `conn.send_text()` → Crow's per-connection `write_buffers_`,
  a `std::vector<std::string>` with **no cap, no drop policy and no coalescing** on the path.
  One socket that stops reading costs the server ~110 KiB/s of RAM for as long as it stays
  connected.
- **Downstream rule** — stands, and is now sharper: never assume a thinned stream is a fresh
  one, and M4/M5 delta venues cannot tolerate either shape without gap + resync.

**A4 · Chaos flag** for deterministic gap testing. Near-required if A1 ever happens: a delta
feed's gap-recovery path cannot be proven without one.

**A5 · Feeder realism** — Hawkes arrivals / mirror mode / FrontierView execution-algo
participant. DepthCharge is its future test client.

**~~A6 · TLS-chain rotation~~ — DONE 2026-08-16.** The note is in Anvil's `deploy/README.md`
beside the certbot step: it records that the firmware pins ISRG Root X1 as its sole anchor with
no fallback and no console, that routine renewals are invisible to us, and that a CA change or a
lost cross-sign is a silent handshake failure fixable only by reflash — flagged as a **lock-step
change: coordinate the firmware update first, then rotate.** The original ask, for the record:

**A6 · TLS-chain rotation** — a DepthCharge-firmware-pinned dependency: an Anvil CA/chain
change means a lock-step firmware update. *Sharpened 2026-08-16:* the firmware pins **ISRG
Root X1** as its only anchor and Anvil deploys with certbot behind nginx, so ordinary renewals
are invisible **so long as the renewed leaf still chains to X1**; a CA move or any shortening
of the presented chain (there are *two* cross-sign hops, and YE←X2 is the newer) is loud on
DepthCharge's serial console and silent on Anvil's. **The whole ask is one line in Anvil's
`deploy/README.md`** — tell DepthCharge before changing CA or chain configuration — or an
explicit `--preferred-chain`, which survives a certbot default change in a way a README line
does not.

- **Owed here, found 2026-08-16:** `firmware/src/anvil_root_ca.hpp`'s own header says the
  ESP-IDF bundle is unreachable because `WiFiClientSecure` offers only `cert_pem`. **That died
  with the M3 transport rewrite** — `ws_transport.cpp` builds an `esp_tls_cfg_t` by hand, which
  has `crt_bundle_attach`, and the framework ships `CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=y` with
  200 certs including X1. The pin is now a *choice*, not a constraint; the comment is stale and
  should say so, and "switch to the bundle" is a live option worth its own decision.

### DepthCharge-side

**D0 · §1 describes a terminal with trade prints, and two of its three venues have none.
Design, or debt?** *[owner's call — not on any M5 stage.]*

- **What is actually true, measured rather than assumed.** `§1` and `§5` describe a
  recent-trade ring and trade prints flashing white at the touch. **Only Anvil supplies
  them.** Kraken puts trades on a `trade` channel DepthCharge does not subscribe, so
  `is_trade("kraken", …)` is always False on a book capture — `tools/tracefile.py` says so
  in its own docstring. Binance is identical: `@trade` and `@aggTrade` are separate streams
  and nothing subscribes them.
- **So the ring has been Anvil-only since M4** and it was never written down. M5 stage B1
  raised it as a Binance-shaped hole and **that framing was wrong**: nothing about this is
  new at the third venue, and it is not an M5 item.
- **The question is whether that is design or debt.** If design, it belongs in §1 beside the
  liveness caveat — *this terminal draws trade prints at venues that interleave them on the
  book socket, and at no others*. If debt, a trade stream is a subscription and a byte
  budget the 2026-08-25 ruling did not price, **at two venues rather than one**, and it
  wants a stage.
- Nothing was subscribed to establish this and nothing needs to be. Recorded 2026-08-25.

**D1 · The rejoin re-rolls the mesh lottery, and the board never roams off what it draws.**
*[A] — one session, host-testable policy half.*

- **Measured 2026-08-16** during the passing acceptance: boot's explicit scan joined at
  **−40 dBm**, the recovery's plain `WiFi.begin()` landed at **−70** and drifted to −76, and
  the watchdog greys went from 1 to **14 in three minutes**.
- **Why:** `connect_wifi()` surveys and joins the strongest by name; the supervisor's rejoin
  cannot, because a blocking all-channel scan on loopTask would stall the 250 ms supervise
  poll. `ALL_CHANNEL_SCAN` + `BY_SIGNAL` narrow the lottery and do not close it — which is
  what §9 (2026-08-13) already recorded when they failed their five-boot acceptance, and the
  boot fix was never extended to this path.
- **The shape of the fix is already established by M3:** the socket connect used to block
  loopTask and now runs on the RX task, with the supervisor deciding and the task doing; the
  rejoin's scan-then-join wants the same treatment.
- Details in [`hardware/bench-2026-08-16-pull-the-wifi-acceptance.md`](hardware/bench-2026-08-16-pull-the-wifi-acceptance.md).

**D2 · Weak-node 1 h soak.** *[B] — owner-driven at the bench.* The one transport-brief
acceptance bar never run on the final build: pin or re-roll the association to a −7x sibling,
hold it an hour, and check that the fades grey the panel without killing the socket (board
B's `…:F9` record is the comparison). Small, and the last thing standing between the
transport rewrite and a fully ticked DoD. *The board is currently sitting on a −75 dBm
association by accident, which is most of the setup.*

**D7 · HUB75 switch-on brownouts the DevKit at roughly 1 in 6 — and COUNTING SWITCH-ONS CANNOT
SETTLE WHY. Scope the rail instead.** *[B] — owner-driven at the bench.*

**The effect, and its softness.** Two `rst:0xf (BROWNOUT_RST)` in **12** switch-ons with the
ribbon attached (2 unprompted on 2026-08-24, then 0 of 5 with the panel grey and 0 of 5 with the
panel driving a live ladder): 16.7%, 95% CI **[2.1%, 48.4%]**. The two unprompted events arrived
unblinded, unstructured and at a moment nobody was counting, so they carry a selection effect —
they are memorable *because* they happened — and the point estimate should not be quoted without
the interval. **THE ROBUST CONCLUSION, which holds across the whole interval: five trials per
arm buys almost nothing.** To distinguish an attached-ribbon rate from zero at Fisher p<0.05
needs ~10 trials per arm at the 48% upper bound, ~30 at the point estimate, and ~60 at 10%.
Twenty switch-ons were run and bought no information. A session tempted to run a few more should
read that sentence twice. **What the run DID retire.** Panel state drops out entirely — grey
(0/5) and live colour ladder (0/5) behave identically — which kills the hypothesis that display
current draw or DMA activity couples the transient. The ribbon-out arm (0/5) is held separate
because the ribbon is still a candidate, but at this power it proves nothing either. **The only
plausible variable left is the mains phase at switch closure**, which is inherently stochastic
and which nothing on the board can observe — so no amount of board-side logging will ever
resolve it. **THE METHOD CHANGE, and it is the same principle this milestone has run on
throughout: measure the thing rather than infer it from counts.** Put a probe on the DevKit's
3V3 and 5V rails and switch the panel on ONCE. Dip depth and margin to the brownout threshold
come out as numbers; sixty switch-ons give a frequency that still needs a model to read. The
Siglent rig and the four-line survey in `hardware/BRINGUP.md` already exist. **Capture the trace
to `hardware/` beside the M2 photos** — that is what turns D7 from an anecdote into evidence.
**EXPLICIT NON-CONCLUSION: none of this says the board is safe.** A ~1-in-6 brownout on panel
switch-on is a real defect. It is tolerable *today* only because the board reboots cleanly and
the panel is switched rarely. At **M7**, inside an enclosure behind a single power switch, panel
switch-on becomes the normal startup path and a one-in-six failure to boot is a broken product.
The scope measurement is what decides whether power sequencing is a nice-to-have or a
requirement of the carrier board. **Already refuted, do not re-propose:**
`hardware/BRINGUP.md:28,36` records the rails as **not tied** (panel on the LRS-50-5, DevKit on
USB, common GND only), so "give the panel its own rail" is already the case.

*Moved here 2026-08-27: this had been pasted into the **Milestones** table between the
M4 and M5 rows, with four cells against that table's six, so it rendered as a milestone with
an empty Depends-on and Status rather than as the backlog item it is. Prose unchanged — the
old `#` and `Track` cells become the heading prefix and the `[B]` tag, and the old Depends-on
and Status cells were empty.*

**D3 · Crucible post — book structures under fire.** flat_map vs dense window, driven by
Anvil's *trend* workload.

**D6 · A price-axis render window — blocked on 8 bytes, and the 8 bytes are the whole ask.**
*[A] — not M4's. Priced 2026-08-19 at stage C so that whoever wants it argues against a number
instead of rediscovering one.*

- **What it is.** A window whose rows are TICKS rather than ranks: a contiguous price band, levels
  placed in their own row, empty ticks rendered unknown. It is the only family of policy in which
  a row's vertical position means the same thing from one frame to the next, which is the property
  a human reads a ladder for.
- **What blocks it, exactly.** It needs to say *row 7 is not a level* with the unknown rows
  **interleaved**. `DisplaySnapshot::bid_count` expresses only TRAILING unknowns, so every
  rank-based policy fits it and no price-based one does.
- **The price, in full.** Two `uint32_t` present-masks carry it exactly — 27 rows fit one, with
  five bits spare. **+8 bytes on `DisplaySnapshot` (1,168 → 1,176), +24 in the three-slot mailbox
  (3,528 → 3,552)**, and three documents quoting the size to correct in the same commit:
  `firmware/src/main.cpp`'s static-footprint block, `snapshot_channel.hpp`'s cost note, and
  ARCHITECTURE §9's 2026-08-07 row. The `static_assert` in `display_snapshot.hpp` is what will
  stop anyone taking it by accident, and its comment already states the procedure.
- **The alternative that needs no field costs more.** Widening a row to span several ticks makes a
  row an AGGREGATE of levels rather than a level — which no renderer here can draw and which stage
  C's §1 forbids — and makes the row width a constant taken from the market's own span, which is
  the mistake §9 (2026-08-16 pm) is about.
- **What would justify it.** Nothing measured yet. Stage C's numbers argue the other way for the
  shipped configuration: at depth 25 into 27 rows every level is rendered by every policy, so a
  price axis would be re-arranging rows that are already all present. It becomes interesting only
  alongside a subscribed depth larger than the panel — the same condition that makes the rank
  policies differ at all. **Both decisions belong to whoever owns the depth, not to whoever owns
  the window.**
- Evidence: `harness/replay/NOTES.md` stage C addendum (the sparsity table that ruled the price
  axis out at one tick per row), ARCHITECTURE §9 2026-08-19, DESIGN §08 strain 24.

**D4 · Live web mirror** *(optional)* — a browser twin of the panel's `DisplaySnapshot` feed.
*That* would be companion site #4 and trigger the shared-component-repo review. The portfolio
page itself is scheduled work (MP) inside the portfolio repo and does **not** count toward
the threshold.

### Closed

**~~D5 · Ping the venue instead of waiting on it — the liveness watchdog gets a clock.~~**
*[A] — **DONE 2026-08-16**, host-green and building on all three firmware arms; unflashed.*
`firmware/src/ws_ping.hpp` (`PingProbe`, ESP-IDF-free) + `harness/tests/test_ws_ping.cpp`; the
client ping is on by default and `depthcharge-noping` is the control arm. The board prints a
`-- ping` line directly under `-- age`, because the pair is the diagnosis: **age high + rtt high
= our own undrained queue (A7 is the fix); age high + rtt low = lag upstream in the broadcaster
(A7 would not help).** That split is the thing no instrument here could make before.

- **Shipped with it, and it would have been a silent regression otherwise:** the silence
  recycle's clock moved from byte-arrival to **data**-arrival (`SupervisorInput::last_data_us`,
  stamped in `on_chunk`). A ping manufactures a pong every 10 s, and a pong is bytes — so the
  board's own control traffic would have kept `kSilenceRecycleUs` from ever firing against a
  peer that answers pongs and publishes nothing, which is exactly the 2026-08-16 00:12 stall.
  ARCHITECTURE §9 carries the general rule.
- **~~Still owed: the Anvil half of A2~~ — CLOSED 2026-08-16, the same day.** This said the
  ordering was read from source and never captured under induced backpressure, so the number was
  evidence rather than a guarantee. Anvil captured it — pong last of 149 frames, zero after it —
  and `PROTOCOL.md` §3 Keepalive now states it as contract. **D5 rests on a measured property.**
  Detail under A2 above.
- Bounds that do not go away: it is **blind to producer-side lag** by construction, and nothing
  branches on it — §6 #5 means a pong can never turn the panel green.

**~~M3 close-out~~** — transport brief closes, docs catch up, Anvil ask rewritten. DONE
2026-08-16 (`66e2f77`…`79c1867` plus the docs commit):
`docs/briefs/M3-closeout-transport-and-docs.md` is executed and its session log is the
hand-off. The old client's build arms are deleted, the rebuilt-window framework is the
default build, `WsSupervisor` recycles a silent socket after five minutes, and the transport
brief's DoD is ticked except the weak-node hour (D2).

**~~Own the websocket client~~** — SHIPPED. Client landed 2026-08-15 (`c52b268`), day-soak
proven, and the old client deleted from the tree 2026-08-16 (`66e2f77`). Brief:
`docs/briefs/M3-transport-own-the-websocket-client.md`, closed with its acceptance numbers in
ARCHITECTURE §9 (2026-08-16).
